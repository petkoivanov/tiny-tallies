{
  "name": "tiny-tallies-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker API for Tiny Tallies — auth, consent, progress sync",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.8",
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.0.0",
    "vitest": "^3.2.4",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "jose": "^6.1.3",
    "zod": "^3.22.4"
  }
}
