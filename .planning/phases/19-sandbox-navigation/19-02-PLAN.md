---
phase: 19-sandbox-navigation
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/components/home/ExploreCard.tsx
  - src/components/home/ExploreGrid.tsx
  - src/components/home/index.ts
  - src/screens/HomeScreen.tsx
  - src/__tests__/screens/HomeScreen.test.tsx
autonomous: true
requirements:
  - SAND-01

must_haves:
  truths:
    - "User sees a 2x3 grid of manipulative cards on the home screen below the stats section"
    - "Each card shows an emoji and short friendly name"
    - "Tapping a card navigates to the SandboxScreen with the correct manipulativeType param"
    - "Cards display a 'new' dot for unexplored manipulatives"
    - "'New' dot disappears permanently after first use"
    - "Cards have scale press feedback on tap"
    - "Start Practice button remains at the bottom after the grid"
    - "Home screen content is scrollable"
  artifacts:
    - path: "src/components/home/ExploreCard.tsx"
      provides: "Individual manipulative card with emoji, name, new dot, scale press feedback"
      exports: ["ExploreCard"]
      min_lines: 40
    - path: "src/components/home/ExploreGrid.tsx"
      provides: "2x3 grid of ExploreCards with section header"
      exports: ["ExploreGrid"]
      min_lines: 40
    - path: "src/components/home/index.ts"
      provides: "Barrel export for home components"
      exports: ["ExploreGrid", "ExploreCard"]
    - path: "src/screens/HomeScreen.tsx"
      provides: "Updated home screen with ScrollView wrapper and Explore section"
      contains: "ScrollView"
    - path: "src/__tests__/screens/HomeScreen.test.tsx"
      provides: "Extended tests covering Explore grid rendering and navigation"
      min_lines: 80
  key_links:
    - from: "src/components/home/ExploreCard.tsx"
      to: "src/store/slices/sandboxSlice.ts"
      via: "useAppStore for exploredManipulatives to show/hide new dot"
      pattern: "exploredManipulatives"
    - from: "src/components/home/ExploreGrid.tsx"
      to: "src/components/home/ExploreCard.tsx"
      via: "Renders 6 ExploreCards in difficulty order"
      pattern: "ExploreCard"
    - from: "src/components/home/ExploreCard.tsx"
      to: "src/navigation/types.ts"
      via: "navigation.navigate('Sandbox', { manipulativeType })"
      pattern: "navigate.*Sandbox"
    - from: "src/screens/HomeScreen.tsx"
      to: "src/components/home/ExploreGrid.tsx"
      via: "Renders ExploreGrid between stats section and button"
      pattern: "ExploreGrid"
---

<objective>
Create the Explore grid UI on the home screen -- a 2x3 grid of tappable manipulative cards with emoji, name, colored backgrounds, 'new' dot indicators, and scale press feedback. Wrap HomeScreen content in ScrollView.

Purpose: Provides the primary entry point for children to access the sandbox, with visual appeal and gentle encouragement ("new" dots) to try all 6 manipulatives.
Output: ExploreCard, ExploreGrid components, updated HomeScreen with ScrollView, extended tests.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-sandbox-navigation/19-CONTEXT.md
@.planning/phases/19-sandbox-navigation/19-RESEARCH.md
@.planning/phases/19-sandbox-navigation/19-01-SUMMARY.md

<interfaces>
<!-- From Plan 19-01 outputs -->

From src/store/slices/sandboxSlice.ts (created in Plan 01):
```typescript
export interface SandboxSlice {
  exploredManipulatives: ManipulativeType[];
  markExplored: (type: ManipulativeType) => void;
}
```

From src/navigation/types.ts (updated in Plan 01):
```typescript
export type RootStackParamList = {
  Home: undefined;
  Session: { sessionId: string };
  Results: { /* ... */ };
  Sandbox: { manipulativeType: ManipulativeType };
};
```

From src/services/cpa/cpaTypes.ts:
```typescript
export type ManipulativeType =
  | 'base_ten_blocks' | 'number_line' | 'fraction_strips'
  | 'counters' | 'ten_frame' | 'bar_model';
```

From src/theme/index.ts:
```typescript
export const colors = { background: '#1a1a2e', backgroundLight: '#16213e', surface: '#0f3460', surfaceLight: '#1a4a7a', primary: '#6366f1', primaryLight: '#818cf8', primaryDark: '#4f46e5', textPrimary: '#ffffff', textSecondary: '#cbd5e1', textMuted: '#64748b' };
export const spacing = { xs: 4, sm: 8, md: 16, lg: 24, xl: 32, xxl: 48 };
export const typography = { fontFamily: { regular: 'Lexend_400Regular', medium: 'Lexend_500Medium', semiBold: 'Lexend_600SemiBold', bold: 'Lexend_700Bold' }, fontSize: { xs: 12, sm: 14, md: 16, lg: 20, xl: 24, xxl: 32, display: 48 } };
export const layout = { borderRadius: { sm: 8, md: 12, lg: 16, xl: 24, round: 9999 }, minTouchTarget: 48 };
```

From src/screens/HomeScreen.tsx (current 238 lines -- will be modified):
- Uses `flex: 1` container with profileSection, statsSection (flex: 1), buttonSection
- statsSection has `flex: 1` which pushes button to bottom -- this must change with ScrollView
- Press pattern: `style={({ pressed }) => [styles.button, pressed && styles.buttonPressed]}`
- Imports: colors, spacing, typography, layout from @/theme
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExploreCard and ExploreGrid components</name>
  <files>
    src/components/home/ExploreCard.tsx,
    src/components/home/ExploreGrid.tsx,
    src/components/home/index.ts
  </files>
  <behavior>
    - ExploreCard renders emoji and display name text
    - ExploreCard shows 'new' dot when manipulative is NOT in exploredManipulatives
    - ExploreCard hides 'new' dot when manipulative IS in exploredManipulatives
    - ExploreCard calls onPress callback when tapped
    - ExploreGrid renders all 6 cards in difficulty order: counters, ten_frame, base_ten_blocks, number_line, fraction_strips, bar_model
    - ExploreGrid shows "Explore" section header
  </behavior>
  <action>
    1. Create `src/components/home/ExploreCard.tsx`:
       - Props: `{ type: ManipulativeType; emoji: string; name: string; bgColor: string; isNew: boolean; onPress: () => void }`
       - Render a Pressable card with:
         - Background color from `bgColor` prop (unique per manipulative)
         - Scale press feedback: `style={({ pressed }) => [styles.card, { backgroundColor: bgColor }, pressed && { transform: [{ scale: 0.95 }] }]}`
         - Emoji text (fontSize ~36) centered
         - Name text below emoji (typography.fontFamily.semiBold, fontSize.md, colors.textPrimary)
         - 'New' dot: small circle (10x10, `colors.primaryLight` background, borderRadius round) positioned absolute top-right corner. Rendered conditionally when `isNew` is true.
         - Card proportions: use `width: '47%'` with `aspectRatio: 1` for square cards (Claude's discretion per CONTEXT)
         - `borderRadius: layout.borderRadius.lg`
         - `accessibilityLabel={name}`, `accessibilityRole="button"`
       - All styles via `StyleSheet.create`

    2. Create `src/components/home/ExploreGrid.tsx`:
       - Props: none (reads from store and uses navigation internally)
       - Define `EXPLORE_ITEMS` ordered array (difficulty order per user decision):
         ```
         [
           { type: 'counters', emoji: '🔴', name: 'Counters', bgColor: '#2a1a1a' },
           { type: 'ten_frame', emoji: '🔟', name: 'Ten Frame', bgColor: '#1a2a3a' },
           { type: 'base_ten_blocks', emoji: '🧱', name: 'Blocks', bgColor: '#1a2a2a' },
           { type: 'number_line', emoji: '📏', name: 'Number Line', bgColor: '#2a2a1a' },
           { type: 'fraction_strips', emoji: '🍕', name: 'Fractions', bgColor: '#2a1a2a' },
           { type: 'bar_model', emoji: '📊', name: 'Bar Model', bgColor: '#1a2a2a' },
         ]
         ```
         Background colors: Use muted dark tones that provide subtle accent differentiation against the #1a1a2e background. Each should be distinct but not jarring -- think 15% opacity tint of the manipulative's signature color.
       - Read `exploredManipulatives` from store via `useAppStore`
       - Get navigation via `useNavigation()`
       - Render section header: "Explore" label using `typography.fontFamily.semiBold`, `typography.fontSize.xl`, `colors.textPrimary`, with `marginBottom: spacing.md`
       - Render a View with `flexDirection: 'row', flexWrap: 'wrap', gap: spacing.md, justifyContent: 'space-between'`
       - Map over `EXPLORE_ITEMS` and render `ExploreCard` for each, with `isNew = !exploredManipulatives.includes(item.type)`
       - onPress: `navigation.navigate('Sandbox', { manipulativeType: item.type })`
       - All styles via `StyleSheet.create`

    3. Create `src/components/home/index.ts`:
       - Barrel export: `export { ExploreGrid } from './ExploreGrid';`
       - Also export: `export { ExploreCard } from './ExploreCard';`
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>ExploreCard renders emoji + name + new dot + scale press, ExploreGrid renders 6 cards in difficulty order with "Explore" header, TypeScript compiles cleanly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HomeScreen ScrollView integration and test extensions</name>
  <files>
    src/screens/HomeScreen.tsx,
    src/__tests__/screens/HomeScreen.test.tsx
  </files>
  <behavior>
    - HomeScreen renders an "Explore" section header
    - HomeScreen renders 6 manipulative cards (one per type)
    - Tapping a card navigates to Sandbox with correct manipulativeType
    - HomeScreen content is in a ScrollView
    - Start Practice button remains visible below the Explore grid
    - 'New' dot shows on cards for unexplored manipulatives
    - 'New' dot is hidden for explored manipulatives
  </behavior>
  <action>
    1. Modify `src/screens/HomeScreen.tsx`:
       - Add `ScrollView` to imports from 'react-native'
       - Import `ExploreGrid` from `@/components/home`
       - Replace the outer `View` (flex: 1 container) with `ScrollView`:
         - Keep `style={[styles.container, { paddingTop: insets.top }]}`
         - Add `contentContainerStyle={styles.scrollContent}` with `{ flexGrow: 1, paddingBottom: insets.bottom }`
         - Remove `paddingBottom: insets.bottom` from the outer style (move to contentContainerStyle)
       - Remove `flex: 1` from `statsSection` style -- no longer needed to push button down; ScrollView handles layout naturally
       - Insert `<ExploreGrid />` between the statsSection View and the buttonSection View
       - Add `paddingTop: spacing.lg` to an optional wrapper or margin above ExploreGrid for visual spacing from stats
       - The section header "Explore" is inside ExploreGrid (not in HomeScreen) per component encapsulation
       - Wrap the ExploreGrid in a View with `paddingHorizontal: spacing.lg` for consistent side padding
       - Start Practice button stays in buttonSection at the bottom -- `flexGrow: 1` on contentContainerStyle + no spacer element keeps it at the end of content
       - Verify file stays under 500 lines (currently 238, adding ~10 lines for imports + ExploreGrid placement)

    2. Extend `src/__tests__/screens/HomeScreen.test.tsx`:
       - Add `exploredManipulatives: []` to the default mockStoreState in `setMockState()`
       - Add `markExplored: jest.fn()` to mockStoreState
       - Mock `@/components/home` module: `ExploreGrid` as a View with testID 'explore-grid' that renders children representing cards. Alternatively, do NOT mock ExploreGrid and let it render -- but then mock `@/store/appStore` comprehensively. The simpler approach: mock `@/components/home` so ExploreGrid is a placeholder View with testID.
       - New tests:
         - "renders Explore section" -- check for explore-grid testID
         - "renders Start Practice button below explore section" -- getByLabelText('Start Practice') still works
         - "navigates to Sandbox when explore card tapped" -- if ExploreGrid is NOT mocked, verify navigation; if mocked, this test belongs in ExploreGrid's own test file. Since ExploreGrid handles navigation internally, mock it as a View and verify it renders.
       - Ensure all existing tests still pass (greeting, level, XP, streak, Start Practice navigation)
       - Run: `npm test -- --testPathPattern=HomeScreen`
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=HomeScreen</automated>
  </verify>
  <done>HomeScreen wrapped in ScrollView, ExploreGrid rendered between stats and button, Start Practice remains at bottom, all existing HomeScreen tests pass, new tests confirm Explore section renders, file under 500 lines</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm test -- --testPathPattern="HomeScreen"` -- all tests pass (existing + new)
2. `npm run typecheck` -- no TypeScript errors
3. `npm test` -- full suite passes (no regressions from HomeScreen changes)
4. HomeScreen.tsx stays under 500 lines
5. ExploreGrid renders 6 cards in correct difficulty order
6. Cards show "new" dot for unvisited manipulatives
</verification>

<success_criteria>
- Home screen shows "Explore" section header below stats
- 2x3 grid of cards rendered: Counters, Ten Frame, Blocks, Number Line, Fractions, Bar Model (difficulty order)
- Each card has emoji + name + unique accent background
- 'New' dot appears on unexplored cards, disappears after first visit
- Scale press feedback on card tap
- Tapping a card navigates to Sandbox with correct manipulativeType
- HomeScreen wrapped in ScrollView with flexGrow: 1
- Start Practice button at bottom after grid
- All tests pass, TypeScript clean, HomeScreen under 500 lines
</success_criteria>

<output>
After completion, create `.planning/phases/19-sandbox-navigation/19-02-SUMMARY.md`
</output>
