---
phase: 090-logarithms-domain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/mathEngine/logarithms.test.ts
  - src/__tests__/mathEngine/domainHandlerRegistry.test.ts
  - src/__tests__/adaptive/prerequisiteGating.test.ts
  - src/__tests__/mathEngine/wordProblems.test.ts
autonomous: true
requirements: [LOG-01, LOG-02]

must_haves:
  truths:
    - "logarithms.test.ts has RED test stubs that fail at import/module level"
    - "domainHandlerRegistry.test.ts expects 27 operations and 201 skills"
    - "prerequisiteGating.test.ts expects 201 skills"
    - "wordProblems.test.ts includes logarithms in ALL_OPERATIONS"
  artifacts:
    - path: "src/__tests__/mathEngine/logarithms.test.ts"
      provides: "RED test stubs for logarithms domain (4 skills, 3 bugs, numeric answers)"
      min_lines: 80
  key_links:
    - from: "src/__tests__/mathEngine/logarithms.test.ts"
      to: "src/services/mathEngine/domains/logarithms"
      via: "import of handler, skills, templates, bugs"
      pattern: "import.*logarithms"
---

<objective>
Create RED test stubs for the logarithms domain. Update count assertions in existing test files.

Purpose: Establish failing test contracts that Plan 02 must satisfy -- Wave 0 RED stubs for TDD.
Output: 1 new test file + 3 updated test files, all failing until Plan 02 lands.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/090-logarithms-domain/090-RESEARCH.md

Reference prior Wave 0 pattern:
@.planning/phases/089-exponential-functions-domain/089-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RED test stubs for logarithms domain</name>
  <files>src/__tests__/mathEngine/logarithms.test.ts</files>
  <action>
Create a new test file following the Wave 0 RED stub pattern from prior HS domains (model on exponentialFunctions.test.ts).

- Import from `@/services/mathEngine/domains/logarithms` (handler)
- Import from `@/services/mathEngine/skills/logarithms` (LOGARITHMS_SKILLS)
- Import from `@/services/mathEngine/templates/logarithms` (LOGARITHMS_TEMPLATES)
- Import from `@/services/mathEngine/bugLibrary/logarithmsBugs` (LOGARITHMS_BUGS)
- Import `createRng` from `@/services/mathEngine/seededRng`
- Tests to stub:
  1. "should be registered in domain registry" (handler exists)
  2. "should have 4 skills" (LOGARITHMS_SKILLS.length === 4)
  3. "should have 4 templates" (LOGARITHMS_TEMPLATES.length === 4)
  4. "should have 3 bug patterns" (LOGARITHMS_BUGS.length === 3)
  5. "all skills use operation logarithms" (every skill.operation === 'logarithms')
  6. "all templates use operation logarithms" (every template.operation === 'logarithms')
  7. "all templates use domain_specific distractor strategy" (every template.distractorStrategy === 'domain_specific')
  8-11. One test per generator type: log10_eval, log2_eval, ln_eval, word_problem -- each creates a template with the right domainConfig.type, calls handler.generate(template, rng), and checks:
    - result.correctAnswer.type === 'numeric'
    - result.questionText is non-empty string
    - result.operands.length >= 3
  12. "log10_eval produces correct exponent as answer" -- generate with known seed, verify answer equals the exponent n where argument = 10^n
  13. "log2_eval produces correct exponent as answer" -- generate with known seed, verify answer equals the exponent n where argument = 2^n
  14. "ln_eval displays as ln(e^n) not numeric argument" -- verify questionText contains "ln(e" and does NOT contain a large numeric argument
  15-17. One test per bug pattern (log_gave_argument, log_off_by_one, log_confused_base): compute() returns a non-null number
  18. "all generators produce integer answers" -- run 20 seeds across all 4 generator types, all correctAnswer.value are integers (Number.isInteger)
  19. "all answers are bounded under 2000" -- run 20 seeds, verify answerNumericValue(result.correctAnswer) < 2000
  20. "log10_eval answer range is 1-6" -- run 20 seeds, verify all answers in [1, 6]
  21. "log2_eval answer range is 1-10" -- run 20 seeds, verify all answers in [1, 10]
  22. "ln_eval answer range is 1-5" -- run 20 seeds, verify all answers in [1, 5]

These imports target files that don't exist yet, so all tests will fail at import/module resolution level (RED).
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test -- --testPathPattern="logarithms" 2>&1 | head -30</automated>
  </verify>
  <done>Test file exists and fails at import level (RED). logarithms.test.ts has ~22 test cases covering registry, skills, templates, bugs, generator output types, integer answers, answer bounds, and per-skill answer ranges.</done>
</task>

<task type="auto">
  <name>Task 2: Update count assertions in 3 existing test files</name>
  <files>src/__tests__/mathEngine/domainHandlerRegistry.test.ts, src/__tests__/adaptive/prerequisiteGating.test.ts, src/__tests__/mathEngine/wordProblems.test.ts</files>
  <action>
Update count assertions to reflect the addition of logarithms (4 skills, 1 new operation).

**domainHandlerRegistry.test.ts:**
- Change operation count assertion from 26 to 27 (in the test name and assertion)
- Change total skill count assertion from 197 to 201
- Add `'logarithms'` to the ALL_OPERATIONS array
- Add `logarithms: ['numeric']` to the expectedTypes map (all logarithms skills use numericAnswer)

**prerequisiteGating.test.ts:**
- Change SKILLS.length assertion from 197 to 201

**wordProblems.test.ts:**
- Add `'logarithms'` to the ALL_OPERATIONS array
- Add `logarithms: 10` to the gradeMap (Common Core HSF-BF standards start in grade 10)

These tests will now fail because the production code still has 26 operations and 197 skills. They will pass after Plan 02 lands.
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test -- --testPathPattern="domainHandlerRegistry|prerequisiteGating|wordProblems" 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 files updated with new counts. Tests fail (RED) because logarithms handler doesn't exist yet. domainHandlerRegistry expects 27 ops/201 skills, prerequisiteGating expects 201, wordProblems includes logarithms.</done>
</task>

</tasks>

<verification>
All 4 test files should exist and produce RED failures:
- logarithms.test.ts: fails at import (module not found)
- domainHandlerRegistry.test.ts: fails on count assertions (26 != 27, 197 != 201)
- prerequisiteGating.test.ts: fails on count assertion (197 != 201)
- wordProblems.test.ts: fails on missing logarithms in registry
</verification>

<success_criteria>
- 1 new test file created with RED stubs
- 3 existing test files updated with new counts
- All tests fail (RED) as expected
- Test contracts match the 4 skills, 4 templates, 3 bugs, numeric answer type documented in research
</success_criteria>

<output>
After completion, create `.planning/phases/090-logarithms-domain/090-01-SUMMARY.md`
</output>
