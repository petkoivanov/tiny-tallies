---
phase: 090-logarithms-domain
plan: 03
type: execute
wave: 3
depends_on: [090-02]
files_modified:
  - src/services/mathEngine/wordProblems/templates.ts
autonomous: true
requirements: [LOG-03, LOG-04]

must_haves:
  truths:
    - "Word problem templates for logarithms generate without error"
    - "All word problem templates use prefix mode exclusively"
    - "AI tutor hints for logarithms guide students toward 'what power gives this result' reasoning without stating the answer"
  artifacts:
    - path: "src/services/mathEngine/wordProblems/templates.ts"
      provides: "3 prefix-mode word problem templates for logarithms domain"
      contains: "logarithms"
  key_links:
    - from: "src/services/mathEngine/wordProblems/templates.ts"
      to: "src/services/mathEngine/domains/logarithms/generators.ts"
      via: "prefix mode word problem template references logarithms operations"
      pattern: "operation.*logarithms"
---

<objective>
Add 3 prefix-mode word problem templates for the logarithms domain and verify AI tutor QA.

Purpose: Complete LOG-03 (word problems) and LOG-04 (AI tutor guidance). Phase 90 done.
Output: 3 new word problem templates in templates.ts. Manual QA auto-approved per user pre-authorization.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/090-logarithms-domain/090-RESEARCH.md
@.planning/phases/090-logarithms-domain/090-02-SUMMARY.md

Reference prior word problem pattern:
@.planning/phases/089-exponential-functions-domain/089-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 prefix-mode word problem templates for logarithms</name>
  <files>src/services/mathEngine/wordProblems/templates.ts</files>
  <action>
Add 3 word problem templates for logarithms to the WORD_PROBLEM_TEMPLATES array in templates.ts. All use prefix mode exclusively (same Pitfall 5 pattern as all prior HS domains).

Template contexts (all at minGrade 10, targeting real-world logarithmic scale scenarios):

1. **pH scale** (minGrade 10, skillFilter: 'log10_eval'):
   - mode: 'prefix'
   - prefix: "A chemist measures the hydrogen ion concentration of a solution. The pH is calculated using log base 10."
   - operation: 'logarithms'
   - Reuses the log10_eval generator which produces log base 10 problems.

2. **Decibel scale** (minGrade 10, skillFilter: 'log10_eval'):
   - mode: 'prefix'
   - prefix: "A sound engineer is measuring noise levels. The decibel scale uses log base 10 to compare sound intensities."
   - operation: 'logarithms'
   - Reuses the log10_eval generator.

3. **Richter scale** (minGrade 10, skillFilter: 'log10_eval'):
   - mode: 'prefix'
   - prefix: "A seismologist is analyzing earthquake data. The Richter scale uses log base 10 to measure earthquake magnitude."
   - operation: 'logarithms'
   - Reuses the log10_eval generator.

Follow the exact template structure from prior HS domains (exponential_functions, polynomials, etc.). Each template object has: id, operation, mode, prefix, minGrade, skillFilter.
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test -- --testPathPattern="wordProblems" 2>&1 | tail -20</automated>
  </verify>
  <done>3 prefix-mode word problem templates added. wordProblems tests pass including logarithms in ALL_OPERATIONS. AI tutor QA auto-approved per user pre-authorization (manual QA sign-off). Phase 90 LOG-03 and LOG-04 complete.</done>
</task>

</tasks>

<verification>
1. `npm test -- --testPathPattern=wordProblems` -- logarithms word problems generate without error
2. `npm test -- --testPathPattern=logarithms` -- all domain tests still pass
3. `npm test` -- full suite green
4. `npm run typecheck` -- clean
</verification>

<success_criteria>
- 3 prefix-mode word problem templates exist for logarithms domain
- All word problem tests pass including logarithms
- AI tutor QA auto-approved (manual QA sign-off per user pre-authorization)
- Full test suite passes
- Phase 90 complete: LOG-01 through LOG-04 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/090-logarithms-domain/090-03-SUMMARY.md`
</output>
