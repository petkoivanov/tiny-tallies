---
phase: 34-visual-skill-map
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - src/components/skillMap/SkillDetailOverlay.tsx
  - src/components/skillMap/index.ts
  - src/screens/SkillMapScreen.tsx
  - src/__tests__/components/SkillDetailOverlay.test.tsx
autonomous: true
requirements:
  - SMAP-03
  - SMAP-04

must_haves:
  truths:
    - "User can tap any skill node and see a detail overlay with skill information"
    - "In-progress/mastered skills show colorful progress bar and 1-6 practice level stars"
    - "Locked skills show prerequisite names with completion status checkmarks"
    - "Overlay dismisses on tap outside or back button"
    - "BKT probability drives the progress bar but is not shown as a raw number"
  artifacts:
    - path: "src/components/skillMap/SkillDetailOverlay.tsx"
      provides: "Modal overlay with skill mastery details, progress bar, stars, prereq checklist"
      min_lines: 100
    - path: "src/screens/SkillMapScreen.tsx"
      provides: "Complete screen with graph + detail overlay wired together"
      contains: "SkillDetailOverlay"
  key_links:
    - from: "src/screens/SkillMapScreen.tsx"
      to: "src/components/skillMap/SkillDetailOverlay.tsx"
      via: "selectedSkillId state drives overlay visibility"
      pattern: "selectedSkillId.*SkillDetailOverlay"
    - from: "src/components/skillMap/SkillDetailOverlay.tsx"
      to: "src/store/helpers/skillStateHelpers.ts"
      via: "getOrCreateSkillState for skill data"
      pattern: "getOrCreateSkillState"
---

<objective>
Create the SkillDetailOverlay modal component showing child-friendly skill mastery details (progress bar, practice level stars, prerequisite checklist), and wire it into SkillMapScreen so tapping any node opens the overlay.

Purpose: Completes the interactive experience -- the map is informational and this overlay gives children concrete understanding of their progress and what to work on next.
Output: SkillDetailOverlay component, full SkillMapScreen wiring, tests.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-visual-skill-map/34-CONTEXT.md
@.planning/phases/34-visual-skill-map/34-RESEARCH.md
@.planning/phases/34-visual-skill-map/34-02-SUMMARY.md

@src/components/skillMap/skillMapTypes.ts
@src/components/skillMap/skillMapColors.ts
@src/components/skillMap/skillMapLayout.ts
@src/components/skillMap/SkillMapGraph.tsx
@src/components/skillMap/index.ts
@src/screens/SkillMapScreen.tsx
@src/components/badges/BadgeDetailOverlay.tsx
@src/theme/index.ts
@src/services/mathEngine/skills.ts
@src/services/adaptive/prerequisiteGating.ts
@src/store/helpers/skillStateHelpers.ts
@src/store/slices/skillStatesSlice.ts

<interfaces>
<!-- From Plan 01/02 outputs and existing codebase -->

From src/components/skillMap/skillMapTypes.ts:
```typescript
export type NodeState = 'locked' | 'unlocked' | 'in-progress' | 'mastered';
export interface SkillNodeData { skillId: string; name: string; operation: Operation; grade: Grade;
  position: NodePosition; state: NodeState; masteryProbability: number; isOuterFringe: boolean; }
```

From src/components/skillMap/skillMapLayout.ts:
```typescript
export function getNodeState(skillId: string, skillStates: Record<string, SkillState>): NodeState;
```

From src/services/mathEngine/skills.ts:
```typescript
export function getSkillById(id: string): SkillDefinition | undefined;
// SkillDefinition: { id, name, operation, grade, prerequisites: string[] }
```

From src/store/helpers/skillStateHelpers.ts:
```typescript
export function getOrCreateSkillState(skillStates: Record<string, SkillState>, skillId: string): SkillState;
// SkillState: { masteryProbability, masteryLocked, leitnerBox (1-6), attempts, ... }
```

From src/services/adaptive/prerequisiteGating.ts:
```typescript
export function isSkillUnlocked(skillId: string, skillStates: Record<string, SkillState>): boolean;
```

BadgeDetailOverlay pattern (Modal + Pressable backdrop):
```typescript
<Modal transparent animationType="fade" onRequestClose={onClose}>
  <Pressable style={styles.backdrop} onPress={onClose}>
    <Pressable style={styles.card} onPress={() => {}}>
      {/* content */}
    </Pressable>
  </Pressable>
</Modal>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SkillDetailOverlay component with child-friendly data presentation</name>
  <files>
    src/components/skillMap/SkillDetailOverlay.tsx,
    src/__tests__/components/SkillDetailOverlay.test.tsx
  </files>
  <behavior>
    - Renders nothing (null) when skillId is null
    - Shows skill name and operation emoji for a valid skill
    - For in-progress skill: shows "How well you know this!" label with progress bar filled proportional to masteryProbability
    - For in-progress skill: shows "Practice level" with N filled stars out of 6 based on leitnerBox
    - For mastered skill: shows fully filled progress bar and congratulatory text
    - For locked skill: shows prerequisite names with checkmark for mastered prereqs and X for unmastered
    - For unlocked (no attempts) skill: shows "Ready to learn!" message with empty progress bar
    - BKT probability is NOT shown as a number anywhere
    - Dismisses when backdrop is pressed (onClose called)
    - Has close button in top-right corner
  </behavior>
  <action>
    Create `src/components/skillMap/SkillDetailOverlay.tsx` (~140-180 lines):

    Props: `{ skillId: string | null; skillStates: Record<string, SkillState>; onClose: () => void }`

    **Layout** (follows BadgeDetailOverlay centered modal pattern exactly):
    - `Modal` transparent, animationType="fade", onRequestClose={onClose}
    - Outer `Pressable` (backdrop, flex:1, rgba(0,0,0,0.6), centered) onPress={onClose}
    - Inner `Pressable` (card, backgroundColor: colors.surface, borderRadius: lg, padding: lg, width: 80%, maxWidth: 320) onPress={() => {}} to prevent dismissal
    - Close button: absolute top-right, 32x32 rounded, "X" text

    **Content (conditional on state from getNodeState):**

    All states show:
    - Large operation emoji (+ or -) in a colored circle at top (use operation tint from skillMapColors)
    - Skill name in bold white text
    - Grade label in secondary text ("Grade {n}")

    **Locked state:**
    - Divider line
    - "Complete these skills first:" header text (secondary color)
    - For each prerequisite (from `getSkillById(skillId).prerequisites`):
      - Row with: checkmark emoji (green) if prereq is mastered (getNodeState returns 'mastered'), X emoji (red) if not, followed by prereq skill name (from `getSkillById(prereqId).name`)
    - No progress bar or stars shown for locked skills

    **Unlocked state (attempts === 0):**
    - "Ready to learn!" text in primaryLight color
    - Empty progress bar (0% fill)
    - Practice level: 1 star filled out of 6

    **In-progress state:**
    - "How well you know this!" label (textSecondary)
    - Progress bar: View with height 12, borderRadius round, background=surfaceLight. Inner fill View with width = `${masteryProbability * 100}%`, background=operation tint light color, min 2% when > 0
    - "Practice level" label (textSecondary)
    - Stars row: 6 star positions. Filled stars (1 to leitnerBox) use gold (#fbbf24), unfilled stars use gray (#475569). Use Star icon from lucide-react-native at size 20 or render as SvgText emoji. Stars must be visual stepping stones, not raw numbers.

    **Mastered state:**
    - Star or trophy emoji large
    - "Mastered!" text in gold color
    - Full progress bar (100% fill, gold color)
    - All 6 stars filled in gold
    - "You're a pro at this!" encouraging text

    Use `StyleSheet.create` for all styles. Import colors, spacing, typography, layout from @/theme. Import `getSkillById` from skills.ts, `getOrCreateSkillState` from skillStateHelpers, `getNodeState` from skillMapLayout, `skillMapColors` from skillMapColors.

    Create `src/__tests__/components/SkillDetailOverlay.test.tsx`:
    - Mock react-native-svg (not needed but just in case), lucide-react-native (Star, Lock, Check icons), store if needed
    - Mock `@/services/mathEngine/skills` with getSkillById returning test skill definitions
    - Mock `@/services/adaptive/prerequisiteGating` with isSkillUnlocked
    - Mock `@/store/helpers/skillStateHelpers` with getOrCreateSkillState

    Tests:
    - "returns null when skillId is null"
    - "shows skill name and operation emoji for in-progress skill"
    - "shows progress bar for in-progress skill" -- check "How well you know this!" text exists
    - "shows practice level stars" -- check "Practice level" text exists
    - "shows prerequisite checklist for locked skill" -- check "Complete these skills first" text
    - "shows mastered celebration for mastered skill" -- check "Mastered!" text
    - "calls onClose when backdrop pressed"
    - "does not show raw BKT number" -- queryByText matching /0\.\d+/ returns null
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=SkillDetailOverlay</automated>
  </verify>
  <done>
    - SkillDetailOverlay renders correct content for all 4 skill states
    - Progress bar driven by masteryProbability, no raw numbers shown
    - Stars reflect leitnerBox value (1-6)
    - Locked skills show prerequisite checklist with completion status
    - All 8 tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire SkillDetailOverlay into SkillMapScreen and run full test suite</name>
  <files>
    src/screens/SkillMapScreen.tsx,
    src/components/skillMap/index.ts,
    src/__tests__/screens/SkillMapScreen.test.tsx
  </files>
  <action>
    Update `src/components/skillMap/index.ts`:
    - Add SkillDetailOverlay export

    Update `src/screens/SkillMapScreen.tsx`:
    - Import SkillDetailOverlay from '@/components/skillMap'
    - The `selectedSkillId` state already exists from Plan 02. The `onNodePress` callback in SkillMapGraph already sets `selectedSkillId`.
    - Render `<SkillDetailOverlay skillId={selectedSkillId} skillStates={skillStates} onClose={() => setSelectedSkillId(null)} />` AFTER the SkillMapGraph container (at the screen level, not inside SVG).
    - The overlay uses React Native Modal so it renders above everything.

    Update `src/__tests__/screens/SkillMapScreen.test.tsx`:
    - Add SkillDetailOverlay to the component mock or let it render
    - Add test: "opens detail overlay when node is tapped" -- simulate layout event, find a node tap target pressable by testID, fireEvent.press, verify overlay content appears (e.g. skill name text or overlay testID)
    - Add test: "closes detail overlay when backdrop pressed" -- open overlay, press backdrop, verify overlay content disappears

    Run full suite validation:
    - `npm test` -- all 1,249+ tests must pass
    - `npm run typecheck` -- TypeScript clean
    - Verify no file exceeds 500 lines
  </action>
  <verify>
    <automated>npm test && npm run typecheck</automated>
  </verify>
  <done>
    - Tapping a node opens SkillDetailOverlay with that skill's data
    - Tapping backdrop or close button dismisses overlay
    - All existing tests still pass (1,249+)
    - TypeScript compiles clean
    - No file exceeds 500-line guardrail
    - Phase 34 is functionally complete
  </done>
</task>

</tasks>

<verification>
npm test && npm run typecheck
</verification>

<success_criteria>
- User can tap any of the 14 skill nodes to see a detail overlay
- In-progress skills show colorful progress bar (not raw BKT number) and 1-6 practice stars
- Locked skills show prerequisite names with completion checkmarks
- Mastered skills show celebratory gold display
- Overlay dismisses on tap outside, close button, or Android back
- All tests pass (existing + new), TypeScript clean
- No files exceed 500 lines
</success_criteria>

<output>
After completion, create `.planning/phases/34-visual-skill-map/34-03-SUMMARY.md`
</output>
