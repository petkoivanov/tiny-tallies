---
phase: 086-systems-of-equations-domain
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/mathEngine/systemsEquations.test.ts
  - src/__tests__/mathEngine/domainHandlerRegistry.test.ts
  - src/__tests__/adaptive/prerequisiteGating.test.ts
  - src/__tests__/mathEngine/wordProblems.test.ts
autonomous: true
requirements: [SYS-01, SYS-02]

must_haves:
  truths:
    - "RED test stubs exist for systemsEquations and fail with import/registry errors (not syntax errors)"
    - "domainHandlerRegistry.test.ts expects 23 operations and 180 skills — fails until Wave 1 lands"
    - "prerequisiteGating.test.ts expects 180 skills — fails until Wave 1 lands"
    - "wordProblems.test.ts ALL_OPERATIONS includes 'systems_equations' — fails until Wave 1 lands"
  artifacts:
    - path: "src/__tests__/mathEngine/systemsEquations.test.ts"
      provides: "RED stubs for SYS-01 (handler, skills, templates, generation) and SYS-02 (distractors, bug IDs)"
    - path: "src/__tests__/mathEngine/domainHandlerRegistry.test.ts"
      provides: "Updated operation count (22→23) and skill count (175→180) with systems_equations in ALL_OPERATIONS and expectedTypes"
    - path: "src/__tests__/adaptive/prerequisiteGating.test.ts"
      provides: "Updated skill count assertion (175→180)"
    - path: "src/__tests__/mathEngine/wordProblems.test.ts"
      provides: "'systems_equations' added to ALL_OPERATIONS array"
  key_links:
    - from: "systemsEquations.test.ts"
      to: "src/services/mathEngine/bugLibrary/index.ts"
      via: "import SYSTEMS_EQUATIONS_BUGS"
      pattern: "SYSTEMS_EQUATIONS_BUGS"
    - from: "domainHandlerRegistry.test.ts"
      to: "MathDomain union"
      via: "'systems_equations' in ALL_OPERATIONS"
      pattern: "systems_equations"
---

<objective>
Create failing (RED) test stubs for the systems_equations domain and update three existing test files with the new counts. These stubs document the contract that Wave 1 must satisfy.

Purpose: Establish the Nyquist-compliant test baseline before any production code is written. All tests in this plan are expected to FAIL until Plan 02 lands.
Output: 1 new test file + 3 count-updated test files, all failing at import/registry level.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/086-systems-of-equations-domain/086-RESEARCH.md
@.planning/phases/086-systems-of-equations-domain/086-VALIDATION.md

<interfaces>
<!-- Reference: existing test files used as shape templates -->

From src/__tests__/mathEngine/statisticsHs.test.ts (shape to follow exactly):
- Import pattern: createRng, getHandler, getSkillsByOperation, getTemplatesBySkill, STATISTICS_HS_BUGS (→ SYSTEMS_EQUATIONS_BUGS), answerNumericValue, MathDomain
- OPERATION constant typed as `MathDomain`
- EXPECTED_SKILL_IDS array (5 skills)
- describe blocks: 'registry', 'skills', 'handler generation — integer answers across all skills', 'bug library', 'distractor generation'
- integer answers loop: seeds 1-20, answerNumericValue(result.correctAnswer), Number.isInteger(value)
- distractor test: generateDistractors(problem, createRng(1), 3, 'domain_specific'), expects 3 distractors, none equal to correct answer

From src/__tests__/mathEngine/domainHandlerRegistry.test.ts:
- ALL_OPERATIONS: MathDomain[] — currently 22 entries ending with 'statistics_hs'
- 'has a handler registered for all 22 operations' → change string to 23, add 'systems_equations' to array
- 'all skills are covered across the 22 domains' → change string to 23, change toBe(175) to toBe(180)
- expectedTypes: add `systems_equations: ['numeric']`

From src/__tests__/adaptive/prerequisiteGating.test.ts:
- Single line: expect(SKILLS.length).toBe(175) → change to toBe(180)

From src/__tests__/mathEngine/wordProblems.test.ts:
- ALL_OPERATIONS array currently ends with 'statistics_hs' on line 20
- Add 'systems_equations' after 'statistics_hs'
- The wordProblems test also has a minWordProblemCount map entry for statistics_hs: 9
  → add systems_equations: 9 entry in that map

Skill IDs for systems_equations (5 skills):
  'substitution_simple', 'substitution_general', 'elimination_add', 'elimination_multiply', 'sys_word_problem'

Bug IDs for SYSTEMS_EQUATIONS_BUGS (3 bugs):
  'sys_swapped_xy', 'sys_sign_error', 'sys_forgot_back_sub'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RED test stubs — systemsEquations.test.ts</name>
  <files>src/__tests__/mathEngine/systemsEquations.test.ts</files>
  <action>
Create a new test file modelled on statisticsHs.test.ts. The file must:

1. Import from the not-yet-existing module paths so imports themselves cause RED failures:
   ```typescript
   import { SYSTEMS_EQUATIONS_BUGS } from '../../services/mathEngine/bugLibrary';
   ```
   (All other imports already exist: createRng, getHandler, getSkillsByOperation, getTemplatesBySkill, answerNumericValue, MathDomain)

2. Set OPERATION = 'systems_equations' as MathDomain (will fail TypeScript until types.ts is updated in Plan 02)

3. EXPECTED_SKILL_IDS = ['substitution_simple', 'substitution_general', 'elimination_add', 'elimination_multiply', 'sys_word_problem'] as const

4. describe blocks with these exact tests:

   **'registry'**
   - 'handler is registered and defined for systems_equations' — getHandler(OPERATION), expects toBeDefined()

   **'skills'**
   - 'has exactly 5 skills' — getSkillsByOperation(OPERATION).length toBe(5)
   - it.each(EXPECTED_SKILL_IDS) 'skill "%s" exists' — checks ids.toContain(skillId)
   - 'every skill has at least one template' — loops skills, getTemplatesBySkill(skill.id).length toBeGreaterThanOrEqual(1)
   - 'every template has distractorStrategy === "domain_specific"' — loops all templates, checks template.distractorStrategy toBe('domain_specific')

   **'handler generation — integer answers across all skills'**
   - it.each(EXPECTED_SKILL_IDS) 'skill "%s" produces integer-valued correctAnswer across seeds 1-20'
   - seeds 1-20, answerNumericValue(result.correctAnswer), Number.isInteger(value) toBe(true)

   **'handler generation — substitution_simple answer bounds'**
   - 'substitution_simple answers are bounded in range [1, 8] across seeds 1-20'
   - checks value >= 1 and value <= 8

   **'bug library'**
   - 'SYSTEMS_EQUATIONS_BUGS has exactly 3 entries' — toHaveLength(3)
   - 'has expected bug IDs' — checks 'sys_swapped_xy', 'sys_sign_error', 'sys_forgot_back_sub' with toContain
   - 'each bug has a non-empty description string' — loops, checks typeof bug.description toBe('string') and length > 0
   - 'all patterns target systems_equations operation' — loops, checks bug.operations.toContain(OPERATION)

   **'distractor generation'**
   - 'generateDistractors returns 3 distractors, none equal to correct answer'
   - Use 'substitution_simple' skill, seed 1, construct problem object with metadata { digitCount: 2, requiresCarry: false, requiresBorrow: false, ...domainData.metadata }, distractorStrategy: 'domain_specific' as const
   - generateDistractors(problem, createRng(1), 3, 'domain_specific')
   - expects distractors.length toBe(3), each d.value not.toBe(correctValue)

Add comment at top: `// Wave 0 RED stubs — all tests FAIL until Wave 1 lands`
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test -- --testPathPattern=systemsEquations 2>&1 | tail -20</automated>
  </verify>
  <done>File exists with all describe blocks. Tests fail at import/runtime level (SYSTEMS_EQUATIONS_BUGS not found, or 'systems_equations' not a valid MathDomain), NOT with syntax errors. Zero passing tests expected.</done>
</task>

<task type="auto">
  <name>Task 2: Update count assertions in 3 existing test files</name>
  <files>
    src/__tests__/mathEngine/domainHandlerRegistry.test.ts,
    src/__tests__/adaptive/prerequisiteGating.test.ts,
    src/__tests__/mathEngine/wordProblems.test.ts
  </files>
  <action>
Make these minimal, targeted edits to each file:

**domainHandlerRegistry.test.ts** (4 changes):
1. Add `'systems_equations'` after `'statistics_hs'` in the ALL_OPERATIONS array (line ~29)
2. Change `'has a handler registered for all 22 operations'` string → `'has a handler registered for all 23 operations'`
3. Change `'all skills are covered across the 22 domains'` string → `'all skills are covered across the 23 domains'`
4. Change `expect(total).toBe(175)` → `expect(total).toBe(180)`
5. Add `systems_equations: ['numeric'],` after the `statistics_hs: ['numeric'],` line in expectedTypes

**prerequisiteGating.test.ts** (1 change):
1. Change `expect(SKILLS.length).toBe(175)` → `expect(SKILLS.length).toBe(180)`

**wordProblems.test.ts** (2 changes):
1. Add `'systems_equations',` after `'statistics_hs',` in the ALL_OPERATIONS array
2. In the minWordProblemCount map (near `statistics_hs: 9`), add `systems_equations: 9,` after it

Do NOT change any logic, imports, or other test cases — these are count-only updates.
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test -- --testPathPattern="domainHandlerRegistry|prerequisiteGating|wordProblems" 2>&1 | tail -30</automated>
  </verify>
  <done>domainHandlerRegistry.test.ts fails with "Expected: 23, Received: 22" (not a syntax error). prerequisiteGating.test.ts fails with "Expected: 180, Received: 175". wordProblems.test.ts fails because 'systems_equations' is not yet a valid MathDomain. Failures confirm the stubs are correctly wired to the missing implementation.</done>
</task>

</tasks>

<verification>
After both tasks:
- npm test -- --testPathPattern=systemsEquations — runs, fails with import/registry errors
- npm test -- --testPathPattern="domainHandlerRegistry|prerequisiteGating|wordProblems" — count assertions fail with numeric mismatch
- No syntax errors (tsc should parse all files even though types will error on 'systems_equations')
- No previously-passing tests broken
</verification>

<success_criteria>
4 files exist with correct content. All new/updated tests FAIL (RED). No regressions in unrelated tests. The failure messages confirm the stubs are wired to the correct missing symbols: SYSTEMS_EQUATIONS_BUGS, 'systems_equations' MathDomain, handler count 22→23, skill count 175→180.
</success_criteria>

<output>
After completion, create `.planning/phases/086-systems-of-equations-domain/086-01-SUMMARY.md`
</output>
