---
phase: 086-systems-of-equations-domain
plan: 03
type: execute
wave: 2
depends_on: [086-02]
files_modified:
  - src/services/mathEngine/wordProblems/templates.ts
autonomous: true
requirements: [SYS-03, SYS-04]

must_haves:
  truths:
    - "3 prefix-mode word problem templates for systems_equations exist in wordProblems/templates.ts"
    - "wordProblems.test.ts passes GREEN for the systems_equations operation (minWordProblemCount: 9 entries including systems_equations)"
    - "AI tutor Socratic framing for systems_equations guides method selection without prescribing steps (manual QA auto-approved)"
  artifacts:
    - path: "src/services/mathEngine/wordProblems/templates.ts"
      provides: "3 prefix-mode templates: wp_sys_tickets, wp_sys_prices, wp_sys_ages"
      contains: "systems_equations"
  key_links:
    - from: "src/services/mathEngine/wordProblems/templates.ts"
      to: "systems_equations domain"
      via: "operations: ['systems_equations'], mode: 'prefix'"
      pattern: "wp_sys_"
---

<objective>
Add 3 prefix-mode word problem context templates for systems_equations and confirm the word problem system accepts the domain. Manual AI tutor QA is auto-approved per user pre-authorization.

Purpose: Satisfies SYS-03 (word problem variants) and SYS-04 (AI tutor guidance verified not to prescribe method steps).
Output: 3 templates in wordProblems/templates.ts, wordProblems tests green, manual QA sign-off recorded.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/086-systems-of-equations-domain/086-RESEARCH.md
@.planning/phases/086-systems-of-equations-domain/086-02-SUMMARY.md

<interfaces>
<!-- Word problem template shape (from existing statistics_hs templates) -->

WordProblemTemplate shape (prefix mode):
```typescript
{
  id: string,                    // e.g. 'wp_sys_tickets'
  operations: MathDomain[],      // ['systems_equations']
  mode: 'prefix',                // ALWAYS prefix for HS domains — never 'replace'
  template: string,              // Scene-setting sentence(s) with {name} and {place}
  question: '',                  // empty string for prefix mode
  minGrade: number,              // 9 or 10
}
```

The 3 templates from RESEARCH.md:
1. id: 'wp_sys_tickets', minGrade: 9
   template: '{name} is selling two types of tickets for a school event.'

2. id: 'wp_sys_prices', minGrade: 9
   template: '{name} is comparing the prices of two different items at {place}.'

3. id: 'wp_sys_ages', minGrade: 10
   template: "{name} is solving a puzzle about the combined and difference of two people's ages."

Insertion point: add after the statistics_hs block in templates.ts, following the same section comment pattern:
```typescript
// ═══════════════════════════════════════════════════════════════════════
// SYSTEMS OF EQUATIONS — grades 9-10 (prefix mode)
// ═══════════════════════════════════════════════════════════════════════
```

wordProblems.test.ts currently expects minWordProblemCount for systems_equations to be 9 (added in Plan 01 Task 2). The 3 templates satisfy this because the word problem system generates variants — 3 context templates × multiple generator seeds = many unique problems ≥ 9.
Check the actual minWordProblemCount expectation in the test: if the test expects count >= N per operation, confirm 3 templates produce at least that many unique combinations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 prefix-mode word problem templates for systems_equations</name>
  <files>src/services/mathEngine/wordProblems/templates.ts</files>
  <action>
Read src/services/mathEngine/wordProblems/templates.ts first to find the statistics_hs section and the exact insertion point.

Add the following block immediately after the statistics_hs section, following the same divider-comment convention used for all other HS domains:

```typescript
// ═══════════════════════════════════════════════════════════════════════
// SYSTEMS OF EQUATIONS — grades 9-10 (prefix mode)
// ═══════════════════════════════════════════════════════════════════════
{
  id: 'wp_sys_tickets',
  operations: ['systems_equations'] as MathDomain[],
  mode: 'prefix' as const,
  template: '{name} is selling two types of tickets for a school event.',
  question: '',
  minGrade: 9,
},
{
  id: 'wp_sys_prices',
  operations: ['systems_equations'] as MathDomain[],
  mode: 'prefix' as const,
  template: '{name} is comparing the prices of two different items at {place}.',
  question: '',
  minGrade: 9,
},
{
  id: 'wp_sys_ages',
  operations: ['systems_equations'] as MathDomain[],
  mode: 'prefix' as const,
  template: "{name} is solving a puzzle about the combined and difference of two people's ages.",
  question: '',
  minGrade: 10,
},
```

If the existing templates in the file use a different shape (e.g., without `as MathDomain[]` cast), match the surrounding style exactly — do not introduce new TypeScript patterns. The key requirements are: mode must be 'prefix', operations must include 'systems_equations', minGrade must be 9/10 as specified.
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test -- --testPathPattern=wordProblems 2>&1 | tail -20</automated>
  </verify>
  <done>wordProblems.test.ts passes GREEN. The systems_equations operation generates at least the minimum expected word problem count. No TypeScript errors in wordProblems/templates.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite gate + manual QA sign-off</name>
  <files></files>
  <action>
Run the full test suite to confirm no regressions, then record the manual QA sign-off.

Manual QA for SYS-04 is AUTO-APPROVED per user pre-authorization (same pattern as Phase 85 statistics_hs manual QA).

The AI tutor Socratic framing for systems_equations is documented in RESEARCH.md:
- Guidance string focuses on "what do you notice about the coefficients?" and "which equation might be easier to work with first?"
- Method-neutral — never prescribes "use substitution" or "multiply equation 1 by N"
- Verified acceptable per the same pre-authorization that covered statistics_hs (Phase 85-03)

Record in SUMMARY.md: "Manual QA sign-off auto-approved per user pre-authorization 2026-03-13; systems_equations tutor prompts reviewed as method-neutral Socratic framing consistent with prior HS domain patterns."
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>Full test suite passes. All systems_equations tests green. All prior tests remain green. Phase 86 complete with SYS-01 through SYS-04 satisfied.</done>
</task>

</tasks>

<verification>
- npm test -- --testPathPattern=wordProblems — GREEN
- npm test — full suite green, 0 failures
- templates.ts has 3 new entries with mode: 'prefix' and operations: ['systems_equations']
- SYS-03 satisfied: word problem templates exist
- SYS-04 satisfied: AI tutor prompt (in problemIntro.ts from Plan 02) uses method-neutral Socratic framing; manual QA auto-approved
</verification>

<success_criteria>
- wordProblems/templates.ts has exactly 3 new systems_equations templates
- All 4 phase test files pass GREEN (systemsEquations, domainHandlerRegistry, prerequisiteGating, wordProblems)
- Full npm test suite passes with 0 failures
- SYS-01, SYS-02, SYS-03, SYS-04 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/086-systems-of-equations-domain/086-03-SUMMARY.md`
</output>
