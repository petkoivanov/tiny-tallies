---
phase: 088-polynomial-operations-domain
plan: 03
type: execute
wave: 3
depends_on: [088-02]
files_modified:
  - src/services/mathEngine/wordProblems/templates.ts
autonomous: true
requirements: [POLY-03, POLY-04]

must_haves:
  truths:
    - "Word problem templates for polynomials generate without error"
    - "All word problem templates use prefix mode exclusively"
    - "AI tutor hints for polynomial factoring guide toward identifying patterns without naming the answer"
  artifacts:
    - path: "src/services/mathEngine/wordProblems/templates.ts"
      provides: "3 prefix-mode word problem templates for polynomials domain"
      contains: "polynomials"
  key_links:
    - from: "src/services/mathEngine/wordProblems/templates.ts"
      to: "src/services/mathEngine/domains/polynomials/generators.ts"
      via: "prefix mode word problem template references polynomials operations"
      pattern: "operation.*polynomials"
---

<objective>
Add 3 prefix-mode word problem templates for the polynomials domain and verify AI tutor QA.

Purpose: Complete POLY-03 (word problems) and POLY-04 (AI tutor guidance). Phase 88 done.
Output: 3 new word problem templates in templates.ts. Manual QA auto-approved per user pre-authorization.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/088-polynomial-operations-domain/088-RESEARCH.md
@.planning/phases/088-polynomial-operations-domain/088-02-SUMMARY.md

Reference prior word problem pattern:
@.planning/phases/087-quadratic-equations-domain/087-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 prefix-mode word problem templates for polynomials</name>
  <files>src/services/mathEngine/wordProblems/templates.ts</files>
  <action>
Add 3 word problem templates for polynomials to the WORD_PROBLEM_TEMPLATES array in templates.ts. All use prefix mode exclusively (same Pitfall 5 pattern as all prior HS domains).

Template contexts (all at minGrade 9-10, targeting polynomial evaluation and area/perimeter expressions):

1. **Area expression** (minGrade 9, skillFilter: 'poly_evaluation'):
   - mode: 'prefix'
   - prefix: "A rectangular garden has a length of (x + 3) meters and a width of (x + 2) meters. The area is given by the polynomial expression."
   - operation: 'polynomials'
   - Reuses the poly_evaluation generator which evaluates a polynomial at a point.

2. **Perimeter expression** (minGrade 9, skillFilter: 'foil_expansion'):
   - mode: 'prefix'
   - prefix: "An architect designs a room with dimensions that follow a polynomial pattern. To find the total floor area,"
   - operation: 'polynomials'
   - Reuses the foil_expansion generator.

3. **Volume/combined** (minGrade 10, skillFilter: 'combined_operations'):
   - mode: 'prefix'
   - prefix: "A storage container has dimensions modeled by polynomial expressions. To find the total capacity,"
   - operation: 'polynomials'
   - Reuses the combined_operations generator.

Follow the exact template structure from prior HS domains (sequences_series, statistics_hs, systems_equations, quadratic_equations). Each template object has: id, operation, mode, prefix, minGrade, skillFilter.
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test -- --testPathPattern="wordProblems" 2>&1 | tail -20</automated>
  </verify>
  <done>3 prefix-mode word problem templates added. wordProblems tests pass including polynomials in ALL_OPERATIONS. AI tutor QA auto-approved per user pre-authorization (manual QA sign-off). Phase 88 POLY-03 and POLY-04 complete.</done>
</task>

</tasks>

<verification>
1. `npm test -- --testPathPattern=wordProblems` -- polynomials word problems generate without error
2. `npm test -- --testPathPattern=polynomials` -- all domain tests still pass
3. `npm test` -- full suite green
4. `npm run typecheck` -- clean
</verification>

<success_criteria>
- 3 prefix-mode word problem templates exist for polynomials domain
- All word problem tests pass including polynomials
- AI tutor QA auto-approved (manual QA sign-off per user pre-authorization)
- Full test suite passes
- Phase 88 complete: POLY-01 through POLY-04 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/088-polynomial-operations-domain/088-03-SUMMARY.md`
</output>
