---
phase: 088-polynomial-operations-domain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/mathEngine/polynomials.test.ts
  - src/__tests__/mathEngine/domainHandlerRegistry.test.ts
  - src/__tests__/adaptive/prerequisiteGating.test.ts
  - src/__tests__/mathEngine/wordProblems.test.ts
autonomous: true
requirements: [POLY-01, POLY-02]

must_haves:
  truths:
    - "polynomials.test.ts has RED test stubs that fail at import/module level"
    - "domainHandlerRegistry.test.ts expects 25 operations and 192 skills"
    - "prerequisiteGating.test.ts expects 192 skills"
    - "wordProblems.test.ts includes polynomials in ALL_OPERATIONS"
  artifacts:
    - path: "src/__tests__/mathEngine/polynomials.test.ts"
      provides: "RED test stubs for polynomials domain (6 skills, 3 bugs, mixed answer types)"
      min_lines: 80
  key_links:
    - from: "src/__tests__/mathEngine/polynomials.test.ts"
      to: "src/services/mathEngine/domains/polynomials"
      via: "import of handler, skills, templates, bugs"
      pattern: "import.*polynomials"
---

<objective>
Create RED test stubs for the polynomials domain. Update count assertions in existing test files.

Purpose: Establish failing test contracts that Plan 02 must satisfy -- Wave 0 RED stubs for TDD.
Output: 1 new test file + 3 updated test files, all failing until Plan 02 lands.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/088-polynomial-operations-domain/088-RESEARCH.md

Reference prior Wave 0 pattern:
@.planning/phases/087-quadratic-equations-domain/087-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RED test stubs for polynomials domain</name>
  <files>src/__tests__/mathEngine/polynomials.test.ts</files>
  <action>
Create a new test file following the Wave 0 RED stub pattern from prior HS domains (model on quadraticEquations.test.ts / systemsEquations.test.ts).

- Import from `@/services/mathEngine/domains/polynomials` (handler)
- Import from `@/services/mathEngine/skills/polynomials` (POLYNOMIALS_SKILLS)
- Import from `@/services/mathEngine/templates/polynomials` (POLYNOMIALS_TEMPLATES)
- Import from `@/services/mathEngine/bugLibrary/polynomialsBugs` (POLYNOMIALS_BUGS)
- Import `createRng` from `@/services/mathEngine/seededRng`
- Tests to stub:
  1. "should be registered in domain registry" (handler exists)
  2. "should have 6 skills" (POLYNOMIALS_SKILLS.length === 6)
  3. "should have 6 templates" (POLYNOMIALS_TEMPLATES.length === 6)
  4. "should have 3 bug patterns" (POLYNOMIALS_BUGS.length === 3)
  5. "all skills use operation polynomials" (every skill.operation === 'polynomials')
  6. "all templates use operation polynomials" (every template.operation === 'polynomials')
  7. "all templates use domain_specific distractor strategy" (every template.distractorStrategy === 'domain_specific')
  8-13. One test per generator type: foil_expansion, poly_evaluation, gcf_factoring, diff_of_squares, combined_operations, word_problem -- each creates a template with the right domainConfig.type, calls handler.generate(template, rng), and checks:
    - For foil_expansion, poly_evaluation, combined_operations, word_problem: result.correctAnswer.type === 'numeric'
    - For gcf_factoring, diff_of_squares: result.correctAnswer.type === 'numeric' (numeric proxy for expression MC)
    - result.questionText is non-empty string
    - result.operands.length >= 3
  14. "foil_expansion evaluates correctly" -- generate with known seed, verify (x+a)(x+b) evaluated at x gives correct answer
  15. "gcf_factoring answer is the GCF value" -- verify correctAnswer.value equals the GCF
  16-18. One test per bug pattern (poly_foil_forgot_middle, poly_wrong_gcf, poly_sign_error): compute() returns a non-null number
  19. "all generators produce integer answers" -- run 20 seeds, all correctAnswer.value are integers (Number.isInteger)
  20. "poly_evaluation computes ax^2+bx+c correctly" -- verify result matches polynomial evaluation formula

These imports target files that don't exist yet, so all tests will fail at import/module resolution level (RED).
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test -- --testPathPattern="polynomials" 2>&1 | head -30</automated>
  </verify>
  <done>Test file exists and fails at import level (RED). polynomials.test.ts has ~20 test cases covering registry, skills, templates, bugs, generator output types, integer answers.</done>
</task>

<task type="auto">
  <name>Task 2: Update count assertions in 3 existing test files</name>
  <files>src/__tests__/mathEngine/domainHandlerRegistry.test.ts, src/__tests__/adaptive/prerequisiteGating.test.ts, src/__tests__/mathEngine/wordProblems.test.ts</files>
  <action>
Update count assertions to reflect the addition of polynomials (6 skills, 1 new operation).

**domainHandlerRegistry.test.ts:**
- Change operation count assertion from 24 to 25 (in the test name and assertion)
- Change total skill count assertion from 186 to 192
- Add `'polynomials'` to the ALL_OPERATIONS array
- Add `polynomials: ['numeric']` to the expectedTypes map (polynomials uses numericAnswer for all skills, including factoring which uses numeric proxy)

**prerequisiteGating.test.ts:**
- Change SKILLS.length assertion from 186 to 192

**wordProblems.test.ts:**
- Add `'polynomials'` to the ALL_OPERATIONS array
- Add `polynomials: 9` to the gradeMap (Common Core HSA-APR/HSA-SSE standards start in grade 9)

These tests will now fail because the production code still has 24 operations and 186 skills. They will pass after Plan 02 lands.
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test -- --testPathPattern="domainHandlerRegistry|prerequisiteGating|wordProblems" 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 files updated with new counts. Tests fail (RED) because polynomials handler doesn't exist yet. domainHandlerRegistry expects 25 ops/192 skills, prerequisiteGating expects 192, wordProblems includes polynomials.</done>
</task>

</tasks>

<verification>
All 4 test files should exist and produce RED failures:
- polynomials.test.ts: fails at import (module not found)
- domainHandlerRegistry.test.ts: fails on count assertions (24 != 25, 186 != 192)
- prerequisiteGating.test.ts: fails on count assertion (186 != 192)
- wordProblems.test.ts: fails on missing polynomials in registry
</verification>

<success_criteria>
- 1 new test file created with RED stubs
- 3 existing test files updated with new counts
- All tests fail (RED) as expected
- Test contracts match the 6 skills, 6 templates, 3 bugs, numeric answer type documented in research
</success_criteria>

<output>
After completion, create `.planning/phases/088-polynomial-operations-domain/088-01-SUMMARY.md`
</output>
