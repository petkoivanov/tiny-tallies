---
phase: 15-foundation-store-schema-services-and-mappings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/cpa/cpaTypes.ts
  - src/services/cpa/cpaMappingService.ts
  - src/services/cpa/skillManipulativeMap.ts
  - src/services/cpa/index.ts
  - src/__tests__/cpa/cpaMappingService.test.ts
  - src/__tests__/cpa/skillManipulativeMap.test.ts
  - babel.config.js
autonomous: true
requirements: [FOUND-02, FOUND-04, CPA-01]

must_haves:
  truths:
    - "deriveCpaStage returns 'concrete' for P(L) < 0.40, 'pictorial' for 0.40-0.85, 'abstract' for >= 0.85"
    - "advanceCpaStage never regresses CPA level (one-way advance only)"
    - "Every skill ID in the SKILLS array has a mapping to at least one manipulative type"
    - "Mapping follows locked pattern: single-digit -> counters/ten_frame, within-20 -> ten_frame/number_line, two-digit -> base_ten_blocks/number_line, three-digit -> base_ten_blocks"
    - "Babel config uses react-native-worklets/plugin instead of react-native-reanimated/plugin"
  artifacts:
    - path: "src/services/cpa/cpaTypes.ts"
      provides: "CpaStage type, ManipulativeType type, SkillManipulativeMapping interface, CPA threshold constants"
      exports: ["CpaStage", "ManipulativeType", "SkillManipulativeMapping", "CPA_CONCRETE_THRESHOLD", "CPA_ABSTRACT_THRESHOLD"]
    - path: "src/services/cpa/cpaMappingService.ts"
      provides: "Pure functions for CPA stage derivation and one-way advance"
      exports: ["deriveCpaStage", "advanceCpaStage"]
    - path: "src/services/cpa/skillManipulativeMap.ts"
      provides: "Static mapping of all 14 skills to ranked manipulative preferences"
      exports: ["SKILL_MANIPULATIVE_MAP", "getManipulativesForSkill", "getPrimaryManipulative"]
    - path: "src/services/cpa/index.ts"
      provides: "Barrel export for CPA module"
    - path: "babel.config.js"
      provides: "Correct Reanimated 4 worklet compilation"
      contains: "react-native-worklets/plugin"
  key_links:
    - from: "src/services/cpa/cpaMappingService.ts"
      to: "src/services/cpa/cpaTypes.ts"
      via: "imports CpaStage type and threshold constants"
      pattern: "import.*from.*cpaTypes"
    - from: "src/services/cpa/skillManipulativeMap.ts"
      to: "src/services/cpa/cpaTypes.ts"
      via: "imports ManipulativeType and SkillManipulativeMapping"
      pattern: "import.*from.*cpaTypes"
    - from: "src/services/cpa/skillManipulativeMap.ts"
      to: "src/services/mathEngine/skills.ts"
      via: "covers every skill ID in SKILLS array"
      pattern: "SKILL_MANIPULATIVE_MAP"
---

<objective>
Create the CPA service module (types, stage derivation, one-way advance), the skill-to-manipulative mapping table, and fix the babel config for Reanimated 4 worklet compilation.

Purpose: Establishes the domain types and pure-function services that Plan 02 depends on for store migration and session integration. Also resolves the babel plugin blocker so Reanimated 4 worklets compile correctly in future phases.

Output: `src/services/cpa/` module with types, CPA service, and mapping table; updated `babel.config.js`; tests for all service functions.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-foundation-store-schema-services-and-mappings/15-CONTEXT.md
@.planning/phases/15-foundation-store-schema-services-and-mappings/15-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/services/adaptive/leitnerTypes.ts (pattern to follow for domain types):
```typescript
export type LeitnerBox = 1 | 2 | 3 | 4 | 5 | 6;
```

From src/services/adaptive/bktCalculator.ts (threshold constants pattern):
```typescript
export const BKT_MASTERY_THRESHOLD = 0.95;
export const BKT_RETEACH_THRESHOLD = 0.40;
```

From src/services/mathEngine/skills.ts (all 14 skill IDs the mapping must cover):
```typescript
// Addition skills
'addition.single-digit.no-carry'
'addition.within-20.no-carry'
'addition.within-20.with-carry'
'addition.two-digit.no-carry'
'addition.two-digit.with-carry'
'addition.three-digit.no-carry'
'addition.three-digit.with-carry'
// Subtraction skills
'subtraction.single-digit.no-borrow'
'subtraction.within-20.no-borrow'
'subtraction.within-20.with-borrow'
'subtraction.two-digit.no-borrow'
'subtraction.two-digit.with-borrow'
'subtraction.three-digit.no-borrow'
'subtraction.three-digit.with-borrow'
```

From babel.config.js (current state to fix):
```javascript
module.exports = function(api) {
  api.cache(true);
  return {
    presets: ['babel-preset-expo'],
    plugins: ['react-native-reanimated/plugin'],  // Must change to worklets
  };
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CPA types and service with tests</name>
  <files>src/services/cpa/cpaTypes.ts, src/services/cpa/cpaMappingService.ts, src/services/cpa/index.ts, src/__tests__/cpa/cpaMappingService.test.ts</files>
  <behavior>
    - deriveCpaStage(0.0) returns 'concrete'
    - deriveCpaStage(0.39) returns 'concrete'
    - deriveCpaStage(0.40) returns 'pictorial'
    - deriveCpaStage(0.84) returns 'pictorial'
    - deriveCpaStage(0.85) returns 'abstract'
    - deriveCpaStage(1.0) returns 'abstract'
    - advanceCpaStage('concrete', 0.50) returns 'pictorial' (advance)
    - advanceCpaStage('pictorial', 0.30) returns 'pictorial' (no regression even though P(L) < 0.40)
    - advanceCpaStage('abstract', 0.10) returns 'abstract' (no regression)
    - advanceCpaStage('concrete', 0.90) returns 'abstract' (skip pictorial)
    - advanceCpaStage('pictorial', 0.90) returns 'abstract' (advance)
    - advanceCpaStage('concrete', 0.10) returns 'concrete' (no change)
  </behavior>
  <action>
    1. Create `src/services/cpa/cpaTypes.ts`:
       - `CpaStage` type: `'concrete' | 'pictorial' | 'abstract'`
       - `ManipulativeType` type: `'base_ten_blocks' | 'number_line' | 'fraction_strips' | 'counters' | 'ten_frame' | 'bar_model'`
       - `SkillManipulativeMapping` interface: `{ skillId: string; manipulatives: ManipulativeType[] }`
       - Constants: `CPA_CONCRETE_THRESHOLD = 0.40`, `CPA_ABSTRACT_THRESHOLD = 0.85`
       - Note: CPA_CONCRETE_THRESHOLD aligns with BKT_RETEACH_THRESHOLD (0.40) per user decision. Research said 0.40/0.85; earlier ARCHITECTURE.md had 0.60/0.85 -- the 0.40/0.85 thresholds are locked.

    2. Create `src/services/cpa/cpaMappingService.ts`:
       - `deriveCpaStage(masteryProbability: number): CpaStage` -- pure function using threshold constants. P(L) < 0.40 = concrete, 0.40 <= P(L) < 0.85 = pictorial, >= 0.85 = abstract.
       - `advanceCpaStage(currentStage: CpaStage, newMasteryProbability: number): CpaStage` -- one-way advance only. Uses a numeric stage-order map (`{ concrete: 0, pictorial: 1, abstract: 2 }`). Returns `max(currentStage, derivedStage)` by order. This is per user decision: once a child reaches pictorial, they NEVER drop back to concrete.
       - Follow the pure-function pattern of bktCalculator.ts (stateless, no store dependency).

    3. Create `src/services/cpa/index.ts`:
       - Barrel re-export from cpaTypes.ts and cpaMappingService.ts (and later skillManipulativeMap.ts from Task 2).

    4. Create `src/__tests__/cpa/cpaMappingService.test.ts`:
       - Test all boundary conditions listed in behavior section.
       - Test one-way advance in all regression scenarios (pictorial->concrete blocked, abstract->anything blocked).
       - Follow existing test patterns (describe blocks, clear test names).
  </action>
  <verify>
    <automated>cd C:/projects/tiny-tallies && npm test -- --testPathPattern=cpa/cpaMappingService</automated>
  </verify>
  <done>CPA types exported, deriveCpaStage handles all threshold boundaries correctly, advanceCpaStage never regresses, all tests pass, TypeScript clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create skill-manipulative mapping table with tests and fix babel config</name>
  <files>src/services/cpa/skillManipulativeMap.ts, src/__tests__/cpa/skillManipulativeMap.test.ts, babel.config.js, src/services/cpa/index.ts</files>
  <behavior>
    - SKILL_MANIPULATIVE_MAP has exactly 14 entries (one per skill in SKILLS array)
    - Every skill ID in SKILLS has a corresponding mapping entry
    - getManipulativesForSkill('addition.single-digit.no-carry') returns ['counters', 'ten_frame', 'bar_model']
    - getManipulativesForSkill('addition.within-20.no-carry') returns ['ten_frame', 'number_line', 'bar_model']
    - getManipulativesForSkill('addition.two-digit.no-carry') returns ['base_ten_blocks', 'number_line', 'bar_model']
    - getManipulativesForSkill('addition.three-digit.no-carry') returns ['base_ten_blocks', 'bar_model']
    - Subtraction mirrors addition: getManipulativesForSkill('subtraction.single-digit.no-borrow') returns ['counters', 'ten_frame', 'bar_model']
    - getPrimaryManipulative('addition.single-digit.no-carry') returns 'counters'
    - getPrimaryManipulative('nonexistent.skill') returns null
    - getManipulativesForSkill('nonexistent.skill') returns []
  </behavior>
  <action>
    1. Create `src/services/cpa/skillManipulativeMap.ts`:
       - Import `ManipulativeType`, `SkillManipulativeMapping` from `./cpaTypes`.
       - Create `SKILL_MANIPULATIVE_MAP: readonly SkillManipulativeMapping[]` with all 14 skills mapped per user decisions:
         - single-digit/within-10: `['counters', 'ten_frame', 'bar_model']`
         - within-20: `['ten_frame', 'number_line', 'bar_model']`
         - two-digit: `['base_ten_blocks', 'number_line', 'bar_model']`
         - three-digit: `['base_ten_blocks', 'bar_model']`
         - Subtraction mirrors addition mappings (per user decision).
         - Bar model included as last entry for every skill (per RESEARCH.md recommendation -- costs nothing and prepares for AI tutor word-problem wrapping).
       - `getManipulativesForSkill(skillId: string): ManipulativeType[]` -- find in map, return array or empty.
       - `getPrimaryManipulative(skillId: string): ManipulativeType | null` -- return first element or null.
       - Note: Fraction strips are NOT mapped to any current skill (sandbox-only per user decision). They exist in ManipulativeType for future use.

    2. Update `src/services/cpa/index.ts` barrel to also re-export from `./skillManipulativeMap`.

    3. Create `src/__tests__/cpa/skillManipulativeMap.test.ts`:
       - Import SKILLS from `@/services/mathEngine/skills` and verify every skill ID has a mapping (coverage test).
       - Verify map has exactly 14 entries.
       - Verify each mapping category follows the correct pattern.
       - Test getManipulativesForSkill for representative skills.
       - Test getPrimaryManipulative for valid and invalid skill IDs.
       - Test that subtraction mirrors addition.

    4. Fix `babel.config.js`:
       - Change `'react-native-reanimated/plugin'` to `'react-native-worklets/plugin'`.
       - Plugin MUST be the last entry in the plugins array (it already is the only entry).
       - No other changes to babel.config.js.
  </action>
  <verify>
    <automated>cd C:/projects/tiny-tallies && npm test -- --testPathPattern=cpa/skillManipulativeMap && npm run typecheck</automated>
  </verify>
  <done>All 14 skills mapped to ranked manipulative lists following Common Core pedagogy and user decisions, bar_model as last entry for all skills, fraction_strips not mapped (sandbox-only), getPrimaryManipulative returns first preference, babel.config.js uses react-native-worklets/plugin, all tests pass, TypeScript clean.</done>
</task>

</tasks>

<verification>
1. `npm test -- --testPathPattern=cpa` -- all CPA service and mapping tests pass
2. `npm run typecheck` -- no TypeScript errors
3. CPA thresholds are exactly 0.40/0.85 (not 0.60/0.85)
4. advanceCpaStage never returns a stage lower than currentStage
5. Every skill ID in SKILLS has a mapping in SKILL_MANIPULATIVE_MAP
6. babel.config.js contains 'react-native-worklets/plugin' and does NOT contain 'react-native-reanimated/plugin'
</verification>

<success_criteria>
- CPA types, service, and mapping table fully implemented and tested
- deriveCpaStage correct at all threshold boundaries
- advanceCpaStage enforces one-way advance
- All 14 skills mapped with correct manipulative preferences
- Babel config fixed for Reanimated 4
- Zero TypeScript errors
- All new and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-foundation-store-schema-services-and-mappings/15-01-SUMMARY.md`
</output>
