---
phase: 15-foundation-store-schema-services-and-mappings
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/store/slices/skillStatesSlice.ts
  - src/store/helpers/skillStateHelpers.ts
  - src/store/migrations.ts
  - src/store/appStore.ts
  - src/services/session/sessionOrchestrator.ts
  - src/services/session/sessionTypes.ts
  - src/hooks/useSession.ts
  - src/__tests__/migrations.test.ts
  - src/__tests__/appStore.test.ts
  - src/__tests__/session/sessionOrchestrator.test.ts
autonomous: true
requirements: [FOUND-01, CPA-01]

must_haves:
  truths:
    - "STORE_VERSION is 5 with a v4->v5 migration that adds cpaLevel to every existing SkillState"
    - "Migration uses BKT-informed initial placement: P(L) < 0.40 = concrete, 0.40-0.85 = pictorial, >= 0.85 = abstract"
    - "New skills created via getOrCreateSkillState or updateSkillState default to cpaLevel: 'concrete'"
    - "commitSessionResults advances CPA level per skill atomically with Elo/BKT/Leitner updates"
    - "CPA advance is one-way: never regresses from higher to lower stage"
  artifacts:
    - path: "src/store/slices/skillStatesSlice.ts"
      provides: "SkillState type with cpaLevel field"
      contains: "cpaLevel: CpaStage"
    - path: "src/store/migrations.ts"
      provides: "v4->v5 migration block with BKT-informed CPA placement"
      contains: "if (version < 5)"
    - path: "src/store/appStore.ts"
      provides: "STORE_VERSION bumped to 5"
      contains: "STORE_VERSION = 5"
    - path: "src/store/helpers/skillStateHelpers.ts"
      provides: "getOrCreateSkillState default includes cpaLevel: 'concrete'"
      contains: "cpaLevel"
    - path: "src/services/session/sessionOrchestrator.ts"
      provides: "commitSessionResults writes cpaLevel from pending update to store"
      contains: "cpaLevel: update.newCpaLevel"
    - path: "src/services/session/sessionTypes.ts"
      provides: "PendingSkillUpdate includes newCpaLevel field"
      contains: "newCpaLevel"
    - path: "src/hooks/useSession.ts"
      provides: "CPA advance computation using advanceCpaStage when building PendingSkillUpdate"
      contains: "advanceCpaStage"
  key_links:
    - from: "src/store/migrations.ts"
      to: "src/services/cpa/cpaMappingService.ts"
      via: "imports deriveCpaStage for BKT-informed initial placement"
      pattern: "import.*deriveCpaStage.*from.*cpa"
    - from: "src/hooks/useSession.ts"
      to: "src/services/cpa/cpaMappingService.ts"
      via: "imports advanceCpaStage for one-way CPA advance when building pending updates"
      pattern: "import.*advanceCpaStage.*from.*cpa"
    - from: "src/store/slices/skillStatesSlice.ts"
      to: "src/services/cpa/cpaTypes.ts"
      via: "imports CpaStage type for SkillState.cpaLevel field"
      pattern: "import.*CpaStage.*from.*cpa"
---

<objective>
Integrate CPA tracking into the store schema (v4->v5 migration) and session commit flow, so every skill tracks its CPA stage and advances atomically with Elo/BKT/Leitner at session end.

Purpose: Completes the FOUND-01 and CPA-01 requirements by wiring the CPA service (created in Plan 01) into the store and session orchestrator. After this plan, the system tracks CPA stage per skill and advances it based on BKT mastery.

Output: Updated store schema (v5), migration with BKT-informed CPA placement, commitSessionResults with CPA advance, updated tests.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-foundation-store-schema-services-and-mappings/15-CONTEXT.md
@.planning/phases/15-foundation-store-schema-services-and-mappings/15-RESEARCH.md
@.planning/phases/15-foundation-store-schema-services-and-mappings/15-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan consumes. -->

From src/services/cpa/cpaTypes.ts (created in Plan 01):
```typescript
export type CpaStage = 'concrete' | 'pictorial' | 'abstract';
export type ManipulativeType = 'base_ten_blocks' | 'number_line' | 'fraction_strips' | 'counters' | 'ten_frame' | 'bar_model';
export const CPA_CONCRETE_THRESHOLD = 0.40;
export const CPA_ABSTRACT_THRESHOLD = 0.85;
```

From src/services/cpa/cpaMappingService.ts (created in Plan 01):
```typescript
export function deriveCpaStage(masteryProbability: number): CpaStage;
export function advanceCpaStage(currentStage: CpaStage, newMasteryProbability: number): CpaStage;
```

From src/store/slices/skillStatesSlice.ts (current state to modify):
```typescript
export type SkillState = {
  eloRating: number;
  attempts: number;
  correct: number;
  lastPracticed?: string;
  masteryProbability: number;
  consecutiveWrong: number;
  masteryLocked: boolean;
  leitnerBox: 1 | 2 | 3 | 4 | 5 | 6;
  nextReviewDue: string | null;
  consecutiveCorrectInBox6: number;
  // ADD: cpaLevel: CpaStage
};
```

From src/store/helpers/skillStateHelpers.ts (current defaults to update):
```typescript
export function getOrCreateSkillState(
  skillStates: Record<string, SkillState>,
  skillId: string,
  defaultElo: number = DEFAULT_ELO,
): SkillState {
  return skillStates[skillId] ?? {
    // ... existing defaults ...
    // ADD: cpaLevel: 'concrete'
  };
}
```

From src/store/migrations.ts (pattern to extend):
```typescript
// After if (version < 4) block:
// ADD: if (version < 5) { ... BKT-informed CPA placement ... }
```

From src/services/session/sessionOrchestrator.ts (commitSessionResults to extend):
```typescript
export function commitSessionResults(
  pendingUpdates: Map<string, PendingSkillUpdate>,
  // ... existing params ...
): SessionFeedback {
  for (const [skillId, update] of pendingUpdates) {
    updateSkillState(skillId, {
      // ... existing fields ...
      // ADD: cpaLevel: update.newCpaLevel
    });
  }
  // ...
}
```

From src/services/session/sessionTypes.ts (PendingSkillUpdate to extend):
```typescript
export interface PendingSkillUpdate {
  // ... existing fields ...
  // ADD: newCpaLevel: CpaStage (from cpaTypes.ts)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store schema v4->v5 migration with CPA level per skill</name>
  <files>src/store/slices/skillStatesSlice.ts, src/store/helpers/skillStateHelpers.ts, src/store/migrations.ts, src/store/appStore.ts, src/__tests__/migrations.test.ts, src/__tests__/appStore.test.ts</files>
  <behavior>
    - SkillState type includes cpaLevel: CpaStage field
    - getOrCreateSkillState default includes cpaLevel: 'concrete'
    - updateSkillState fallback default includes cpaLevel: 'concrete'
    - STORE_VERSION is 5
    - migrateStore(state, 4) adds cpaLevel to every skill using BKT-informed placement
    - migrateStore from v4: skill with P(L)=0.15 gets cpaLevel='concrete'
    - migrateStore from v4: skill with P(L)=0.55 gets cpaLevel='pictorial'
    - migrateStore from v4: skill with P(L)=0.92 gets cpaLevel='abstract'
    - migrateStore from v4: skill with missing masteryProbability defaults to 0.1 -> 'concrete'
    - migrateStore from v4: empty skillStates remains empty
    - migrateStore chains v1->v5 correctly (full chain test)
    - Existing v4 test "migrateStore from version 4 returns state unchanged" must be updated -- v4 state now gets cpaLevel added
    - STORE_VERSION test updates from 4 to 5
  </behavior>
  <action>
    1. Update `src/store/slices/skillStatesSlice.ts`:
       - Import `CpaStage` from `../../services/cpa/cpaTypes`. This follows the pattern where LeitnerBox is defined in services (leitnerTypes.ts) and imported by the store. Services own domain types.
       - Add `cpaLevel: CpaStage` to the `SkillState` type with JSDoc: `/** CPA progression stage derived from BKT mastery. One-way advance only. */`
       - Add `cpaLevel: 'concrete' as const` to the fallback default object in `updateSkillState` (line 39-49 area).

    2. Update `src/store/helpers/skillStateHelpers.ts`:
       - Add `cpaLevel: 'concrete' as const` to the default SkillState returned by `getOrCreateSkillState` (line 17-28 area).
       - TypeScript strict mode will catch if this is missed.

    3. Update `src/store/migrations.ts`:
       - Import `deriveCpaStage` from `../../services/cpa/cpaMappingService`.
       - Add `if (version < 5)` block after the existing `if (version < 4)` block (replacing the `// Future migrations chain here` comment).
       - Inside: iterate `Object.keys(skillStates)`, read `masteryProbability ?? 0.1`, set `skill.cpaLevel ??= deriveCpaStage(masteryProbability)`.
       - This mirrors the v3->v4 Leitner migration pattern exactly.

    4. Update `src/store/appStore.ts`:
       - Change `STORE_VERSION = 4` to `STORE_VERSION = 5`.
       - No partialize changes needed -- cpaLevel lives inside skillStates which is already partialized.

    5. Update `src/__tests__/migrations.test.ts`:
       - Add test: `v4->v5 migration adds cpaLevel with BKT-informed placement` with low/mid/high P(L) skills.
       - Add test: `v4->v5 migration handles empty skillStates cleanly`.
       - Add test: `v4->v5 migration with missing masteryProbability defaults to concrete`.
       - Update existing test `migrateStore from version 4 returns state unchanged` -- it now needs to add cpaLevel to input OR change assertion to expect cpaLevel added.
       - Update chain test `migrateStore chains v1->v4 correctly` to become v1->v5 chain test, expecting cpaLevel on the output skill.

    6. Update `src/__tests__/appStore.test.ts`:
       - Change `STORE_VERSION equals 4` test to `STORE_VERSION equals 5`.
       - Update `updateSkillState with non-existent skillId creates entry with DEFAULT_ELO` to expect `cpaLevel: 'concrete'` in the output.
       - Update `updateSkillState with existing skillId preserves non-updated fields` to include `cpaLevel: 'concrete'` in expected output.
       - Update `getOrCreateSkillState returns default when skillId not found` to expect `cpaLevel: 'concrete'`.
       - Update `getOrCreateSkillState returns existing state when skillId exists` to include `cpaLevel: 'concrete'` in the existing state.
       - Update `getOrCreateSkillState accepts optional defaultElo parameter` to expect `cpaLevel: 'concrete'`.
  </action>
  <verify>
    <automated>cd C:/projects/tiny-tallies && npm test -- --testPathPattern="(migrations|appStore)" && npm run typecheck</automated>
  </verify>
  <done>STORE_VERSION is 5, v4->v5 migration adds BKT-informed cpaLevel to all existing skills, all default SkillState objects include cpaLevel: 'concrete', all migration and store tests pass, TypeScript clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire CPA advance into commitSessionResults</name>
  <files>src/services/session/sessionTypes.ts, src/services/session/sessionOrchestrator.ts, src/hooks/useSession.ts, src/__tests__/session/sessionOrchestrator.test.ts</files>
  <behavior>
    - PendingSkillUpdate includes newCpaLevel field of type CpaStage
    - commitSessionResults writes cpaLevel from update.newCpaLevel to the store via updateSkillState
    - When a skill's P(L) crosses from < 0.40 to >= 0.40, its CPA advances from concrete to pictorial (if currently concrete)
    - When a skill's P(L) crosses from < 0.85 to >= 0.85, its CPA advances from pictorial to abstract (if currently pictorial or concrete)
    - CPA never regresses: a skill at 'abstract' stays 'abstract' even if P(L) drops
    - Existing commitSessionResults tests still pass (XP, level-up, streak logic unchanged)
  </behavior>
  <action>
    1. Update `src/services/session/sessionTypes.ts`:
       - Import `CpaStage` from `../../services/cpa/cpaTypes`.
       - Add `newCpaLevel: CpaStage` field to the `PendingSkillUpdate` interface (after `newConsecutiveCorrectInBox6`).
       - Add JSDoc: `/** CPA stage after one-way advance logic */`

    2. Update `src/services/session/sessionOrchestrator.ts`:
       - In `commitSessionResults`, add `cpaLevel: update.newCpaLevel` to the `updateSkillState` call inside the for-loop (alongside existing fields like eloRating, attempts, etc.).
       - That is ALL that changes in the orchestrator. The CPA advance computation (calling `advanceCpaStage`) happens in the `useSession` hook where `pendingUpdates` are built -- but that integration is Phase 18 scope. For now, the orchestrator just writes whatever `newCpaLevel` is in the update.
       - Note: The existing `useSession.ts` hook builds `PendingSkillUpdate` objects. Adding `newCpaLevel` to the type will cause a TypeScript error in `useSession.ts` because it won't provide the field. To keep TypeScript clean, set the field as required in the interface but also update `useSession.ts` to compute and include `newCpaLevel` using `advanceCpaStage(currentSkillState.cpaLevel, newMasteryPL)`. This keeps the system consistent now rather than deferring a broken type.

    3. Update `src/hooks/useSession.ts`:
       - Import `advanceCpaStage` from `../services/cpa`.
       - In the location where `PendingSkillUpdate` objects are created/accumulated, compute `newCpaLevel` using `advanceCpaStage(currentCpaLevel, newMasteryPL)` where `currentCpaLevel` comes from the current skill state's `cpaLevel` (defaulting to `'concrete'` if not present).
       - This ensures the full pipeline works: answer -> accumulate update with CPA advance -> commit at session end.

    4. Update `src/__tests__/session/sessionOrchestrator.test.ts`:
       - Update existing `commitSessionResults` test fixtures to include `newCpaLevel` in PendingSkillUpdate objects. For existing tests, just add `newCpaLevel: 'concrete'` to maintain current behavior.
       - Add a new test: `commitSessionResults writes cpaLevel from pending update` -- create a pending update with `newCpaLevel: 'pictorial'`, verify `updateSkillState` is called with `cpaLevel: 'pictorial'`.
       - Add a new test: `commitSessionResults handles mixed CPA levels across skills` -- two skills, one concrete and one abstract, verify each gets its correct cpaLevel.

    5. Run full test suite to ensure nothing is broken:
       - `npm test` should pass all 557+ tests plus new ones.
  </action>
  <verify>
    <automated>cd C:/projects/tiny-tallies && npm test -- --testPathPattern="session" && npm run typecheck</automated>
  </verify>
  <done>PendingSkillUpdate includes newCpaLevel, commitSessionResults writes cpaLevel to store, useSession computes CPA advance using advanceCpaStage, all session tests pass, full test suite passes, TypeScript clean.</done>
</task>

</tasks>

<verification>
1. `npm test` -- ALL tests pass (existing 557+ plus new migration, store, session, and CPA tests)
2. `npm run typecheck` -- zero TypeScript errors
3. STORE_VERSION is 5 in appStore.ts
4. Migration v4->v5 exists in migrations.ts with BKT-informed CPA placement
5. Every SkillState default location includes `cpaLevel: 'concrete'` (skillStatesSlice, skillStateHelpers)
6. commitSessionResults includes `cpaLevel: update.newCpaLevel` in its updateSkillState call
7. PendingSkillUpdate interface includes `newCpaLevel: CpaStage`
8. useSession hook computes CPA advance via advanceCpaStage
</verification>

<success_criteria>
- Store schema v5 with cpaLevel per skill
- v4->v5 migration correctly places existing skills using BKT mastery thresholds
- All three default-value locations updated atomically (slice, helpers, migration)
- commitSessionResults writes CPA level to store
- useSession computes one-way CPA advance
- Full test suite passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-foundation-store-schema-services-and-mappings/15-02-SUMMARY.md`
</output>
