---
phase: 087-quadratic-equations-domain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/mathEngine/quadraticEquations.test.ts
  - src/__tests__/mathEngine/multiSelect.test.ts
  - src/__tests__/mathEngine/domainHandlerRegistry.test.ts
  - src/__tests__/adaptive/prerequisiteGating.test.ts
  - src/__tests__/mathEngine/wordProblems.test.ts
autonomous: true
requirements: [QUAD-01, QUAD-02, QUAD-03]

must_haves:
  truths:
    - "quadraticEquations.test.ts has RED test stubs that fail at import/module level"
    - "multiSelect.test.ts has RED test stubs for formatAsMultiSelect"
    - "domainHandlerRegistry.test.ts expects 24 operations and 186 skills"
    - "prerequisiteGating.test.ts expects 186 skills"
    - "wordProblems.test.ts includes quadratic_equations in ALL_OPERATIONS"
  artifacts:
    - path: "src/__tests__/mathEngine/quadraticEquations.test.ts"
      provides: "RED test stubs for quadratic_equations domain (6 skills, 3 bugs)"
      min_lines: 80
    - path: "src/__tests__/mathEngine/multiSelect.test.ts"
      provides: "RED test stubs for formatAsMultiSelect function"
      min_lines: 30
  key_links:
    - from: "src/__tests__/mathEngine/quadraticEquations.test.ts"
      to: "src/services/mathEngine/domains/quadraticEquations"
      via: "import of handler, skills, templates, bugs"
      pattern: "import.*quadraticEquations"
    - from: "src/__tests__/mathEngine/multiSelect.test.ts"
      to: "src/services/mathEngine/answerFormats/multiSelect"
      via: "import of formatAsMultiSelect"
      pattern: "import.*formatAsMultiSelect"
---

<objective>
Create RED test stubs for the quadratic_equations domain and the formatAsMultiSelect answer pipeline function. Update count assertions in existing test files.

Purpose: Establish failing test contracts that Plan 02 must satisfy -- Wave 0 RED stubs for TDD.
Output: 2 new test files + 3 updated test files, all failing until Plan 02 lands.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/087-quadratic-equations-domain/087-RESEARCH.md

Reference prior Wave 0 pattern:
@.planning/phases/086-systems-of-equations-domain/086-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RED test stubs for quadraticEquations domain and multiSelect formatter</name>
  <files>src/__tests__/mathEngine/quadraticEquations.test.ts, src/__tests__/mathEngine/multiSelect.test.ts</files>
  <action>
Create two new test files following the Wave 0 RED stub pattern from prior HS domains.

**quadraticEquations.test.ts** (model on systemsEquations.test.ts):
- Import from `@/services/mathEngine/domains/quadraticEquations` (handler)
- Import from `@/services/mathEngine/skills/quadraticEquations` (QUADRATIC_EQUATIONS_SKILLS)
- Import from `@/services/mathEngine/templates/quadraticEquations` (QUADRATIC_EQUATIONS_TEMPLATES)
- Import from `@/services/mathEngine/bugLibrary/quadraticEquationsBugs` (QUADRATIC_EQUATIONS_BUGS)
- Import `createRng` from `@/services/mathEngine/seededRng`
- Tests to stub:
  1. "should be registered in domain registry" (handler exists)
  2. "should have 6 skills" (QUADRATIC_EQUATIONS_SKILLS.length === 6)
  3. "should have 6 templates" (QUADRATIC_EQUATIONS_TEMPLATES.length === 6)
  4. "should have 3 bug patterns" (QUADRATIC_EQUATIONS_BUGS.length === 3)
  5. "all skills use operation quadratic_equations" (every skill.operation === 'quadratic_equations')
  6. "all templates use operation quadratic_equations" (every template.operation === 'quadratic_equations')
  7. "all templates use domain_specific distractor strategy" (every template.distractorStrategy === 'domain_specific')
  8-13. One test per generator type: factoring_monic, factoring_leading_coeff, quadratic_formula_simple, quadratic_formula_rational, completing_the_square, word_problem -- each creates a template with the right domainConfig.type, calls handler.generate(template, rng), and checks:
    - result.correctAnswer.type === 'multi_select'
    - result.correctAnswer.values.length === 2
    - result.correctAnswer.values[0] !== result.correctAnswer.values[1] (distinct roots)
    - result.questionText is non-empty string
    - result.operands.length === 4 (wrongSignR1, wrongSignR2, sum, product)
  14. "distractor values should include wrong-sign roots" -- generate a problem, verify operands[0] === -correctAnswer.values[0] and operands[1] === -correctAnswer.values[1]
  15-17. One test per bug pattern (quad_wrong_sign, quad_sum_product_confusion, quad_only_one_root): compute() returns a non-null number
  18. "all generators produce integer roots" -- run 20 seeds, all values are integers (Number.isInteger)
  19. "factoring_leading_coeff has leading coefficient > 1" -- questionText starts with "2x" or "3x" (not "x")
  20. "word_problem generator produces question with context sentence" -- questionText is longer than standard equation format

**multiSelect.test.ts**:
- Import `formatAsMultiSelect` from `@/services/mathEngine/answerFormats/multiSelect`
- Import `multiSelectAnswer` from `@/services/mathEngine/types`
- Import `Problem` type from `@/services/mathEngine/types`
- Tests to stub:
  1. "should produce MultiSelectPresentation with format multi_select" -- create a mock Problem with multiSelectAnswer([3, -5]), operands [-3, 5, -2, -15], call formatAsMultiSelect, check result.format === 'multi_select'
  2. "should include all correct values in options" -- result.options should contain values 3 and -5
  3. "should include distractor values from operands" -- result.options should contain at least one of -3 or 5
  4. "should produce exactly 4 options" -- 2 correct + 2 distractors
  5. "correctIndices should point to correct values" -- result.correctIndices.map(i => result.options[i].value) should contain both correct values
  6. "should throw if answer is not multi_select type" -- passing a numericAnswer Problem should throw

These imports target files that don't exist yet, so all tests will fail at import/module resolution level (RED).
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test -- --testPathPattern="quadraticEquations|multiSelect" 2>&1 | head -30</automated>
  </verify>
  <done>Both test files exist and fail at import level (RED). quadraticEquations.test.ts has ~20 test cases. multiSelect.test.ts has ~6 test cases.</done>
</task>

<task type="auto">
  <name>Task 2: Update count assertions in 3 existing test files</name>
  <files>src/__tests__/mathEngine/domainHandlerRegistry.test.ts, src/__tests__/adaptive/prerequisiteGating.test.ts, src/__tests__/mathEngine/wordProblems.test.ts</files>
  <action>
Update count assertions to reflect the addition of quadratic_equations (6 skills, 1 new operation).

**domainHandlerRegistry.test.ts:**
- Change operation count assertion from 23 to 24
- Change total skill count assertion from 180 to 186
- Add `'quadratic_equations'` to the ALL_OPERATIONS array
- Add `quadratic_equations: ['multi_select']` to the `expectedTypes` map (NOTE: this is different from all prior domains which use `['numeric']` -- quadratics use multi_select because both roots must be selected)

**prerequisiteGating.test.ts:**
- Change SKILLS.length assertion from 180 to 186

**wordProblems.test.ts:**
- Add `'quadratic_equations'` to the ALL_OPERATIONS array
- Add `quadratic_equations: 9` to the gradeMap (Common Core HSA-REI grade 9)

These tests will now fail because the production code still has 23 operations and 180 skills. They will pass after Plan 02 lands.
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test -- --testPathPattern="domainHandlerRegistry|prerequisiteGating|wordProblems" 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 files updated with new counts. Tests fail (RED) because quadratic_equations handler doesn't exist yet. domainHandlerRegistry expects 24 ops/186 skills, prerequisiteGating expects 186, wordProblems includes quadratic_equations.</done>
</task>

</tasks>

<verification>
All 5 test files should exist and produce RED failures:
- quadraticEquations.test.ts: fails at import (module not found)
- multiSelect.test.ts: fails at import (module not found)
- domainHandlerRegistry.test.ts: fails on count assertions (23 != 24, 180 != 186)
- prerequisiteGating.test.ts: fails on count assertion (180 != 186)
- wordProblems.test.ts: fails on missing quadratic_equations in registry
</verification>

<success_criteria>
- 2 new test files created with RED stubs
- 3 existing test files updated with new counts
- All tests fail (RED) as expected
- Test contracts match the 6 skills, 6 templates, 3 bugs, multi_select answer type documented in research
</success_criteria>

<output>
After completion, create `.planning/phases/087-quadratic-equations-domain/087-01-SUMMARY.md`
</output>
