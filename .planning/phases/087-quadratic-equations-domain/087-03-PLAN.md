---
phase: 087-quadratic-equations-domain
plan: 03
type: execute
wave: 3
depends_on: [087-02]
files_modified:
  - src/services/mathEngine/wordProblems/templates.ts
autonomous: false
requirements: [QUAD-04, QUAD-05]

must_haves:
  truths:
    - "3 prefix-mode word problem templates exist for quadratic_equations"
    - "Word problems generate without error for quadratic_equations domain"
    - "AI tutor hints for quadratics use Socratic questioning without revealing roots"
  artifacts:
    - path: "src/services/mathEngine/wordProblems/templates.ts"
      provides: "3 prefix-mode word problem templates for quadratic_equations"
      contains: "wp_quad_area"
  key_links:
    - from: "src/services/mathEngine/wordProblems/templates.ts"
      to: "src/services/mathEngine/domains/quadraticEquations"
      via: "operations: ['quadratic_equations'] on templates"
      pattern: "quadratic_equations"
---

<objective>
Add word problem prefix templates for quadratic_equations and verify AI tutor Socratic framing.

Purpose: Complete QUAD-04 (word problem variants) and QUAD-05 (AI tutor QA).
Output: 3 word problem templates added, AI tutor QA checkpoint completed.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/087-quadratic-equations-domain/087-RESEARCH.md
@.planning/phases/087-quadratic-equations-domain/087-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 quadratic_equations word problem prefix templates</name>
  <files>src/services/mathEngine/wordProblems/templates.ts</files>
  <action>
Add 3 prefix-mode word problem templates to the WORD_PROBLEM_TEMPLATES array in `src/services/mathEngine/wordProblems/templates.ts`. Use prefix mode exclusively (same as all prior HS domains -- avoids {a}/{b} operand mismatch per Pitfall 5).

Templates to add (per Research):

```typescript
// QUADRATIC EQUATIONS -- grades 9-10 (prefix mode)
{
  id: 'wp_quad_area',
  operations: ['quadratic_equations'],
  mode: 'prefix',
  template: '{name} is designing a rectangular garden where one side is {a} meters longer than the other.',
  question: '',
  minGrade: 9,
},
{
  id: 'wp_quad_projectile',
  operations: ['quadratic_equations'],
  mode: 'prefix',
  template: '{name} launches a ball into the air from the roof of a building.',
  question: '',
  minGrade: 10,
},
{
  id: 'wp_quad_number',
  operations: ['quadratic_equations'],
  mode: 'prefix',
  template: '{name} is thinking of two numbers whose product and sum have a special relationship.',
  question: '',
  minGrade: 9,
},
```

Place these after the existing systems_equations templates, maintaining alphabetical/phase order of the HS domain blocks.

Run word problems test to verify quadratic_equations templates are recognized.
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test -- --testPathPattern=wordProblems 2>&1 | tail -15</automated>
  </verify>
  <done>3 prefix-mode word problem templates added for quadratic_equations. wordProblems test passes with quadratic_equations in ALL_OPERATIONS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Quadratic equations domain with multi-select answer format, 6 skills, 3 bug patterns, 3 word problem templates, and AI tutor Socratic framing for two-root problems</what-built>
  <how-to-verify>
    1. Run `npx expo start` and start a practice session with a grade 9-10 profile
    2. Navigate to quadratic equations problems
    3. Verify: Problems display as x^2 + bx + c = 0 with "Find both roots" instruction
    4. Verify: Answer options render as checkboxes (MultiSelectMC), not radio buttons
    5. Verify: Selecting both correct roots and pressing "Check" scores as correct
    6. Verify: Selecting only one root scores as incorrect (all-or-nothing)
    7. Get a problem wrong deliberately, then trigger AI tutor HINT mode
    8. Verify: HINT mode uses Socratic questioning about factoring approach WITHOUT revealing either root value
    9. Exhaust the hint ladder to reach BOOST mode
    10. Verify: BOOST mode mentions BOTH roots (e.g., "The roots are 3 and -5")
    11. Check that word problems show context sentences before the equation

    Manual QA can be auto-approved per prior phase pre-authorization pattern (Phases 84-86).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm test -- --testPathPattern=wordProblems` -- quadratic_equations templates recognized
2. `npm test` -- full suite GREEN
3. `npm run typecheck` -- no type errors
4. Manual QA confirms MultiSelectMC rendering, all-or-nothing grading, Socratic hints, BOOST showing both roots
</verification>

<success_criteria>
- 3 prefix-mode word problem templates exist for quadratic_equations (area, projectile, number contexts)
- Word problems generate without error
- AI tutor QA confirms Socratic framing in HINT mode (no root values revealed)
- AI tutor QA confirms BOOST mode shows both roots
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/087-quadratic-equations-domain/087-03-SUMMARY.md`
</output>
