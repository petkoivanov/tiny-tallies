---
phase: 39-profile-management-ui
plan: "02"
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - src/components/profile/ProfileCreationWizard.tsx
  - src/components/profile/index.ts
  - src/screens/ProfileSetupScreen.tsx
  - src/navigation/AppNavigator.tsx
  - src/navigation/types.ts
  - src/__tests__/components/profile/ProfileCreationWizard.test.tsx
  - src/__tests__/screens/ProfileSetupScreen.test.tsx
autonomous: true
requirements: [PROF-02]

must_haves:
  truths:
    - "Parent can create a new child profile with name, age, grade, and avatar"
    - "Profile creation is PIN-gated (PinGate wraps the wizard)"
    - "First-time fresh install shows ProfileSetup with PIN creation flow"
    - "After creating first profile, app navigates to Home with no back button to setup"
    - "Wizard validates name (non-empty, max 20 chars) before advancing"
    - "Max 5 profiles enforced (store already guards, UI should communicate)"
  artifacts:
    - path: "src/components/profile/ProfileCreationWizard.tsx"
      provides: "Multi-step profile creation form"
      exports: ["ProfileCreationWizard"]
    - path: "src/screens/ProfileSetupScreen.tsx"
      provides: "First-run and post-delete profile setup screen"
      exports: ["default"]
    - path: "src/navigation/AppNavigator.tsx"
      provides: "ProfileSetup route with real screen, ProfileManagement route"
      contains: "ProfileSetupScreen"
  key_links:
    - from: "src/screens/ProfileSetupScreen.tsx"
      to: "src/components/profile/PinGate.tsx"
      via: "PinGate wraps creation wizard"
      pattern: "PinGate"
    - from: "src/screens/ProfileSetupScreen.tsx"
      to: "profilesSlice.addChild"
      via: "wizard onComplete calls addChild"
      pattern: "addChild"
    - from: "src/navigation/AppNavigator.tsx"
      to: "src/screens/ProfileSetupScreen.tsx"
      via: "Stack.Screen component replacement"
      pattern: "ProfileSetupScreen"
---

<objective>
Build the profile creation wizard and ProfileSetupScreen, replacing the Phase 38 placeholder in AppNavigator, so parents can add new child profiles behind the PIN gate.

Purpose: PROF-02 requires PIN-gated profile creation with name, age, grade, and avatar selection. This screen handles both fresh install (first profile) and "add another child" flows.
Output: Working ProfileCreationWizard, ProfileSetupScreen replacing placeholder, navigation wiring.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-profile-management-ui/39-RESEARCH.md
@.planning/phases/39-profile-management-ui/39-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/components/profile/PinGate.tsx (created in Plan 01):
```typescript
interface PinGateProps {
  children: React.ReactNode;
  onCancel: () => void;
  title?: string;
  subtitle?: string;
}
// Renders children after PIN verification. Handles both create and verify flows.
```

From src/store/slices/profilesSlice.ts:
```typescript
addChild: (profile: NewChildProfile) => string | null;
// Returns childId on success, null if at max (5) capacity
```

From src/store/helpers/childDataHelpers.ts:
```typescript
export type NewChildProfile = {
  childName: string;
  childAge: number;
  childGrade: number;
  avatarId: AllAvatarId | null;
};
```

From src/store/constants/avatars.ts:
```typescript
export const AVATARS: Avatar[];  // 14 regular avatars with { id, emoji, label }
export type AllAvatarId = ...;
```

From src/navigation/types.ts:
```typescript
ProfileSetup: undefined;
// Already in RootStackParamList from Phase 38
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProfileCreationWizard component</name>
  <files>
    src/components/profile/ProfileCreationWizard.tsx,
    src/components/profile/index.ts,
    src/__tests__/components/profile/ProfileCreationWizard.test.tsx
  </files>
  <behavior>
    - Step 'name': renders text input, Next button disabled when empty or whitespace-only
    - Step 'name': Next button disabled when name > 20 chars, shows character count
    - Step 'age-grade': renders age picker (5-12) and grade picker (K, 1-6), both required
    - Step 'age-grade': grade defaults based on age (age - 5, clamped to 0-6)
    - Step 'avatar': renders grid of 14 avatars from AVATARS constant
    - Step 'avatar': selecting an avatar and pressing Done calls onComplete with full profile
    - Step 'avatar': if no avatar selected, uses null (default will apply)
    - Back button returns to previous step, disabled on first step
    - Cancel button calls onCancel (if provided)
    - initialValues pre-fills fields (for edit mode reuse)
  </behavior>
  <action>
    1. Create `src/components/profile/ProfileCreationWizard.tsx`:
       - Props: `{ onComplete: (profile: NewChildProfile) => void, onCancel?: () => void, initialValues?: Partial<NewChildProfile> }`
       - Local state: `step: 'name' | 'age-grade' | 'avatar'`, `name: string`, `age: number | null`, `grade: number | null`, `avatarId: AllAvatarId | null`
       - Initialize state from `initialValues` if provided

       Step 1 (Name):
       - Large friendly prompt: "What's your learner's name?"
       - TextInput with maxLength={20}, autoFocus
       - Character counter (e.g., "5/20")
       - "Next" button enabled when name.trim().length > 0
       - If onCancel provided, show "Cancel" in top-left

       Step 2 (Age & Grade):
       - "How old is {name}?" with horizontal age selector (5-12, pressable circles)
       - "What grade?" with horizontal grade selector (K, 1, 2, 3, 4, 5, 6 as pressable chips)
       - Auto-select grade when age changes: grade = Math.max(0, Math.min(6, age - 5))
       - "Back" and "Next" buttons. Next enabled when both age and grade selected
       - Grade K = 0 internally

       Step 3 (Avatar):
       - "Choose an avatar for {name}!" with grid of 14 AVATARS (not special/earned ones)
       - Use AvatarCircle component for each avatar with selection highlight
       - "Back" and "Done" buttons. Done calls onComplete({ childName: name.trim(), childAge: age, childGrade: grade, avatarId })
       - If no avatar tapped, avatarId is null (system assigns default)

       Use StyleSheet.create for all styles. Use `lucide-react-native` for Back/Next arrows (ChevronLeft, ChevronRight).

    2. Update `src/components/profile/index.ts` barrel to export ProfileCreationWizard.

    3. Write ProfileCreationWizard.test.tsx:
       - Mock `lucide-react-native`, `react-native-safe-area-context`
       - Mock `@/components/avatars` AvatarCircle as a simple Pressable with testID
       - Mock `@/store/constants/avatars` AVATARS with 3 test avatars
       - Test step navigation (name -> age-grade -> avatar -> done)
       - Test name validation (empty disabled, whitespace disabled, valid enabled)
       - Test age-grade auto-linking
       - Test onComplete called with correct profile shape
       - Test onCancel called when cancel pressed
       - Test initialValues pre-fill
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=ProfileCreationWizard</automated>
  </verify>
  <done>
    - 3-step wizard works end-to-end with proper validation
    - All step transitions work (forward and back)
    - onComplete receives correctly shaped NewChildProfile
    - Tests cover happy path and edge cases
  </done>
</task>

<task type="auto">
  <name>Task 2: ProfileSetupScreen and navigation wiring</name>
  <files>
    src/screens/ProfileSetupScreen.tsx,
    src/navigation/AppNavigator.tsx,
    src/navigation/types.ts,
    src/__tests__/screens/ProfileSetupScreen.test.tsx
  </files>
  <action>
    1. Create `src/screens/ProfileSetupScreen.tsx`:
       - Wraps ProfileCreationWizard inside PinGate
       - PinGate props: title="Parent Setup", subtitle="Create a PIN to manage profiles"
       - On first-ever use (no PIN): PinGate shows create flow, then wizard
       - On subsequent use (PIN exists): PinGate shows verify flow, then wizard
       - No onCancel for PinGate when it's the only screen (fresh install): pass `onCancel={() => {}}` (no-op, can't cancel first profile). If navigated to from management (when children exist), onCancel should `navigation.goBack()`.
       - Determine which mode by checking `Object.keys(children).length` from store
       - Wizard onComplete: call `addChild(profile)`, then `navigation.reset({ index: 0, routes: [{ name: 'Home' }] })` per Pitfall 2
       - Handle `_needsMigrationPrompt` flag: if true, show a variant with "Welcome! Let's set up your learner's profile" messaging, call `setMigrationComplete()` after wizard completes

    2. Update `src/navigation/AppNavigator.tsx`:
       - Remove `ProfileSetupPlaceholder` function and its styles
       - Import `ProfileSetupScreen` from `@/screens/ProfileSetupScreen`
       - Replace `component={ProfileSetupPlaceholder}` with `component={ProfileSetupScreen}`
       - Add `ProfileManagement` route: `<Stack.Screen name="ProfileManagement" component={ProfileManagementScreen} />` (import from `@/screens/ProfileManagementScreen`). Since ProfileManagementScreen doesn't exist yet (Plan 03), create a minimal placeholder inline similar to the old ProfileSetupPlaceholder pattern. Plan 03 will replace it.

    3. Update `src/navigation/types.ts`:
       - Add `ProfileManagement: undefined` to RootStackParamList

    4. Write ProfileSetupScreen.test.tsx:
       - Mock PinGate to immediately render children (skip PIN in test)
       - Mock ProfileCreationWizard to render a button that calls onComplete with test profile
       - Mock store with Approach A: test with 0 children (fresh install) and >0 children (add more)
       - Mock navigation.reset
       - Test that addChild is called with profile from wizard
       - Test that navigation.reset is called with Home route after creation
       - Test migration prompt variant when _needsMigrationPrompt is true
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="ProfileSetupScreen|AppNavigator" && npm run typecheck</automated>
  </verify>
  <done>
    - ProfileSetupScreen replaces placeholder in AppNavigator
    - Fresh install shows PinGate -> wizard -> Home (no back button)
    - ProfileManagement route exists in navigation types
    - Migration prompt handled for existing v0.7 users
    - All tests pass, typecheck clean
  </done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern="ProfileCreationWizard|ProfileSetupScreen|AppNavigator"` -- all pass
- `npm run typecheck` -- clean
- Fresh install flow: ProfileSetup -> PIN create -> wizard -> Home
- Add child flow: ProfileSetup -> PIN verify -> wizard -> Home
</verification>

<success_criteria>
- PROF-02 fully implemented: parent can add a new child with name, age, grade, avatar behind PIN gate
- ProfileSetupPlaceholder replaced with real screen
- Navigation reset prevents back-button to setup screen
- Migration prompt handled for existing users
</success_criteria>

<output>
After completion, create `.planning/phases/39-profile-management-ui/39-02-SUMMARY.md`
</output>
