---
phase: 09-session-results-ui-polish
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/screens/ResultsScreen.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/SessionScreen.tsx
  - src/__tests__/screens/ResultsScreen.test.tsx
autonomous: true
requirements: [UI-03, UI-04, UI-05]

must_haves:
  truths:
    - "Results screen shows XP progress bar with before/after fill and '+N XP' label"
    - "Results screen shows dynamic motivational message based on score percentage"
    - "Results screen shows streak count with Flame icon matching HomeScreen pattern"
    - "Results screen shows level-up callout when leveled up during the session"
    - "All interactive elements across Home, Session, and Results meet 48dp minimum touch target"
    - "All three screens use high-contrast dark theme consistently with the established palette"
  artifacts:
    - path: "src/screens/ResultsScreen.tsx"
      provides: "Polished results screen with XP bar, motivational message, streak, level-up callout"
      contains: "motivationalMessage"
    - path: "src/__tests__/screens/ResultsScreen.test.tsx"
      provides: "Updated tests covering new results content"
      contains: "leveledUp"
  key_links:
    - from: "src/screens/ResultsScreen.tsx"
      to: "src/navigation/types.ts"
      via: "Reads extended Results route params (leveledUp, newLevel, streakCount)"
      pattern: "leveledUp|newLevel|streakCount"
    - from: "src/screens/ResultsScreen.tsx"
      to: "src/services/gamification/levelProgression.ts"
      via: "Uses calculateLevelFromXp for XP progress bar math"
      pattern: "calculateLevelFromXp"
    - from: "src/screens/ResultsScreen.tsx"
      to: "src/store/appStore.ts"
      via: "Reads current xp from store for XP bar end state"
      pattern: "useAppStore"
---

<objective>
Polish the ResultsScreen into a rewarding session-complete experience and audit all three screens for touch target compliance and dark theme consistency.

Purpose: The results screen should feel like a reward moment, celebrating the child's effort. XP progress visualization, motivational messages, and streak/level-up callouts reinforce the positive learning loop. The cross-screen audit ensures accessibility (48dp touch targets) and visual consistency.

Output: Rich ResultsScreen with XP bar, motivational message, streak, and level-up callout. All screens audited for 48dp and dark theme compliance.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-session-results-ui-polish/09-CONTEXT.md
@.planning/phases/09-session-results-ui-polish/09-01-SUMMARY.md
@src/screens/ResultsScreen.tsx
@src/screens/HomeScreen.tsx
@src/screens/SessionScreen.tsx
@src/theme/index.ts
@src/navigation/types.ts
@src/services/gamification/levelProgression.ts
@src/store/slices/gamificationSlice.ts
@src/__tests__/screens/ResultsScreen.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish ResultsScreen with XP bar, motivational message, streak, and level-up callout</name>
  <files>
    src/screens/ResultsScreen.tsx
    src/__tests__/screens/ResultsScreen.test.tsx
  </files>
  <action>
**1. Expand ResultsScreen to read new route params and store state:**

Read the extended route params (after Plan 01's type changes):
```typescript
const { score, total, xpEarned, durationMs, leveledUp, newLevel, streakCount } = route.params;
```

Read current XP from the store (this is post-session XP, already committed):
```typescript
const xp = useAppStore((state) => state.xp);
```

Import `calculateLevelFromXp` from `@/services/gamification/levelProgression` and `useAppStore` from `@/store/appStore`.

Calculate XP bar values:
```typescript
const { xpIntoCurrentLevel, xpNeededForNextLevel } = calculateLevelFromXp(xp);
const previousXpInLevel = xpIntoCurrentLevel - xpEarned; // Where XP was before this session
// Clamp previousXpInLevel to 0 minimum (could be negative if leveled up)
const startFill = Math.max(0, previousXpInLevel);
```

Note: If the child leveled up, the "before" state was at or near 100% of the PREVIOUS level's bar. For simplicity in v0.2 (Phase 10 adds animation), just show the current fill state with the "+N XP" label. The XP bar shows current progress into the current level.

**2. Add dynamic motivational message:**

Create a helper function:
```typescript
function getMotivationalMessage(scorePercent: number): string {
  if (scorePercent >= 90) return 'Amazing!';
  if (scorePercent >= 70) return 'Great job!';
  return 'Nice effort!';
}
```

Calculate: `const scorePercent = total > 0 ? (score / total) * 100 : 0;`

Replace the "Session Complete!" title with the motivational message. Or better: keep "Session Complete!" as a subtitle and show the motivational message as a large, colorful primary heading above it.

Per the context decisions, always positive, never punitive.

**3. Restructure the stats card:**

The stats card should contain these rows in order (Claude's discretion on ordering — logical flow from performance to rewards):

Row 1: **Score** — `12 / 15` (existing, keep as-is)
Divider
Row 2: **XP Earned** — `+85 XP` (existing, keep as-is)
Divider
Row 3: **XP Progress bar** — Show XP progress bar reusing HomeScreen's visual pattern:
  - Container with label: `{xpIntoCurrentLevel} / {xpNeededForNextLevel} XP to Level {currentLevel + 1}`
  - Bar: full width, height 10, borderRadius round, background colors.surface (slightly lighter than card)
  - Fill: borderRadius round, width = percentage, backgroundColor colors.primary
Divider
Row 4: **Streak** — `Streak: {streakCount} week{s}` with Flame icon (lucide-react-native) and Check icon if practiced this week
  - Import `Flame` from lucide-react-native
  - Use same visual pattern as HomeScreen streak row
  - `streakCount` comes from route params
Divider (conditional)
Row 5 (conditional): **Level Up callout** — Only show when `leveledUp` is true
  - Text: `Level Up! -> Level {newLevel}` (use arrow character or right arrow)
  - Color: colors.primaryLight for emphasis
  - fontFamily: typography.fontFamily.bold, fontSize: typography.fontSize.lg

Row 6: **Time** — `2m 30s` (existing, keep as-is)

**4. Keep the Done button as-is** — it already meets 48dp (minHeight: layout.minTouchTarget) and uses the correct theme.

**5. Update styles:**

Add new styles for:
- `motivationalMessage` — large, bold, colorful (colors.correct for 90%+, colors.primaryLight for 70%+, colors.textPrimary for below)
- `xpBarContainer`, `xpBarBackground`, `xpBarFill`, `xpBarLabel` — matching HomeScreen pattern
- `streakRow` — flexDirection row, alignItems center, gap spacing.sm
- `levelUpRow` — prominently colored row
- Keep the subtitle "Session Complete!" smaller below the motivational message

**6. Update ResultsScreen tests:**

Update the mock route params to include the new fields:
```typescript
mockRouteParams = {
  sessionId: 'test-session-123',
  score: 12,
  total: 15,
  xpEarned: 150,
  durationMs: 150000,
  leveledUp: false,
  newLevel: 3,
  streakCount: 2,
};
```

Mock useAppStore to return xp value:
```typescript
jest.mock('@/store/appStore', () => ({
  useAppStore: (selector: any) => selector({ xp: 500 }),
}));
```

Add new tests:
- Renders motivational message "Great job!" for 80% score (12/15)
- Renders "Amazing!" for 100% score
- Renders "Nice effort!" for low score
- Renders streak count with Flame icon (test for streak text)
- Shows level-up callout when leveledUp is true
- Does NOT show level-up callout when leveledUp is false
- Renders XP progress bar
- Keep all existing passing tests (score, XP earned, duration formatting, Done button)

Run tests: `npm test -- --testPathPattern=ResultsScreen`
  </action>
  <verify>
    <automated>cd C:/projects/tiny-tallies && npm test -- --testPathPattern=ResultsScreen</automated>
    <manual>Verify ResultsScreen displays motivational message, XP bar, streak, and level-up callout</manual>
  </verify>
  <done>
    - ResultsScreen shows dynamic motivational message based on score percentage
    - Stats card includes XP progress bar, streak with Flame icon, conditional level-up callout
    - All new content uses theme colors and typography consistently
    - Updated tests pass covering all new content
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-screen 48dp touch target and dark theme audit</name>
  <files>
    src/screens/HomeScreen.tsx
    src/screens/SessionScreen.tsx
    src/screens/ResultsScreen.tsx
  </files>
  <action>
Audit all three screens for 48dp minimum touch target compliance (UI-04) and dark theme consistency (UI-05). Fix any violations found.

**48dp Touch Target Audit Checklist:**

Per the CONTEXT.md decisions: keep quit button (X) as-is (already meets 48dp), keep problem text at fontSize.display (48dp).

Check each interactive element has `minWidth: layout.minTouchTarget` (48) AND `minHeight: layout.minTouchTarget` (48):

**HomeScreen.tsx:**
- Start Practice button: currently minHeight: 56 — PASS (above 48dp)
- Avatar/profile elements: not interactive — N/A
- No other interactive elements — PASS

**SessionScreen.tsx:**
- Quit button (X): minWidth/minHeight: layout.minTouchTarget — PASS
- Answer option buttons: minHeight: layout.minTouchTarget + 16 = 64, minWidth: 140 — PASS
- No other interactive elements — PASS

**ResultsScreen.tsx:**
- Done button: minHeight: layout.minTouchTarget — PASS
- No other interactive elements — PASS

If any element is found below 48dp during implementation, fix it by adding `minWidth: layout.minTouchTarget` and/or `minHeight: layout.minTouchTarget`.

**Dark Theme Consistency Audit Checklist:**

Verify all three screens use ONLY colors from `src/theme/index.ts`. Check for:
- No hardcoded color strings (except the alpha-channel variants like `#84cc1620` for answer button tints from Plan 01 — these are acceptable as they derive from theme colors)
- All backgrounds use colors.background or colors.surface/surfaceLight hierarchy
- All text uses colors.textPrimary, textSecondary, or textMuted
- All accents use colors.primary/primaryLight/primaryDark, correct, or incorrect
- No white (#fff, #ffffff, white) used for backgrounds (only for textPrimary)

**Specific checks per screen:**

**HomeScreen.tsx:**
- Background: colors.background — PASS
- Surface elements: colors.surface — PASS
- Text hierarchy: textPrimary, textSecondary, primaryLight — PASS
- XP bar: colors.surface (bg) + colors.primary (fill) — PASS

**SessionScreen.tsx (after Plan 01 changes):**
- Background: colors.background — PASS
- Progress bar: colors.surface (bg) + phase-colored fill — verify uses theme colors
- Answer buttons: colors.surface (bg) — PASS
- Feedback colors: colors.correct, colors.incorrect — PASS

**ResultsScreen.tsx (after Task 1 changes):**
- Background: colors.background — verify
- Stats card: colors.surface — verify
- New XP bar: colors.surface + colors.primary — verify
- Motivational message: verify uses theme colors
- Streak/level-up: verify uses theme colors

**If violations found:** Fix them using the correct theme constants. Document any fixes made.

**If no violations found:** Confirm compliance in the summary. This task may result in zero code changes if all screens already comply — that is a valid outcome.

Run full test suite to verify no regressions: `npm test`
Run typecheck: `npm run typecheck`
  </action>
  <verify>
    <automated>cd C:/projects/tiny-tallies && npm run typecheck && npm test</automated>
    <manual>Visually confirm all three screens use consistent dark theme and no small touch targets</manual>
  </verify>
  <done>
    - All interactive elements across Home, Session, and Results meet 48dp minimum
    - All three screens use only theme colors — no hardcoded colors outside theme
    - Full test suite passes with no regressions
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
Run the full verification suite:
1. `npm run typecheck` — TypeScript compiles with no errors
2. `npm test` — Full test suite passes (no regressions)
3. Cross-reference: ResultsScreen reads leveledUp, newLevel, streakCount from route params (set by Plan 01)
4. Cross-reference: All theme colors from src/theme/index.ts, no hardcoded values
</verification>

<success_criteria>
- ResultsScreen shows motivational message ("Amazing!" / "Great job!" / "Nice effort!") based on score
- ResultsScreen has XP progress bar with current level progress
- ResultsScreen shows streak with Flame icon
- ResultsScreen shows level-up callout when applicable
- All touch targets across all 3 screens meet 48dp minimum
- Consistent dark theme with established palette across all 3 screens
- All tests pass, TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-session-results-ui-polish/09-02-SUMMARY.md`
</output>
