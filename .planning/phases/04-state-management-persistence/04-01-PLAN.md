---
phase: 04-state-management-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/constants/avatars.ts
  - src/store/helpers/skillStateHelpers.ts
  - src/store/slices/childProfileSlice.ts
  - src/store/slices/skillStatesSlice.ts
  - src/store/slices/sessionStateSlice.ts
  - src/__tests__/appStore.test.ts
autonomous: true
requirements: [STOR-01, STOR-02, STOR-03]

must_haves:
  truths:
    - "Child profile stores name, age, grade, and avatar selection with a fixed set of animal avatar options"
    - "Skill states track per-skill Elo rating, attempts, and correct counts with lazy initialization on first encounter"
    - "Session state tracks current problem index, answers given, score, XP earned, and optional timing/format metadata"
    - "isProfileComplete helper returns true only when all four profile fields are non-null"
  artifacts:
    - path: "src/store/constants/avatars.ts"
      provides: "Avatar constant set with 8 animal options (id, label, emoji)"
      exports: ["AVATARS", "AvatarId", "DEFAULT_AVATAR_ID"]
    - path: "src/store/helpers/skillStateHelpers.ts"
      provides: "Lazy skill state initialization and profile completeness check"
      exports: ["getOrCreateSkillState", "isProfileComplete", "DEFAULT_ELO"]
    - path: "src/store/slices/childProfileSlice.ts"
      provides: "Child profile slice with AvatarId-typed avatarId"
      exports: ["ChildProfileSlice", "createChildProfileSlice"]
    - path: "src/store/slices/skillStatesSlice.ts"
      provides: "Skill states slice with enriched updateSkillState using lazy init"
      exports: ["SkillState", "SkillStatesSlice", "createSkillStatesSlice"]
    - path: "src/store/slices/sessionStateSlice.ts"
      provides: "Session state slice with enriched SessionAnswer including optional timing and format"
      exports: ["SessionAnswer", "SessionStateSlice", "createSessionStateSlice"]
    - path: "src/__tests__/appStore.test.ts"
      provides: "Tests for enriched slice behaviors and helpers"
  key_links:
    - from: "src/store/slices/childProfileSlice.ts"
      to: "src/store/constants/avatars.ts"
      via: "AvatarId type import"
      pattern: "import.*AvatarId.*avatars"
    - from: "src/store/helpers/skillStateHelpers.ts"
      to: "src/store/slices/skillStatesSlice.ts"
      via: "SkillState type import"
      pattern: "import.*SkillState.*skillStatesSlice"
    - from: "src/store/helpers/skillStateHelpers.ts"
      to: "src/store/slices/childProfileSlice.ts"
      via: "ChildProfileSlice type import for isProfileComplete"
      pattern: "import.*ChildProfileSlice.*childProfileSlice"
---

<objective>
Enrich existing Zustand store slices with complete data shapes for child profile, skill tracking, and session state. Add avatar constants, lazy skill state initialization helper, and profile completeness checker.

Purpose: STOR-01, STOR-02, STOR-03 require the store slices to carry full data shapes. The Phase 1 slices have the right structure but need enrichment: avatar as a typed constant set (not bare string), skill states with lazy init from a default Elo, and session answers with optional timing/format metadata for future analytics.

Output: Enriched slice files, avatar constants module, skill state helper module, and updated tests.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-state-management-persistence/04-CONTEXT.md
@.planning/phases/04-state-management-persistence/04-RESEARCH.md

@src/store/appStore.ts
@src/store/slices/childProfileSlice.ts
@src/store/slices/skillStatesSlice.ts
@src/store/slices/sessionStateSlice.ts
@src/store/slices/gamificationSlice.ts
@src/__tests__/appStore.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/store/appStore.ts:
```typescript
export type AppState = ChildProfileSlice & SkillStatesSlice & SessionStateSlice & GamificationSlice;
export const STORE_VERSION = 1;
export const useAppStore = create<AppState>()((...a) => ({ ...slices }));
```

From src/store/slices/childProfileSlice.ts:
```typescript
export interface ChildProfileSlice {
  childName: string | null;
  childAge: number | null;
  childGrade: number | null;
  avatarId: string | null;
  setChildProfile: (profile: Partial<Pick<ChildProfileSlice, 'childName' | 'childAge' | 'childGrade' | 'avatarId'>>) => void;
}
```

From src/store/slices/skillStatesSlice.ts:
```typescript
export type SkillState = { eloRating: number; attempts: number; correct: number; };
export interface SkillStatesSlice {
  skillStates: Record<string, SkillState>;
  updateSkillState: (skillId: string, update: Partial<SkillState>) => void;
  resetSkillStates: () => void;
}
```

From src/store/slices/sessionStateSlice.ts:
```typescript
export interface SessionAnswer { problemId: string; answer: number; correct: boolean; }
export interface SessionStateSlice {
  isSessionActive: boolean;
  currentProblemIndex: number;
  sessionScore: number;
  sessionXpEarned: number;
  sessionAnswers: SessionAnswer[];
  startSession: () => void;
  endSession: () => void;
  recordAnswer: (answer: SessionAnswer) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create avatar constants and skill/profile helper modules</name>
  <files>src/store/constants/avatars.ts, src/store/helpers/skillStateHelpers.ts</files>
  <behavior>
    - avatars: AVATARS array has exactly 8 entries, each with id, label, emoji string fields
    - avatars: AvatarId type is a union of the 8 avatar id strings
    - avatars: DEFAULT_AVATAR_ID is 'fox' (first entry)
    - helpers: getOrCreateSkillState returns existing state when skillId exists in record
    - helpers: getOrCreateSkillState returns default SkillState {eloRating: 1000, attempts: 0, correct: 0} when skillId not found
    - helpers: getOrCreateSkillState accepts optional defaultElo parameter (defaults to DEFAULT_ELO = 1000)
    - helpers: isProfileComplete returns false when any of childName/childAge/childGrade/avatarId is null
    - helpers: isProfileComplete returns true when all four fields are non-null
  </behavior>
  <action>
    1. Create `src/store/constants/avatars.ts`:
       - Export `AVATARS` as a readonly array of `{ id, label, emoji }` objects with `as const`
       - 8 animal avatars: fox, owl, bear, rabbit, cat, dog, panda, koala (fun for ages 6-9)
       - Export `AvatarId` type derived from `typeof AVATARS[number]['id']`
       - Export `DEFAULT_AVATAR_ID: AvatarId = 'fox'`

    2. Create `src/store/helpers/skillStateHelpers.ts`:
       - Import `SkillState` from `../slices/skillStatesSlice`
       - Export `DEFAULT_ELO = 1000` constant (standard Elo starting value, middle of 800-1250 template range)
       - Export `getOrCreateSkillState(skillStates: Record<string, SkillState>, skillId: string, defaultElo: number = DEFAULT_ELO): SkillState`
         - Returns `skillStates[skillId]` if exists, else `{ eloRating: defaultElo, attempts: 0, correct: 0 }`
       - Export `isProfileComplete(profile: { childName: string | null; childAge: number | null; childGrade: number | null; avatarId: string | null }): boolean`
         - Returns true only when ALL four fields are non-null
         - Takes a plain object (not the full AppState) so it can be used as a derived selector

    3. Write tests in `src/__tests__/appStore.test.ts` — add a new describe block for helpers:
       - Test AVATARS has 8 entries with required fields
       - Test getOrCreateSkillState returns existing state
       - Test getOrCreateSkillState returns default when missing
       - Test isProfileComplete returns false with any null field
       - Test isProfileComplete returns true with all fields set
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=appStore</automated>
  </verify>
  <done>Avatar constants module exports AVATARS (8 entries), AvatarId type, and DEFAULT_AVATAR_ID. Skill helpers module exports getOrCreateSkillState (lazy init with DEFAULT_ELO=1000) and isProfileComplete. All new tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich slice types with avatar typing, lazy skill init, and session metadata</name>
  <files>src/store/slices/childProfileSlice.ts, src/store/slices/skillStatesSlice.ts, src/store/slices/sessionStateSlice.ts, src/__tests__/appStore.test.ts</files>
  <action>
    1. Update `src/store/slices/childProfileSlice.ts`:
       - Import `AvatarId` from `../constants/avatars`
       - Change `avatarId` type from `string | null` to `AvatarId | null`
       - Update `setChildProfile` Pick type to use the new avatarId type (the Partial<Pick<...>> pattern still works since AvatarId extends string)
       - No other changes needed — the slice shape is already correct for STOR-01

    2. Update `src/store/slices/skillStatesSlice.ts`:
       - Import `DEFAULT_ELO` from `../helpers/skillStateHelpers`
       - Enrich `updateSkillState` action to handle lazy initialization:
         When `state.skillStates[skillId]` is undefined, start from `{ eloRating: DEFAULT_ELO, attempts: 0, correct: 0 }` before spreading the update.
         This means: `const current = state.skillStates[skillId] ?? { eloRating: DEFAULT_ELO, attempts: 0, correct: 0 };`
         Then: `[skillId]: { ...current, ...update }`
       - Add an optional `lastPracticed` field to `SkillState`: `lastPracticed?: string` (ISO date string, useful for future spaced repetition)
       - Keep `resetSkillStates` unchanged

    3. Update `src/store/slices/sessionStateSlice.ts`:
       - Enrich `SessionAnswer` interface with optional metadata fields:
         - `timeMs?: number` — time in milliseconds to answer (useful for Phase 6+ analytics)
         - `format?: 'mc' | 'free'` — answer format used (multiple choice vs free text)
         - `bugId?: string` — matched misconception bugId from distractor (for Phase 3 tracking)
       - Add `sessionStartTime: number | null` to `SessionStateSlice` (Date.now() timestamp)
       - Update `startSession()` to set `sessionStartTime: Date.now()`
       - Update `endSession()` to reset `sessionStartTime: null`
       - These are OPTIONAL fields — backwards compatible, no migration needed

    4. Update existing test in `src/__tests__/appStore.test.ts`:
       - Update the "STORE_VERSION equals 1" test — keep it as-is for now (version change is Plan 04-02)
       - Add test: updateSkillState with non-existent skillId creates entry with DEFAULT_ELO
       - Add test: updateSkillState with existing skillId preserves non-updated fields
       - Add test: startSession sets sessionStartTime to a number (Date.now())
       - Add test: recordAnswer with optional metadata fields (timeMs, format, bugId) preserves them

    IMPORTANT: Do NOT change STORE_VERSION or add persist middleware — that is Plan 04-02's responsibility.
    IMPORTANT: Do NOT modify appStore.ts in this task — only slice files and their tests.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=appStore && npm run typecheck</automated>
  </verify>
  <done>childProfileSlice uses AvatarId type for avatarId. skillStatesSlice handles lazy initialization with DEFAULT_ELO for unknown skill IDs. sessionStateSlice includes optional timeMs/format/bugId on SessionAnswer and tracks sessionStartTime. All existing tests pass. TypeScript compiles with zero errors.</done>
</task>

</tasks>

<verification>
1. `npm test -- --testPathPattern=appStore` — all store tests pass including new helper and enrichment tests
2. `npm run typecheck` — TypeScript strict mode compiles with zero errors
3. Avatar constants module exports AVATARS (8 entries), AvatarId union type, DEFAULT_AVATAR_ID
4. getOrCreateSkillState handles both existing and missing skill IDs
5. isProfileComplete correctly checks all four profile fields
6. updateSkillState creates new entries with DEFAULT_ELO when skill is unknown
7. SessionAnswer supports optional timeMs, format, and bugId metadata
</verification>

<success_criteria>
- STOR-01: childProfileSlice stores name, age, grade, and typed avatar selection (AvatarId from 8 constants)
- STOR-02: skillStatesSlice tracks per-skill Elo (lazy init at 1000), attempts, correct counts
- STOR-03: sessionStateSlice tracks problem index, answers (with metadata), score, XP, start time
- All tests green, typecheck passes, no regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-state-management-persistence/04-01-SUMMARY.md`
</output>
