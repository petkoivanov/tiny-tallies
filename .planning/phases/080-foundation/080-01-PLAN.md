---
phase: 080-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/store/migrations.test.ts
  - src/__tests__/components/session/NumberPad.test.tsx
  - src/__tests__/mathEngine/answerTypes.test.ts
  - src/__tests__/components/session/MultiSelectMC.test.tsx
autonomous: true
requirements:
  - FOUND-04
  - FOUND-05
  - FOUND-06
  - FOUND-07

must_haves:
  truths:
    - "Four test files exist and all tests fail (RED) before any implementation"
    - "migrations.test.ts asserts v21->v22 migration runs without data loss"
    - "NumberPad.test.tsx asserts ± key toggles sign and maxDigits excludes the minus sign"
    - "answerTypes.test.ts asserts setsEqual and answerNumericValue handle multi_select"
    - "MultiSelectMC.test.tsx asserts checkbox rendering, Check button gate, and binary grading"
  artifacts:
    - path: "src/__tests__/store/migrations.test.ts"
      provides: "FOUND-04 test stubs"
    - path: "src/__tests__/components/session/NumberPad.test.tsx"
      provides: "FOUND-05 test stubs"
    - path: "src/__tests__/mathEngine/answerTypes.test.ts"
      provides: "FOUND-06 test stubs"
    - path: "src/__tests__/components/session/MultiSelectMC.test.tsx"
      provides: "FOUND-07 test stubs"
  key_links:
    - from: "src/__tests__/store/migrations.test.ts"
      to: "src/store/migrations.ts"
      via: "migrateStore import"
      pattern: "migrateStore"
    - from: "src/__tests__/mathEngine/answerTypes.test.ts"
      to: "src/services/mathEngine/types.ts"
      via: "setsEqual + multiSelectAnswer imports"
      pattern: "setsEqual|multiSelectAnswer"
---

<objective>
Create four failing test stubs (Wave 0) that define the verification contract for FOUND-04 through FOUND-07 before any implementation begins. These tests must FAIL initially — they drive implementation in Plans 02 and 04.

Purpose: Nyquist compliance — every implementation task has a pre-existing automated verify command. No plan shall create code without a test waiting for it.
Output: Four test files, all failing on first run.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/080-foundation/080-RESEARCH.md
@.planning/phases/080-foundation/080-VALIDATION.md
</context>

<interfaces>
<!-- Key contracts the test stubs must target. These types do not exist yet — the stubs import them and fail. -->

From src/store/migrations.ts (current):
```typescript
export function migrateStore(state: unknown, version: number): unknown;
// Fast-path: if (version >= 21) return state;
// After Plan 03: if (version >= 22) return state;
```

From src/services/mathEngine/types.ts (post-Plan-02 additions to target):
```typescript
export interface MultiSelectAnswer {
  readonly type: 'multi_select';
  readonly values: readonly number[];
}
export function multiSelectAnswer(values: number[]): MultiSelectAnswer;
export function setsEqual(a: readonly number[], b: readonly number[]): boolean;
export function answerNumericValue(answer: Answer): number; // must handle 'multi_select'
```

From src/components/session/NumberPad.tsx (post-Plan-04 additions to target):
```typescript
interface NumberPadProps {
  allowNegative?: boolean; // new
  // existing props unchanged
}
```

From src/components/session/MultiSelectMC.tsx (new component, Plan 04):
```typescript
interface MultiSelectMCProps {
  options: readonly ChoiceOption[];
  correctIndices: readonly number[];
  onAnswer: (correct: boolean) => void;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store migration + answer-types test stubs</name>
  <files>
    src/__tests__/store/migrations.test.ts
    src/__tests__/mathEngine/answerTypes.test.ts
  </files>
  <behavior>
    migrations.test.ts:
    - Test 1: migrateStore({}, 21) returns state unchanged (fast-path)
    - Test 2: migrateStore({ children: { c1: { childGrade: 8 } } }, 20) runs v21->v22 migration and returns state without error
    - Test 3: migrateStore({}, 19) runs through all migrations up to v22 without throwing

    answerTypes.test.ts:
    - Test 1: setsEqual([1, 2], [2, 1]) returns true (order-independent)
    - Test 2: setsEqual([1, 2], [1, 3]) returns false
    - Test 3: setsEqual([1], [1, 2]) returns false (length mismatch)
    - Test 4: multiSelectAnswer([1, -3]) returns { type: 'multi_select', values: [1, -3] }
    - Test 5: answerNumericValue({ type: 'multi_select', values: [3, -3] }) returns 3 (first value, Elo proxy)
  </behavior>
  <action>
    Follow tiny-tallies-testing SKILL.md patterns exactly (mock order, path aliases).

    src/__tests__/store/migrations.test.ts:
    - Use Approach B (direct store access) — import migrateStore directly from @/store/migrations
    - Do NOT import useAppStore — only the pure migrateStore function
    - Write 3 describe blocks: 'fast-path', 'v21 -> v22 migration', 'full migration chain'
    - Tests must FAIL because v22 migration block does not yet exist

    src/__tests__/mathEngine/answerTypes.test.ts:
    - Import setsEqual, multiSelectAnswer, answerNumericValue from @/services/mathEngine/types
    - No mocks needed — pure function tests
    - Tests must FAIL because setsEqual and multiSelectAnswer do not yet exist
    - The answerNumericValue test must FAIL because 'multi_select' case is not yet handled
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="migrations|answerTypes" 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both files exist. Tests run and FAIL with import errors or assertion errors (not syntax errors). No TypeScript errors in the test files themselves (imports will show "module has no exported member" — this is expected and correct for RED state).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NumberPad + MultiSelectMC test stubs</name>
  <files>
    src/__tests__/components/session/NumberPad.test.tsx
    src/__tests__/components/session/MultiSelectMC.test.tsx
  </files>
  <behavior>
    NumberPad.test.tsx:
    - Test 1: renders ± key when allowNegative={true}
    - Test 2: does NOT render ± key when allowNegative={false} (or prop absent)
    - Test 3: pressing ± on value '5' changes display to '-5'
    - Test 4: pressing ± on value '-5' changes display to '5'
    - Test 5: pressing ± on empty value does nothing (no-op)
    - Test 6: maxDigits={2} with allowNegative={true} — can type '-99' (2 significant digits, minus not counted)
    - Test 7: maxDigits={2} with allowNegative={true} — cannot append 3rd digit to '-99'

    MultiSelectMC.test.tsx:
    - Test 1: renders all option labels
    - Test 2: Check button is disabled when no options selected
    - Test 3: Check button is enabled after tapping one option
    - Test 4: tapping an option marks it selected (visual feedback via testID)
    - Test 5: tapping a selected option deselects it
    - Test 6: selecting all correct options and pressing Check calls onAnswer(true)
    - Test 7: selecting only one of two correct options and pressing Check calls onAnswer(false)
    - Test 8: after Check pressed, options show green/red feedback
  </behavior>
  <action>
    Follow tiny-tallies-testing SKILL.md Component Test Template.

    src/__tests__/components/session/NumberPad.test.tsx:
    - Import NumberPad from @/components/session/NumberPad
    - The component likely requires store context or theme — check existing NumberPad for what it reads from store. If it uses useAppStore, set up Approach A mock with minimal state.
    - Use fireEvent.press to simulate key presses. Find keys by text content ('±', '5', '9', etc.)
    - Find display by testID="numberpad-display" (add this testID assumption — Plan 04 will ensure it exists)
    - Tests must FAIL because allowNegative prop does not yet exist

    src/__tests__/components/session/MultiSelectMC.test.tsx:
    - Import MultiSelectMC from @/components/session/MultiSelectMC (component does not exist yet — file will fail to resolve)
    - Props: options=[{value:3},{value:-3},{value:0},{value:6}], correctIndices=[0,1], onAnswer=jest.fn()
    - Mock lucide-react-native if MultiSelectMC imports any icons (checkmark icons likely)
    - Tests must FAIL because the file does not yet exist
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="NumberPad|MultiSelectMC" 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both files exist. Tests run and FAIL with module resolution errors or assertion failures. All 4 Wave 0 stub files are present. Run: ls src/__tests__/store/migrations.test.ts src/__tests__/components/session/NumberPad.test.tsx src/__tests__/mathEngine/answerTypes.test.ts src/__tests__/components/session/MultiSelectMC.test.tsx — all 4 must exist.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `ls src/__tests__/store/migrations.test.ts src/__tests__/mathEngine/answerTypes.test.ts src/__tests__/components/session/NumberPad.test.tsx src/__tests__/components/session/MultiSelectMC.test.tsx` — all 4 files exist
- `npm test -- --testPathPattern="migrations|answerTypes|NumberPad|MultiSelectMC" 2>&1 | grep -E "FAIL|PASS|Tests:"` — all 4 test suites report FAIL (not PASS, not syntax error crash)
- `npm run typecheck` — may have type errors in test files for missing exports (acceptable in Wave 0)
</verification>

<success_criteria>
All 4 Wave 0 test stubs exist on disk and fail when run. The failure mode is "cannot find exported member" or "cannot find module" — NOT "SyntaxError" or "TypeError: require is not a function". The RED state is intentional and correct.
</success_criteria>

<output>
After completion, create `.planning/phases/080-foundation/080-01-SUMMARY.md`
</output>
