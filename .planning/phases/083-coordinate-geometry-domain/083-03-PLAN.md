---
phase: 083-coordinate-geometry-domain
plan: 03
type: execute
wave: 2
depends_on:
  - 083-02
files_modified:
  - src/services/mathEngine/wordProblems/templates.ts
autonomous: false
requirements:
  - COORD-03
  - COORD-04

must_haves:
  truths:
    - "generateWordProblem returns non-null for operation 'coordinate_geometry' at grades 8, 9, and 10"
    - "At least 4 word problem templates cover map/distance, ramp/slope, and city contexts"
    - "All templates use mode: 'prefix' so the original geometry problem text is preserved unchanged"
    - "wordProblems.test.ts passes with 'coordinate_geometry' included in ALL_OPERATIONS"
    - "Manual QA sign-off confirms AI tutor hints guide toward the relevant formula without revealing substitution steps"
  artifacts:
    - path: "src/services/mathEngine/wordProblems/templates.ts"
      provides: "4-6 new word problem templates for coordinate_geometry"
      contains: "wp_coord_map"
  key_links:
    - from: "src/services/mathEngine/wordProblems/generator.ts"
      to: "src/services/mathEngine/wordProblems/templates.ts"
      via: "filter by operation === 'coordinate_geometry'"
      pattern: "WORD_PROBLEM_TEMPLATES.filter"
---

<objective>
Add 4-6 word problem prefix templates for the coordinate_geometry domain and verify them with automated tests. Conclude with a human QA checkpoint to validate AI tutor hint quality (COORD-04 — Socratic formula-identification hints that never reveal substitution steps).

Purpose: COORD-03 requires real-world distance/slope contexts. COORD-04 requires manual QA because the Socratic hint quality cannot be tested automatically — coordinate geometry is novel HS territory (same precedent as Phase 82 linear equations QA).
Output: 4-6 new word problem entries in templates.ts; manual QA checkpoint signed off.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/083-coordinate-geometry-domain/083-RESEARCH.md
@.planning/phases/083-coordinate-geometry-domain/083-02-SUMMARY.md

<interfaces>
<!-- Key contracts from codebase. -->

From src/services/mathEngine/wordProblems/types.ts:
```typescript
export interface WordProblemTemplate {
  id: string;
  operations: MathDomain[];
  template: string;    // context sentence — for prefix mode, prepended before original geometry question
  question: string;    // '' for prefix mode (original question preserved)
  minGrade: number;
  maxGrade?: number;
  mode?: 'prefix' | 'replace'; // prefix = prepend context, keep original equation/geometry text
}
```

From src/services/mathEngine/wordProblems/generator.ts (prefix mode behavior):
- prefix mode: returns { text: contextSentence + ' ' + originalQuestionText, question: originalQuestion }
- {name} and {place} are substitution vars from the word problem system
- {a} and {b} are NOT used in prefix mode (see Research Pitfall 7 — operand mismatch)
- The geometry problem text (e.g., "Find the slope of the line through (2, 3) and (5, 7).") comes from the generator's questionText field — the word problem template just adds a scene-setting sentence in front

RESEARCH NOTE: Use mode: 'prefix' for all coordinate geometry word problems. Coordinate values are internal computation values, not displayable context nouns. The original problem text is self-contained — prefix context is all that is needed. Do NOT use replace mode or reference {a}/{b}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 4-6 word problem prefix templates to templates.ts</name>
  <files>src/services/mathEngine/wordProblems/templates.ts</files>
  <behavior>
    - generateWordProblem('coordinate_geometry', 5, 3, 8, rng) returns non-null
    - generateWordProblem('coordinate_geometry', 5, 3, 9, rng) returns non-null
    - generateWordProblem('coordinate_geometry', 5, 3, 10, rng) returns non-null
    - result.text includes the scene-setting sentence from the template
    - result.text also includes the geometry question text (prefix mode appending behavior)
    - At minimum 4 templates with IDs: wp_coord_map, wp_coord_city, wp_coord_ramp, wp_coord_phone
    - All templates: operations: ['coordinate_geometry'], mode: 'prefix', minGrade: 8, question: ''
  </behavior>
  <action>
    Append a new section to src/services/mathEngine/wordProblems/templates.ts after the linear_equations section. Add a section comment:

    // ═══════════════════════════════════════════════════════════════════════════
    // COORDINATE GEOMETRY — grades 8-10 (prefix mode)
    // ═══════════════════════════════════════════════════════════════════════════

    Add these 4 templates (all mode: 'prefix', question: '', minGrade: 8, operations: ['coordinate_geometry']):

    wp_coord_map:    template: '{name} is reading a map where each unit represents one kilometer.'
    wp_coord_city:   template: '{name} is planning a walking route through {place}.'
    wp_coord_ramp:   template: '{name} is checking the steepness of a ramp for a school project.'
    wp_coord_phone:  template: '{name} is finding the distance between two cell towers on a grid.'
                     minGrade: 9 (slightly more abstract real-world context)

    Optionally add 2 more for grade 9-10 variety:
    wp_coord_bridge: template: '{name} is designing a bridge support and needs to find its slope.'
                     minGrade: 9
    wp_coord_map2:   template: '{name} is plotting the path of a delivery drone on a coordinate grid.'
                     minGrade: 10

    These templates are COPPA compliant — no personal information, no chat, age-appropriate school/real-world contexts. They do NOT use {a}/{b} substitution tokens (prefix mode only).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=wordProblems 2>&1 | tail -20</automated>
  </verify>
  <done>wordProblems.test.ts passes with 'coordinate_geometry' in ALL_OPERATIONS. generateWordProblem returns non-null for grades 8, 9, and 10.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual QA — AI tutor formula-identification hint quality</name>
  <action>
    Run automated suite to confirm all coordinate geometry tests pass, then perform manual QA of AI tutor hints.
    Run: npm test -- --testPathPattern="coordinateGeometry|domainHandlerRegistry|wordProblems|prerequisiteGating"
    Confirm all pass, then proceed to manual hint review as described below.
  </action>
  <what-built>
    Complete coordinate_geometry domain: 6 skills (grades 8-10), 7 templates with domain-specific distractors (FractionAnswer for slope, integer distance/midpoint), 3 bug patterns with formula-identification description strings, 4-6 word problem prefix templates, and AI tutor integration via operation='coordinate_geometry' + bugDescription strings.
  </what-built>
  <how-to-verify>
    1. Start the app: `npx expo start`
    2. Navigate to a practice session for a grade 8-9 student
    3. Trigger a slope problem (e.g., "Find the slope of the line through (1, 2) and (4, 6).")
    4. Answer incorrectly 2-3 times to escalate to HINT mode
    5. Request 3 hints — verify NONE state the slope value directly (e.g., "the slope is 4/3")
    6. Verify hints ask formula-guiding questions: "What do we call the ratio of vertical change to horizontal change?" or "Which direction did the y values change from the first point to the second?"
    7. Repeat for a distance problem — verify hints ask about the Pythagorean theorem formula, not "substitute x1=1, y1=2"
    8. Escalate a problem to BOOST mode — verify the BOOST prompt DOES show the answer (BOOST is allowed to reveal)
    9. Try at least one midpoint and one line-equation problem hint sequence

    Acceptance: Minimum 10 hint outputs reviewed across slope, distance, and midpoint/line problem types; none reveal the formula substitution step directly in HINT mode; at least half guide toward the relevant formula conceptually.

    Per RESEARCH.md: COORD-04 requires Socratic formula-identification framing — "What formula gives us the length of the hypotenuse from the two legs?" not "Use sqrt((x2-x1)^2 + (y2-y1)^2) with x1=1, y1=2, x2=4, y2=6."
  </how-to-verify>
  <verify>
    <automated>npm test -- --testPathPattern="coordinateGeometry|domainHandlerRegistry|wordProblems|prerequisiteGating" 2>&1 | tail -10</automated>
  </verify>
  <done>All automated tests GREEN and manual QA sign-off given: 10+ hints reviewed across slope/distance/midpoint/line types, none reveal substitution steps in HINT mode.</done>
  <resume-signal>Type "approved" after reviewing 10+ hint outputs, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern="coordinateGeometry|domainHandlerRegistry|wordProblems|prerequisiteGating"` — all GREEN
- `npm test` — full suite GREEN
- `npm run typecheck` — 0 errors
- Manual QA checkpoint approved by user
</verification>

<success_criteria>
- 4-6 word problem templates added with prefix mode, grades 8-10, map/distance/ramp/city contexts
- wordProblems.test.ts GREEN with coordinate_geometry in ALL_OPERATIONS
- Full test suite passes
- Manual QA sign-off: 10+ Gemini Socratic hints reviewed across all problem types; none reveal substitution steps in HINT mode
- Phase 83 is complete: all 4 requirements (COORD-01 through COORD-04) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/083-coordinate-geometry-domain/083-03-SUMMARY.md`
</output>
