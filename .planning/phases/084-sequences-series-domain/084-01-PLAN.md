---
phase: 084-sequences-series-domain
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/mathEngine/sequencesSeries.test.ts
  - src/__tests__/mathEngine/domainHandlerRegistry.test.ts
  - src/__tests__/adaptive/prerequisiteGating.test.ts
  - src/__tests__/mathEngine/wordProblems.test.ts
autonomous: true
requirements:
  - SEQ-01
  - SEQ-02
  - SEQ-03
  - SEQ-04

must_haves:
  truths:
    - "src/__tests__/mathEngine/sequencesSeries.test.ts exists with RED tests for all 5 skills, integer answers across seeds, domain_specific distractor strategy, and bug description strings"
    - "domainHandlerRegistry.test.ts updated: 'sequences_series' in ALL_OPERATIONS, count 20→21, skill count 165→170"
    - "prerequisiteGating.test.ts updated: SKILLS.length 165→170"
    - "wordProblems.test.ts updated: 'sequences_series' in ALL_OPERATIONS, gradeMap includes sequences_series: 9"
  artifacts:
    - path: "src/__tests__/mathEngine/sequencesSeries.test.ts"
      provides: "Failing test stubs for SEQ-01 through SEQ-03"
      exports: []
    - path: "src/__tests__/mathEngine/domainHandlerRegistry.test.ts"
      provides: "Updated registry test — 21 operations, 170 skills, sequences_series answer types"
      contains: "sequences_series"
    - path: "src/__tests__/adaptive/prerequisiteGating.test.ts"
      provides: "Updated skill count (165→170)"
      contains: "170"
    - path: "src/__tests__/mathEngine/wordProblems.test.ts"
      provides: "Word problems coverage for sequences_series"
      contains: "sequences_series"
  key_links:
    - from: "sequencesSeries.test.ts"
      to: "src/services/mathEngine/domains/sequencesSeries/index.ts"
      via: "import sequencesSeriesHandler"
      pattern: "sequencesSeriesHandler"
---

<objective>
Create failing test stubs (Wave 0) that define the acceptance contracts for all four SEQ requirements before any implementation exists. Also update three existing test files whose hardcoded counts and operation lists change when sequences_series is added.

Purpose: Nyquist compliance — executor implements against RED tests. The registry test currently asserts 20 operations and 165 skills (post-Phase 83); adding sequences_series changes those to 21 and 170. The prerequisiteGating.test.ts currently asserts 165 (accurate after Phase 83) — update to 170 for the 5 new skills.
Output: One new test file (sequencesSeries.test.ts) and three updated test files. All are RED until Wave 1 lands.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/084-sequences-series-domain/084-RESEARCH.md
@.planning/phases/084-sequences-series-domain/084-VALIDATION.md

<interfaces>
<!-- Key types and contracts from codebase. Executor should use these directly — no codebase exploration needed. -->

From src/services/mathEngine/types.ts (current MathDomain — append 'sequences_series'):
```typescript
export type MathDomain =
  | 'addition' | 'subtraction' | 'multiplication' | 'division'
  | 'fractions' | 'place_value' | 'time' | 'money' | 'patterns'
  | 'measurement' | 'ratios' | 'exponents' | 'expressions'
  | 'geometry' | 'probability' | 'number_theory'
  | 'basic_graphs' | 'data_analysis'
  | 'linear_equations'
  | 'coordinate_geometry';
// Phase 84 will add: 'sequences_series'
```

From src/services/mathEngine/seededRng.ts:
```typescript
export function createRng(seed: number): SeededRng;
export interface SeededRng { intRange(min: number, max: number): number; next(): number; }
```

From src/services/mathEngine/domains/registry.ts:
```typescript
export function getHandler(operation: MathDomain): DomainHandler;
```

From src/services/mathEngine/skills/index.ts:
```typescript
export const SKILLS: readonly SkillDefinition[];
export function getSkillsByOperation(operation: MathDomain): SkillDefinition[];
```

From src/services/mathEngine/bugLibrary/index.ts:
```typescript
export function generateDistractors(answer: Answer, operands: number[], operation: MathDomain, rng: SeededRng): number[];
```

domainHandlerRegistry.test.ts current state (post-Phase 83):
- ALL_OPERATIONS has 20 entries (last entry: 'coordinate_geometry')
- it('has a handler registered for all 20 operations', ...) — must change to 21
- expect(total).toBe(165) — must change to 170
- it('all skills are covered across the 20 domains', ...) — must change to 21
- expectedTypes includes coordinate_geometry: ['numeric', 'fraction']

prerequisiteGating.test.ts current state:
- Asserts: expect(SKILLS.length).toBe(165) — must change to 170

wordProblems.test.ts current state:
- ALL_OPERATIONS has 20 entries (last entry: 'coordinate_geometry')
- gradeMap includes coordinate_geometry: 8
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create sequencesSeries.test.ts with failing stubs</name>
  <files>src/__tests__/mathEngine/sequencesSeries.test.ts</files>
  <behavior>
    - Test: handler is registered and defined for 'sequences_series'
    - Test: getSkillsByOperation('sequences_series').length === 5
    - Test: each of the 5 skill IDs exists: 'arithmetic_next_term', 'arithmetic_nth_term', 'geometric_next_term', 'geometric_nth_term', 'seq_word_problem'
    - Test: each skill has at least one template
    - Test: every template has distractorStrategy === 'domain_specific'
    - Test: all generator outputs are integer-valued across seeds 1-20 — Number.isInteger(answerNumericValue(result.correctAnswer)) for every skill
    - Test: geometric sequence answers are bounded (answerNumericValue(result.correctAnswer) < 2000) across seeds 1-20 for geometric_nth_term template
    - Test: SEQUENCES_SERIES_BUGS has exactly 3 entries with ids: 'seq_arithmetic_wrong_step', 'seq_arithmetic_off_by_one', 'seq_geometric_uses_arithmetic'
    - Test: each bug's description is a non-empty string (feeds bugDescription in AI tutor prompts)
    - Test: generateDistractors for a sequences_series problem returns exactly 3 distractors, none equal to correct answer
  </behavior>
  <action>
    Create src/__tests__/mathEngine/sequencesSeries.test.ts following the same structure as coordinateGeometry.test.ts (same folder: src/__tests__/mathEngine/).

    Import order:
    1. No jest.mock() needed — pure unit tests against engine services
    2. Imports: createRng from seededRng, getHandler from domains/registry, getSkillsByOperation from skills/index, getTemplatesBySkill from templates/index, generateDistractors from bugLibrary/index, SEQUENCES_SERIES_BUGS from bugLibrary/index, answerNumericValue from types

    ALL tests FAIL when created because src/services/mathEngine/domains/sequencesSeries does not exist yet. This is the correct Wave 0 RED state.

    For the integer-solutions test: iterate seeds 1-20 for all 5 skills. Use the first template returned by getTemplatesBySkill for each skill. Call handler.generate(template, createRng(seed)). Assert Number.isInteger(answerNumericValue(result.correctAnswer)).

    For the geometric bounds test: use the geometric_nth_term template specifically (domainConfig.type: 'geometric_nth_term'), iterate seeds 1-20, assert answerNumericValue(result.correctAnswer) < 2000.

    For the bug count test: import SEQUENCES_SERIES_BUGS and assert SEQUENCES_SERIES_BUGS.length === 3. Assert each has a non-empty string description.

    For the distractor test: use arithmetic_next_term template with seed 1, call generateDistractors(result.correctAnswer, result.operands, 'sequences_series', createRng(1)). Assert length === 3 and every distractor !== answerNumericValue(result.correctAnswer).

    File path: src/__tests__/mathEngine/sequencesSeries.test.ts
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=sequencesSeries 2>&1 | tail -20</automated>
  </verify>
  <done>File exists, all tests FAIL with import or assertion errors (not syntax errors). Running the command shows failures, not parse errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update domainHandlerRegistry.test.ts, prerequisiteGating.test.ts, and wordProblems.test.ts</name>
  <files>
    src/__tests__/mathEngine/domainHandlerRegistry.test.ts,
    src/__tests__/adaptive/prerequisiteGating.test.ts,
    src/__tests__/mathEngine/wordProblems.test.ts
  </files>
  <action>
    Update domainHandlerRegistry.test.ts:
    1. Add 'sequences_series' to ALL_OPERATIONS array (after 'coordinate_geometry')
    2. Update the two it() descriptions from '20 operations' to '21 operations'
    3. Update expect(total).toBe(165) to expect(total).toBe(170) (5 new skills)
    4. Add sequences_series: ['numeric'] to the expectedTypes object in the handler answer types test (all sequences_series answers are NumericAnswer — no FractionAnswer)

    Update prerequisiteGating.test.ts:
    1. Find the assertion expect(SKILLS.length).toBe(165)
    2. Change 165 to 170 (sequences_series adds 5 skills)
    3. No other changes needed in this file

    Update wordProblems.test.ts:
    1. Add 'sequences_series' to the ALL_OPERATIONS array (after 'coordinate_geometry')
    2. Add sequences_series: 9 to the gradeMap object (sequences starts at grade 9)

    All three files must compile without syntax errors. TypeScript type errors for 'sequences_series' not yet in MathDomain union are expected RED failures — Wave 1 resolves them.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="domainHandlerRegistry|prerequisiteGating|wordProblems" 2>&1 | tail -20</automated>
  </verify>
  <done>All three files updated. Tests fail because 'sequences_series' is not in MathDomain union yet. No syntax errors in any modified file.</done>
</task>

</tasks>

<verification>
After Wave 0 is complete:
- `npm test -- --testPathPattern=sequencesSeries` fails with import/module-not-found errors
- `npm test -- --testPathPattern=domainHandlerRegistry` fails with type or assertion errors referencing 'sequences_series'
- `npm test -- --testPathPattern=prerequisiteGating` fails (count assertion: 165 no longer matches — expects 170)
- No syntax errors in any modified file
</verification>

<success_criteria>
Four test files exist with the correct test contracts. All new/modified tests FAIL (RED) because the implementation does not exist yet. Wave 1 will turn them GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/084-sequences-series-domain/084-01-SUMMARY.md`
</output>
