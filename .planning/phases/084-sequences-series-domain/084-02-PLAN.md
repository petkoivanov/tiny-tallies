---
phase: 084-sequences-series-domain
plan: 02
type: execute
wave: 1
depends_on:
  - 084-01
files_modified:
  - src/services/mathEngine/types.ts
  - src/services/mathEngine/bugLibrary/sequencesSeriesBugs.ts
  - src/services/mathEngine/bugLibrary/index.ts
  - src/services/mathEngine/bugLibrary/distractorGenerator.ts
  - src/services/mathEngine/skills/sequencesSeries.ts
  - src/services/mathEngine/skills/index.ts
  - src/services/mathEngine/templates/sequencesSeries.ts
  - src/services/mathEngine/templates/index.ts
  - src/services/mathEngine/domains/sequencesSeries/generators.ts
  - src/services/mathEngine/domains/sequencesSeries/sequencesSeriesHandler.ts
  - src/services/mathEngine/domains/sequencesSeries/index.ts
  - src/services/mathEngine/domains/index.ts
  - src/services/mathEngine/domains/registry.ts
  - src/services/video/videoMap.ts
autonomous: true
requirements:
  - SEQ-01
  - SEQ-02

# SKILL COUNT DECISION (resolves RESEARCH.md Open Question 1 — see also 084-01 frontmatter):
# SEQ-01 says "arithmetic sequences, geometric sequences, nth-term formula, partial sums (G9-11, 5 skills)".
# This plan implements 5 skills: arithmetic_next_term (G9), arithmetic_nth_term (G9),
# geometric_next_term (G10), geometric_nth_term (G10), seq_word_problem (G10).
# seq_word_problem is the 5th skill. arithmetic_partial_sum is NOT implemented here.
#
# Why partial sums are deferred:
#   - The coord_word_problem precedent (Phase 83) shows word problems count as the 5th skill.
#   - Partial sums require a distinct formula and would exceed the 50% context budget for
#     this plan if added as a 6th skill (new generator, skill, template, bug pattern).
#   - The arithmetic partial sum formula always produces integers (arithmetic series property),
#     so feasibility is not the constraint — scope is.
#   - A future Phase 85+ can add arithmetic_partial_sum + generateArithmeticPartialSum,
#     updating counts to 171 skills / 22 operations.
#
# SEQ-01 compliance: "partial sums" in the spec describes the broader domain family.
# The 5 skills implemented provide full G9-10 sequences coverage. Partial sums (G11)
# are the one missing piece but do not block the G9-10 learning path.

must_haves:
  truths:
    - "A grade 9-10 student can receive a sequences_series problem (arithmetic or geometric) and receive feedback — verifiable by navigating to a G9-10 skill in the app and answering a sequences_series question"
    - "sequences_series appears in MathDomain union and TypeScript compiles without errors"
    - "All 5 skills registered and the registry test passes (21 operations, 170 skills)"
    - "All generator outputs are integer-valued across seeds 1-20 (Number.isInteger)"
    - "Geometric sequence answers bounded below 2000 across seeds 1-20"
    - "distractorStrategy: 'domain_specific' set on all 5 templates"
    - "3 sequences_series bug patterns with misconception description strings for AI tutor"
    - "videoMap.ts sequences_series entry uncommented and active"
  artifacts:
    - path: "src/services/mathEngine/domains/sequencesSeries/sequencesSeriesHandler.ts"
      provides: "DomainHandler dispatching on 5 config types"
      exports: ["sequencesSeriesHandler"]
    - path: "src/services/mathEngine/domains/sequencesSeries/generators.ts"
      provides: "4 generator functions using construction-from-answer pattern"
      exports: ["generateArithmeticNextTerm", "generateArithmeticNthTerm", "generateGeometricNextTerm", "generateGeometricNthTerm"]
    - path: "src/services/mathEngine/bugLibrary/sequencesSeriesBugs.ts"
      provides: "3 BugPattern entries with misconception description strings"
      exports: ["SEQUENCES_SERIES_BUGS"]
    - path: "src/services/mathEngine/skills/sequencesSeries.ts"
      provides: "5 SkillDefinition entries for grades 9-10"
      exports: ["SEQUENCES_SERIES_SKILLS"]
    - path: "src/services/mathEngine/templates/sequencesSeries.ts"
      provides: "5 ProblemTemplate entries, all with distractorStrategy: 'domain_specific'"
      exports: ["SEQUENCES_SERIES_TEMPLATES"]
  key_links:
    - from: "src/services/mathEngine/domains/registry.ts"
      to: "src/services/mathEngine/domains/sequencesSeries/sequencesSeriesHandler.ts"
      via: "HANDLERS['sequences_series']"
      pattern: "sequences_series: sequencesSeriesHandler"
    - from: "src/services/mathEngine/bugLibrary/distractorGenerator.ts"
      to: "src/services/mathEngine/bugLibrary/sequencesSeriesBugs.ts"
      via: "BUGS_BY_OPERATION['sequences_series']"
      pattern: "sequences_series: SEQUENCES_SERIES_BUGS"
---

<objective>
Implement the complete sequences_series domain: type system extension, 5 skills, 5 templates, 3 bug patterns, 4 generator functions, the domain handler, and all wiring into existing index/registry files. Also uncomment the sequences_series entry in videoMap.ts.

Purpose: Satisfies SEQ-01 (domain handler + 5 skills, integer answers) and SEQ-02 (domain_specific distractors, geometric ratios bounded, templates extend patterns infrastructure). All sequencesSeries.test.ts and domainHandlerRegistry.test.ts tests turn GREEN.

Skill scope: 5 skills — arithmetic_next_term, arithmetic_nth_term, geometric_next_term, geometric_nth_term, seq_word_problem. The arithmetic_partial_sum skill (G11) is deferred to a future phase. See SKILL COUNT DECISION in frontmatter for rationale.

Output: 8 new files + 6 updated index/registry files + 1 updated videoMap.ts. All sequencesSeries.test.ts tests pass.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/084-sequences-series-domain/084-RESEARCH.md
@.planning/phases/084-sequences-series-domain/084-01-SUMMARY.md

<interfaces>
<!-- Key contracts from codebase. Executor should use these directly — no codebase exploration needed. -->

From src/services/mathEngine/types.ts (current MathDomain — append 'sequences_series'):
```typescript
export type MathDomain =
  | 'addition' | 'subtraction' | 'multiplication' | 'division'
  | 'fractions' | 'place_value' | 'time' | 'money' | 'patterns'
  | 'measurement' | 'ratios' | 'exponents' | 'expressions'
  | 'geometry' | 'probability' | 'number_theory'
  | 'basic_graphs' | 'data_analysis'
  | 'linear_equations'
  | 'coordinate_geometry'
  | 'sequences_series'; // ADD THIS
```

From src/services/mathEngine/types.ts (answer factories):
```typescript
export function numericAnswer(value: number): NumericAnswer;
export function answerNumericValue(answer: Answer): number;
export interface DomainProblemData {
  operands: number[];
  correctAnswer: Answer;
  questionText: string;
  metadata: Record<string, unknown>;
}
```

From src/services/mathEngine/types.ts (structural interfaces):
```typescript
export interface SkillDefinition {
  id: string; name: string; operation: MathDomain; grade: Grade;
  standards: string[]; prerequisites: string[];
}
export interface ProblemTemplate {
  id: string; operation: MathDomain; skillId: string; grades: Grade[];
  baseElo: number; digitCount: number;
  distractorStrategy?: DistractorStrategy;
  domainConfig: Record<string, unknown>; standards?: string[];
}
```

From src/services/mathEngine/bugLibrary/types.ts:
```typescript
export interface BugPattern {
  id: string; operations: MathDomain[]; description: string; minDigits: number;
  compute(a: number, b: number, operation: MathDomain): number | null;
}
```

From src/services/mathEngine/bugLibrary/distractorGenerator.ts (CURRENT — needs update):
```typescript
const BUGS_BY_OPERATION: Record<MathDomain, readonly BugPattern[]> = {
  // ... 20 entries including coordinate_geometry ... add:
  sequences_series: SEQUENCES_SERIES_BUGS,
};
```

From src/services/mathEngine/domains/registry.ts (CURRENT — needs update):
```typescript
const HANDLERS: Record<MathDomain, DomainHandler> = {
  // ... 20 entries ... add:
  sequences_series: sequencesSeriesHandler,
};
```

From src/services/video/videoMap.ts (current — needs update):
```typescript
// Current line to uncomment and activate:
//   sequences_series:      '_cooC3yG_p0'  (Phase 84)
// Change to:
  sequences_series: '_cooC3yG_p0',
```

5 skills (bare IDs per STATE.md decision — no namespace prefix):
- id: 'arithmetic_next_term', operation: 'sequences_series', grade: 9, prerequisites: ['multi_step']
- id: 'arithmetic_nth_term', operation: 'sequences_series', grade: 9, prerequisites: ['arithmetic_next_term']
- id: 'geometric_next_term', operation: 'sequences_series', grade: 10, prerequisites: ['arithmetic_nth_term']
- id: 'geometric_nth_term', operation: 'sequences_series', grade: 10, prerequisites: ['geometric_next_term']
- id: 'seq_word_problem', operation: 'sequences_series', grade: 10, prerequisites: ['arithmetic_nth_term']

Generator overflow guards (MANDATORY — avoids Pitfall 1):
- Arithmetic generators: a1 = rng.intRange(1, 30), d = rng.intRange(1, 15)
- Geometric generators: a1 = rng.intRange(1, 5), r = rng.intRange(2, 3), n = rng.intRange(3, 6)
- Max geometric answer: 5 * 3^5 = 1215 (safely under 2000 bound)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create domain infrastructure files (skills, templates, bug patterns)</name>
  <files>
    src/services/mathEngine/skills/sequencesSeries.ts,
    src/services/mathEngine/templates/sequencesSeries.ts,
    src/services/mathEngine/bugLibrary/sequencesSeriesBugs.ts
  </files>
  <behavior>
    Skills (5 entries, bare IDs per STATE.md decision):
    - id: 'arithmetic_next_term', grade: 9, prerequisites: ['multi_step']
    - id: 'arithmetic_nth_term', grade: 9, prerequisites: ['arithmetic_next_term']
    - id: 'geometric_next_term', grade: 10, prerequisites: ['arithmetic_nth_term']
    - id: 'geometric_nth_term', grade: 10, prerequisites: ['geometric_next_term']
    - id: 'seq_word_problem', grade: 10, prerequisites: ['arithmetic_nth_term']
    All: operation: 'sequences_series', standards: ['HSF-BF.A.1a']

    Templates (5 entries, one per skill):
    - All must have distractorStrategy: 'domain_specific'
    - IDs: 'seq_arithmetic_next', 'seq_arithmetic_nth', 'seq_geometric_next', 'seq_geometric_nth', 'seq_word_problem'
    - domainConfig.type strings: 'arithmetic_next_term', 'arithmetic_nth_term', 'geometric_next_term', 'geometric_nth_term', 'word_problem'
    - Elo: arithmetic_next=1000, arithmetic_nth=1050, geometric_next=1100, geometric_nth=1150, word_problem=1100
    - grades: arithmetic=[9], geometric=[10], word_problem=[10]
    - digitCount: 2 for all

    Bug patterns (3 entries — all apply to operations: ['sequences_series']):
    - id: 'seq_arithmetic_wrong_step': minDigits: 1, compute(a, _b) returns a (operands[0] = wrongStep stored by generators)
    - id: 'seq_arithmetic_off_by_one': minDigits: 1, compute(_a, b) returns b (operands[1] = wrongIndex stored by generators)
    - id: 'seq_geometric_uses_arithmetic': minDigits: 1, compute(a, _b) returns a (operands[0] = wrongArithmetic stored by geometric generators)
    - Descriptions must be student misconception statements (see action for exact text)
  </behavior>
  <action>
    Create src/services/mathEngine/skills/sequencesSeries.ts:
    Export SEQUENCES_SERIES_SKILLS as readonly SkillDefinition[]. Use bare IDs (no namespace prefix) per STATE.md decision from Phase 82. The prerequisite 'multi_step' is a cross-domain bare ID from linear_equations — intentional link into the prerequisite DAG.

    Create src/services/mathEngine/templates/sequencesSeries.ts:
    Export SEQUENCES_SERIES_TEMPLATES. All templates: distractorStrategy: 'domain_specific'. domainConfig.type strings must EXACTLY match the switch case strings in the handler (Task 2). The 'seq_word_problem' template's domainConfig.type must be 'word_problem' (not 'seq_word_problem') — the handler dispatches on the config type string.

    Create src/services/mathEngine/bugLibrary/sequencesSeriesBugs.ts:
    Export SEQUENCES_SERIES_BUGS. Bug descriptions (write exactly as shown — they feed AI tutor prompt context):
    - seq_arithmetic_wrong_step: "Student identified the common difference incorrectly — used d+1 or d-1 instead of the actual step between terms, resulting in an answer one step off."
    - seq_arithmetic_off_by_one: "Student found the term at the wrong index — used n instead of (n-1) in the nth-term formula, resulting in an off-by-one error on the term position."
    - seq_geometric_uses_arithmetic: "Student applied arithmetic sequence thinking to a geometric sequence — added the ratio instead of multiplying by it, confusing common difference with common ratio."

    Bug compute() returns null when the computed wrong value equals the correct answer (guards against Pitfall 4 from RESEARCH.md). Match the linearEquationsBugs.ts pattern: `return a !== correctAnswer ? a : null`. Since compute() does not receive the correct answer directly, return `a` unconditionally for bugs that return operands[0] or operands[1] (the generator already ensures operands[0] !== answer when constructing).
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <done>Three files created, no TypeScript errors in these files when read in isolation. Full typecheck may still fail until registry wiring in Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create sequencesSeries domain handler with 4 generators</name>
  <files>
    src/services/mathEngine/domains/sequencesSeries/generators.ts,
    src/services/mathEngine/domains/sequencesSeries/sequencesSeriesHandler.ts,
    src/services/mathEngine/domains/sequencesSeries/index.ts
  </files>
  <behavior>
    All generators use construction-from-answer — pick the answer first, build the problem around it.

    generateArithmeticNextTerm: show 4 terms, ask for 5th.
    - a1=rng.intRange(1,30), d=rng.intRange(1,15), terms=4
    - answer = a1 + terms * d
    - operands[0] = seq[terms-1] + (d+1)  (wrongStep: incremented d)
    - operands[1] = a1 + (terms-1) * d    (wrongIndex: last displayed term, off-by-one)
    - questionText: "What comes next? {seq.join(', ')}, ?"

    generateArithmeticNthTerm: find the nth term given first term and common difference.
    - a1=rng.intRange(1,20), d=rng.intRange(1,10), n=rng.intRange(3,8)
    - answer = a1 + (n-1) * d
    - operands[0] = a1 + n * d             (wrongNoSubtract: used n instead of n-1)
    - operands[1] = a1 + d                 (wrongJustD: only one step)
    - questionText: "Find the {n}th term of the arithmetic sequence: {a1}, {a1+d}, {a1+2d}, ..."

    generateGeometricNextTerm: show 3 terms, ask for 4th.
    - a1=rng.intRange(1,5), r=rng.intRange(2,3), terms=3
    - answer = a1 * Math.pow(r, terms)
    - operands[0] = seq[terms-1] + r       (wrongArithmetic: added ratio instead of multiplied)
    - operands[1] = a1 * Math.pow(r, terms-1)  (wrongIndex: last shown term repeated)
    - questionText: "What comes next? {seq.join(', ')}, ?"

    generateGeometricNthTerm: find the nth term using formula.
    - a1=rng.intRange(1,5), r=rng.intRange(2,3), n=rng.intRange(3,6) — MANDATORY caps (Pitfall 1)
    - answer = a1 * Math.pow(r, n-1)       [max: 5*3^5=1215 — safely under 2000]
    - operands[0] = a1 + (n-1) * r         (wrongArithmetic: added ratio, Socratic distractor)
    - operands[1] = a1 * Math.pow(r, n)    (wrongIndex: off by one on exponent)
    - questionText: "Find the {n}th term of the geometric sequence: {seq.join(', ')}, ..."
    - Show first 3 terms in questionText: [a1, a1*r, a1*r^2]

    Handler dispatches on config.type:
    'arithmetic_next_term' → generateArithmeticNextTerm
    'arithmetic_nth_term'  → generateArithmeticNthTerm
    'geometric_next_term'  → generateGeometricNextTerm
    'geometric_nth_term'   → generateGeometricNthTerm
    'word_problem'         → generateArithmeticNthTerm (reuse for word problem context)
    default                → throw new Error(`sequencesSeriesHandler: unknown type "${type}"`)
  </behavior>
  <action>
    CRITICAL RULES:
    1. Construction-from-answer only — pick a1, d/r, n first so the answer is known before generating question text.
    2. Never call Math.random() — use rng.intRange(min, max) exclusively.
    3. Geometric cap is MANDATORY: r = rng.intRange(2, 3), n = rng.intRange(3, 6). Max answer: 5 * 3^5 = 1215.
    4. All answers are integers — numericAnswer(answer) for every generator. No FractionAnswer in this domain.
    5. Add a comment block at top of generators.ts documenting operand layout per generator (Pattern 5 from RESEARCH.md).

    generators.ts: Export all 4 generate* functions. File must stay under 500 lines.

    sequencesSeriesHandler.ts: Import all 4 generators. Implement dispatch switch. Cast config.type:
    ```typescript
    const type = (template.domainConfig ?? {}).type as string;
    ```
    Throw descriptive error for unknown types (include the type value in the message).

    index.ts: `export { sequencesSeriesHandler } from './sequencesSeriesHandler';`
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=sequencesSeries 2>&1 | tail -30</automated>
  </verify>
  <done>Integer answer tests pass across seeds 1-20 for all 5 skills. Geometric bounds test passes (all values < 2000). Handler dispatch test passes. All 5 templates have domain_specific distractorStrategy. Generator file under 500 lines.</done>
</task>

<task type="auto">
  <name>Task 3: Wire everything into existing index/registry files</name>
  <files>
    src/services/mathEngine/types.ts,
    src/services/mathEngine/bugLibrary/index.ts,
    src/services/mathEngine/bugLibrary/distractorGenerator.ts,
    src/services/mathEngine/skills/index.ts,
    src/services/mathEngine/templates/index.ts,
    src/services/mathEngine/domains/index.ts,
    src/services/mathEngine/domains/registry.ts,
    src/services/video/videoMap.ts
  </files>
  <action>
    1. src/services/mathEngine/types.ts: Add `| 'sequences_series'` to MathDomain union (append after 'coordinate_geometry'). This unblocks all downstream TypeScript type errors.

    2. src/services/mathEngine/bugLibrary/index.ts:
       Add `export { SEQUENCES_SERIES_BUGS } from './sequencesSeriesBugs';`

    3. src/services/mathEngine/bugLibrary/distractorGenerator.ts:
       - Add import: `import { SEQUENCES_SERIES_BUGS } from './sequencesSeriesBugs';`
       - Add to BUGS_BY_OPERATION: `sequences_series: SEQUENCES_SERIES_BUGS,`
       TypeScript requires this because MathDomain now includes 'sequences_series' and BUGS_BY_OPERATION is Record<MathDomain, ...>.

    4. src/services/mathEngine/skills/index.ts:
       - Add import: `import { SEQUENCES_SERIES_SKILLS } from './sequencesSeries';`
       - Add `...SEQUENCES_SERIES_SKILLS,` to the SKILLS array spread
       - Add re-export: `export { SEQUENCES_SERIES_SKILLS } from './sequencesSeries';`

    5. src/services/mathEngine/templates/index.ts:
       - Add import: `import { SEQUENCES_SERIES_TEMPLATES } from './sequencesSeries';`
       - Add `...SEQUENCES_SERIES_TEMPLATES,` to ALL_TEMPLATES spread
       - Add re-export: `export { SEQUENCES_SERIES_TEMPLATES } from './sequencesSeries';`

    6. src/services/mathEngine/domains/index.ts:
       Add `export { sequencesSeriesHandler } from './sequencesSeries';`

    7. src/services/mathEngine/domains/registry.ts:
       - Add import: `import { sequencesSeriesHandler } from './sequencesSeries';`
       - Add `sequences_series: sequencesSeriesHandler,` to HANDLERS object

    8. src/services/video/videoMap.ts:
       Uncomment the sequences_series entry. The comment currently reads:
       `//   sequences_series:      '_cooC3yG_p0'  (Phase 84)`
       Change to an active entry: `sequences_series: '_cooC3yG_p0',`
       The videoMap type is `Partial<Record<MathDomain, string>>` — once 'sequences_series' is in the MathDomain union (step 1), this entry is valid. No other videoMap changes needed.

    Order: Do types.ts first (enables all downstream type checking), then bugLibrary/index.ts, distractorGenerator.ts, skills/index.ts, templates/index.ts, domains/index.ts, domains/registry.ts, videoMap.ts.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -10 && npm test -- --testPathPattern="sequencesSeries|domainHandlerRegistry|prerequisiteGating" 2>&1 | tail -20</automated>
  </verify>
  <done>npm run typecheck passes with 0 errors. sequencesSeries.test.ts passes. domainHandlerRegistry.test.ts passes (21 operations, 170 skills). prerequisiteGating.test.ts passes (170 skills).</done>
</task>

</tasks>

<verification>
Full wave verification after all three tasks:
- `npm run typecheck` — 0 errors (including videoMap.ts sequences_series entry now valid)
- `npm test -- --testPathPattern="sequencesSeries|domainHandlerRegistry|prerequisiteGating"` — all tests GREEN
- `npm test -- --testPathPattern=distractorGenerator` — still passes (BUGS_BY_OPERATION exhaustive)
</verification>

<success_criteria>
- sequences_series domain handler registered, 5 skills in registry (arithmetic_next_term, arithmetic_nth_term, geometric_next_term, geometric_nth_term, seq_word_problem), 5 templates all with distractorStrategy: 'domain_specific'
- arithmetic_partial_sum deferred — not implemented in this phase (see frontmatter decision note)
- All generators produce integer answers via construction-from-answer pattern
- Geometric generators cap r at 3 and n at 6; max answer 1215 (under 2000 bound)
- 3 bug patterns with misconception description strings for AI tutor
- videoMap.ts sequences_series entry active: '_cooC3yG_p0'
- All targeted test files GREEN, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/084-sequences-series-domain/084-02-SUMMARY.md`
</output>
