---
phase: 084-sequences-series-domain
plan: 03
type: execute
wave: 2
depends_on:
  - 084-02
files_modified:
  - src/services/mathEngine/wordProblems/templates.ts
autonomous: false
requirements:
  - SEQ-03
  - SEQ-04

must_haves:
  truths:
    - "generateWordProblem returns non-null for operation 'sequences_series' at grades 9 and 10"
    - "At least 6 word problem templates cover savings, growth, and population contexts"
    - "All templates use mode: 'prefix' so the original sequence question text is preserved unchanged"
    - "wordProblems.test.ts passes with 'sequences_series' included in ALL_OPERATIONS"
    - "Manual QA sign-off confirms AI tutor hints ask about common difference or ratio without revealing the formula"
  artifacts:
    - path: "src/services/mathEngine/wordProblems/templates.ts"
      provides: "6 new word problem templates for sequences_series"
      contains: "wp_seq_savings"
  key_links:
    - from: "src/services/mathEngine/wordProblems/generator.ts"
      to: "src/services/mathEngine/wordProblems/templates.ts"
      via: "filter by operation === 'sequences_series'"
      pattern: "WORD_PROBLEM_TEMPLATES.filter"
---

<objective>
Add 6 word problem prefix templates for the sequences_series domain and verify them with automated tests. Conclude with a human QA checkpoint to validate AI tutor hint quality (SEQ-04 — Socratic questioning about common difference or ratio that never reveals the formula).

Purpose: SEQ-03 requires savings and growth contexts. SEQ-04 requires manual QA because Socratic hint quality for sequences is a new HS topic (same precedent as Phase 82 linear equations and Phase 83 coordinate geometry QA checkpoints).
Output: 6 new word problem entries in templates.ts; manual QA checkpoint signed off.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/084-sequences-series-domain/084-RESEARCH.md
@.planning/phases/084-sequences-series-domain/084-02-SUMMARY.md

<interfaces>
<!-- Key contracts from codebase. -->

From src/services/mathEngine/wordProblems/types.ts:
```typescript
export interface WordProblemTemplate {
  id: string;
  operations: MathDomain[];
  template: string;    // context sentence — prefix mode prepends this before original sequence question
  question: string;    // '' for prefix mode (original question preserved)
  minGrade: number;
  maxGrade?: number;
  mode?: 'prefix' | 'replace';
}
```

Prefix mode behavior (from wordProblems/generator.ts):
- Returns { text: contextSentence + ' ' + originalQuestionText, question: originalQuestion }
- {name} and {place} are valid substitution vars
- Do NOT use {a} or {b} — they represent operand values, not context nouns for sequences (Pitfall 5 from RESEARCH.md)
- The sequence question text (e.g., "Find the 5th term of the arithmetic sequence: 3, 7, 11, ...") comes from the generator's questionText — prefix template just adds a scene-setting sentence

All 6 templates must be COPPA compliant: age-appropriate school/real-world contexts, no personal information, no chat.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 6 word problem prefix templates to templates.ts</name>
  <files>src/services/mathEngine/wordProblems/templates.ts</files>
  <behavior>
    - generateWordProblem('sequences_series', 5, 3, 9, rng) returns non-null
    - generateWordProblem('sequences_series', 5, 3, 10, rng) returns non-null
    - result.text includes the scene-setting sentence from the template
    - result.text also includes the original sequence question text (prefix mode behavior)
    - At minimum 6 templates with IDs: wp_seq_savings, wp_seq_growth, wp_seq_population, wp_seq_stacking, wp_seq_tiles, wp_seq_interest
    - All templates: operations: ['sequences_series'], mode: 'prefix', question: ''
    - Grade split: 3 at minGrade: 9 (arithmetic contexts), 3 at minGrade: 10 (geometric/growth contexts)
  </behavior>
  <action>
    Append a new section to src/services/mathEngine/wordProblems/templates.ts after the coordinate_geometry section. Add a section comment:

    // ═══════════════════════════════════════════════════════════════════════════
    // SEQUENCES & SERIES — grades 9-10 (prefix mode)
    // ═══════════════════════════════════════════════════════════════════════════

    Add these 6 templates (all mode: 'prefix', question: '', operations: ['sequences_series']):

    wp_seq_savings (minGrade: 9):
      template: '{name} is saving the same amount of money each week toward a new bike.'

    wp_seq_growth (minGrade: 9):
      template: '{name} is tracking how a plant grows the same number of centimeters each day.'

    wp_seq_stacking (minGrade: 9):
      template: '{name} is stacking boxes where each row has the same number of boxes more than the row below it.'

    wp_seq_population (minGrade: 10):
      template: '{name} is studying how a bacteria colony doubles in size each hour in a science experiment.'

    wp_seq_tiles (minGrade: 10):
      template: '{name} is tiling a patio where each new row has three times as many tiles as the previous row.'

    wp_seq_interest (minGrade: 10):
      template: '{name} is tracking how an investment grows by the same percentage each year.'

    These contexts map naturally to sequences: savings/growth/stacking → arithmetic; population/tiles/interest → geometric. The prefix sentence provides real-world grounding without needing {a}/{b} operand substitution.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=wordProblems 2>&1 | tail -20</automated>
  </verify>
  <done>wordProblems.test.ts passes with 'sequences_series' in ALL_OPERATIONS. generateWordProblem returns non-null for grades 9 and 10.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual QA — AI tutor Socratic hint quality for sequences</name>
  <action>
    Run automated suite to confirm all sequences tests pass, then perform manual QA of AI tutor hints.
    Run: npm test -- --testPathPattern="sequencesSeries|domainHandlerRegistry|wordProblems|prerequisiteGating"
    Confirm all pass, then proceed to manual hint review as described below.
  </action>
  <what-built>
    Complete sequences_series domain: 5 skills (grades 9-10), 5 templates with domain-specific distractors (integer answers across all generators, geometric values bounded under 2000), 3 bug patterns with misconception description strings, 6 word problem prefix templates (savings/growth/stacking for arithmetic; population/tiles/interest for geometric), and AI tutor integration via operation='sequences_series' + bugDescription strings.
  </what-built>
  <how-to-verify>
    1. Start the app: `npx expo start`
    2. Navigate to a practice session for a grade 9-10 student
    3. Trigger an arithmetic sequence problem (e.g., "What comes next? 3, 7, 11, 15, ?")
    4. Answer incorrectly 2-3 times to escalate to HINT mode
    5. Request 3 hints — verify NONE state the common difference directly (e.g., "the difference is 4" or "add 4 each time")
    6. Verify hints ask guiding questions: "What pattern do you notice between consecutive terms?" or "How much does each term increase compared to the previous one?"
    7. Trigger a geometric sequence problem (e.g., "Find the 4th term of the geometric sequence: 2, 6, 18, ...")
    8. Request 3 hints in HINT mode — verify NONE state the ratio or next term directly
    9. Verify hints guide toward ratio reasoning: "How did the sequence go from the first term to the second?" not "multiply by 3"
    10. Escalate a problem to BOOST mode — verify the BOOST prompt DOES show the answer (BOOST is allowed to reveal)
    11. Try at least one word problem variant to confirm prefix context renders correctly

    Acceptance: Minimum 10 hint outputs reviewed across arithmetic and geometric sequence problem types; none reveal the common difference, ratio, or answer in HINT mode; at least half guide toward the pattern using Socratic questioning.

    Per RESEARCH.md SEQ-04: hints must guide students toward the common difference or ratio concept without naming the value ("What does multiplying by the same number each time tell you about the sequence type?").
  </how-to-verify>
  <verify>
    <automated>npm test -- --testPathPattern="sequencesSeries|domainHandlerRegistry|wordProblems|prerequisiteGating" 2>&1 | tail -10</automated>
  </verify>
  <done>All automated tests GREEN and manual QA sign-off given: 10+ hints reviewed across arithmetic and geometric sequence types, none reveal the common difference or ratio in HINT mode.</done>
  <resume-signal>Type "approved" after reviewing 10+ hint outputs, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern="sequencesSeries|domainHandlerRegistry|wordProblems|prerequisiteGating"` — all GREEN
- `npm test` — full suite GREEN
- `npm run typecheck` — 0 errors
- Manual QA checkpoint approved by user
</verification>

<success_criteria>
- 6 word problem templates added with prefix mode, grades 9-10, savings/growth/population/stacking/tiles/interest contexts
- wordProblems.test.ts GREEN with sequences_series in ALL_OPERATIONS
- Full test suite passes
- Manual QA sign-off: 10+ Gemini Socratic hints reviewed across arithmetic and geometric sequence types; none reveal common difference or ratio in HINT mode
- Phase 84 is complete: all 4 requirements (SEQ-01 through SEQ-04) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/084-sequences-series-domain/084-03-SUMMARY.md`
</output>
