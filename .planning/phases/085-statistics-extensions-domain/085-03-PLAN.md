---
phase: 085-statistics-extensions-domain
plan: 03
type: execute
wave: 2
depends_on:
  - 085-02
files_modified:
  - src/services/mathEngine/wordProblems/templates.ts
autonomous: false
requirements:
  - STATS-03
  - STATS-04

must_haves:
  truths:
    - "generateWordProblem returns non-null for operation 'statistics_hs' at grades 9 and 10"
    - "At least 3 word problem templates cover survey and test-score contexts"
    - "All templates use mode: 'prefix' so the original statistics question text is preserved unchanged"
    - "wordProblems.test.ts passes with 'statistics_hs' included in ALL_OPERATIONS"
    - "Manual QA sign-off confirms AI tutor hints distinguish conceptual questions (normal distribution properties) from computational ones (z-score calculation) in their framing"
  artifacts:
    - path: "src/services/mathEngine/wordProblems/templates.ts"
      provides: "3 new word problem templates for statistics_hs"
      contains: "wp_stats_survey"
  key_links:
    - from: "src/services/mathEngine/wordProblems/generator.ts"
      to: "src/services/mathEngine/wordProblems/templates.ts"
      via: "filter by operation === 'statistics_hs'"
      pattern: "WORD_PROBLEM_TEMPLATES.filter"
---

<objective>
Add 3 word problem prefix templates for the statistics_hs domain and verify them with automated tests. Conclude with a human QA checkpoint to validate AI tutor hint quality (STATS-04 — distinguishing conceptual vs. computational hint framing, neither revealing the z-score answer).

Purpose: STATS-03 requires survey and test-score contexts. STATS-04 requires manual QA because statistics_hs introduces both conceptual (normal distribution) and computational (z-score) problem types that demand different Socratic hint strategies — a new distinction not present in prior HS domains.
Output: 3 new word problem entries in templates.ts; manual QA checkpoint signed off.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/085-statistics-extensions-domain/085-RESEARCH.md
@.planning/phases/085-statistics-extensions-domain/085-02-SUMMARY.md

<interfaces>
<!-- Key contracts from codebase. -->

From src/services/mathEngine/wordProblems/types.ts:
```typescript
export interface WordProblemTemplate {
  id: string;
  operations: MathDomain[];
  template: string;    // context sentence — prefix mode prepends this before original statistics question
  question: string;    // '' for prefix mode (original question preserved)
  minGrade: number;
  maxGrade?: number;
  mode?: 'prefix' | 'replace';
}
```

Prefix mode behavior (from wordProblems/generator.ts):
- Returns { text: contextSentence + ' ' + originalQuestionText, question: originalQuestion }
- {name} and {place} are valid substitution vars
- Do NOT use {a} or {b} — they represent operand values, not context nouns for statistics (same Pitfall 5 as prior HS domains)
- The statistics question text (e.g., "A dataset has mean 70 and standard deviation 10. What is the z-score of the value 90?") comes from the generator's questionText — prefix template just adds a scene-setting sentence

All 3 templates must be COPPA compliant: age-appropriate school/real-world contexts, no personal information, no chat.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 3 word problem prefix templates to templates.ts</name>
  <files>src/services/mathEngine/wordProblems/templates.ts</files>
  <behavior>
    - generateWordProblem('statistics_hs', 5, 3, 9, rng) returns non-null
    - generateWordProblem('statistics_hs', 5, 3, 10, rng) returns non-null
    - result.text includes the scene-setting sentence from the template
    - result.text also includes the original statistics question text (prefix mode behavior)
    - At minimum 3 templates with IDs: wp_stats_survey, wp_stats_scores, wp_stats_data
    - All templates: operations: ['statistics_hs'], mode: 'prefix', question: ''
    - Grade split: 2 at minGrade: 9 (general contexts), 1 at minGrade: 10 (data analysis context)
  </behavior>
  <action>
    Append a new section to src/services/mathEngine/wordProblems/templates.ts after the sequences_series section. Add a section comment:

    // ═══════════════════════════════════════════════════════════════════════════
    // STATISTICS HS — grades 9-10 (prefix mode)
    // ═══════════════════════════════════════════════════════════════════════════

    Add these 3 templates (all mode: 'prefix', question: '', operations: ['statistics_hs']):

    wp_stats_survey (minGrade: 9):
      template: '{name} is analyzing survey results about test scores at {place}.'

    wp_stats_scores (minGrade: 9):
      template: '{name} is studying how exam scores are distributed in a class.'

    wp_stats_data (minGrade: 10):
      template: '{name} collected data on heights of students and is analyzing the spread.'

    These contexts map naturally to statistics: survey/scores → z-score calculation; heights/data → standard deviation concept and normal distribution. The prefix sentence provides real-world grounding without needing {a}/{b} operand substitution.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=wordProblems 2>&1 | tail -20</automated>
  </verify>
  <done>wordProblems.test.ts passes with 'statistics_hs' in ALL_OPERATIONS. generateWordProblem returns non-null for grades 9 and 10.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual QA — AI tutor Socratic hint quality for statistics</name>
  <action>
    Run automated suite to confirm all statistics tests pass, then perform manual QA of AI tutor hints.
    Run: npm test -- --testPathPattern="statisticsHs|domainHandlerRegistry|wordProblems|prerequisiteGating"
    Confirm all pass, then proceed to manual hint review as described below.
  </action>
  <what-built>
    Complete statistics_hs domain: 5 skills (grades 9-10), 5 templates with domain-specific distractors (integer answers across all generators, z-scores bounded in [-2, 2]), 3 bug patterns with misconception description strings, 3 word problem prefix templates (survey/scores/data contexts), and AI tutor integration via operation='statistics_hs' + bugDescription strings.
  </what-built>
  <how-to-verify>
    1. Start the app: `npx expo start`
    2. Navigate to a practice session for a grade 9-10 student
    3. Trigger a z-score calculation problem (e.g., "A dataset has mean 70 and standard deviation 10. What is the z-score of the value 90?")
    4. Answer incorrectly 2-3 times to escalate to HINT mode
    5. Request 3 hints — verify NONE state the z-score formula answer directly (e.g., "the z-score is 2" or "divide 20 by 10")
    6. Verify computational hints ask guiding questions about the formula structure: "What information do you need to find how far from the mean a value is?" or "What does dividing by the standard deviation tell you?"
    7. Trigger a normal distribution property problem (e.g., "What percent of values fall within 1σ of the mean?")
    8. Request 3 hints in HINT mode — verify hints are CONCEPTUAL, not formula-based: "What does the bell curve shape tell you about where most values cluster?" rather than "The answer uses 68%"
    9. Verify the hint framing clearly DIFFERS between the z-score type (formula reasoning) and the normal rule type (conceptual reasoning about distributions)
    10. Escalate a z-score problem to BOOST mode — verify the BOOST prompt DOES show the answer (BOOST is allowed to reveal)
    11. Try at least one word problem variant to confirm prefix context renders correctly before the statistics question

    Acceptance: Minimum 10 hint outputs reviewed across z-score and normal distribution problem types; none reveal the z-score value or percentage in HINT mode; hints for z-score problems ask about formula structure while hints for normal distribution problems ask about bell curve concepts.

    Per RESEARCH.md STATS-04: the AI tutor should distinguish between "what does the formula mean?" (z-score) and "what does the bell curve tell you?" (normal distribution) framing — never naming the answer in either case.
  </how-to-verify>
  <verify>
    <automated>npm test -- --testPathPattern="statisticsHs|domainHandlerRegistry|wordProblems|prerequisiteGating" 2>&1 | tail -10</automated>
  </verify>
  <done>All automated tests GREEN and manual QA sign-off given: 10+ hints reviewed across z-score and normal distribution problem types, none reveal the answer in HINT mode, and hint framing clearly distinguishes computational from conceptual question types.</done>
  <resume-signal>Type "approved" after reviewing 10+ hint outputs, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern="statisticsHs|domainHandlerRegistry|wordProblems|prerequisiteGating"` — all GREEN
- `npm test` — full suite GREEN
- `npm run typecheck` — 0 errors
- Manual QA checkpoint approved by user
</verification>

<success_criteria>
- 3 word problem templates added with prefix mode, grades 9-10, survey/scores/data contexts
- wordProblems.test.ts GREEN with statistics_hs in ALL_OPERATIONS
- Full test suite passes
- Manual QA sign-off: 10+ Gemini Socratic hints reviewed across z-score and normal distribution problem types; none reveal the answer in HINT mode; conceptual hints clearly differ in framing from computational hints
- Phase 85 is complete: all 4 requirements (STATS-01 through STATS-04) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/085-statistics-extensions-domain/085-03-SUMMARY.md`
</output>
