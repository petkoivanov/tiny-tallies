---
phase: 085-statistics-extensions-domain
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/mathEngine/statisticsHs.test.ts
  - src/__tests__/mathEngine/domainHandlerRegistry.test.ts
  - src/__tests__/adaptive/prerequisiteGating.test.ts
  - src/__tests__/mathEngine/wordProblems.test.ts
autonomous: true
requirements:
  - STATS-01
  - STATS-02
  - STATS-03
  - STATS-04

must_haves:
  truths:
    - "src/__tests__/mathEngine/statisticsHs.test.ts exists with RED tests for all 5 skills (stats_stddev_concept, stats_normal_rule, stats_zscore, stats_percentile, stats_word_problem), integer answers across seeds 1-20, domain_specific distractorStrategy on all templates, and bug description strings"
    - "domainHandlerRegistry.test.ts updated: 'statistics_hs' in ALL_OPERATIONS, count 21→22, skill count 170→175, statistics_hs: ['numeric'] in expectedTypes"
    - "prerequisiteGating.test.ts updated: SKILLS.length 170→175"
    - "wordProblems.test.ts updated: 'statistics_hs' in ALL_OPERATIONS, gradeMap includes statistics_hs: 9"
  artifacts:
    - path: "src/__tests__/mathEngine/statisticsHs.test.ts"
      provides: "Failing test stubs for STATS-01 through STATS-04"
      exports: []
    - path: "src/__tests__/mathEngine/domainHandlerRegistry.test.ts"
      provides: "Updated registry test — 22 operations, 175 skills, statistics_hs answer types"
      contains: "statistics_hs"
    - path: "src/__tests__/adaptive/prerequisiteGating.test.ts"
      provides: "Updated skill count (170→175)"
      contains: "175"
    - path: "src/__tests__/mathEngine/wordProblems.test.ts"
      provides: "Word problems coverage for statistics_hs"
      contains: "statistics_hs"
  key_links:
    - from: "statisticsHs.test.ts"
      to: "src/services/mathEngine/domains/statisticsHs/index.ts"
      via: "import statisticsHsHandler"
      pattern: "statisticsHsHandler"
---

<objective>
Create failing test stubs (Wave 0) that define the acceptance contracts for all four STATS requirements before any implementation exists. Also update three existing test files whose hardcoded counts and operation lists change when statistics_hs is added.

Purpose: Nyquist compliance — executor implements against RED tests. The registry test currently asserts 21 operations and 170 skills (post-Phase 84); adding statistics_hs changes those to 22 and 175. prerequisiteGating.test.ts currently asserts 170 — update to 175 for the 5 new skills.

Output: One new test file (statisticsHs.test.ts) and three updated test files. All are RED until Wave 1 lands.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/085-statistics-extensions-domain/085-RESEARCH.md
@.planning/phases/085-statistics-extensions-domain/085-VALIDATION.md

<interfaces>
<!-- Key types and contracts from codebase. Executor should use these directly — no codebase exploration needed. -->

From src/services/mathEngine/types.ts (current MathDomain — append 'statistics_hs'):
```typescript
export type MathDomain =
  | 'addition' | 'subtraction' | 'multiplication' | 'division'
  | 'fractions' | 'place_value' | 'time' | 'money' | 'patterns'
  | 'measurement' | 'ratios' | 'exponents' | 'expressions'
  | 'geometry' | 'probability' | 'number_theory'
  | 'basic_graphs' | 'data_analysis'
  | 'linear_equations'
  | 'coordinate_geometry'
  | 'sequences_series';
// Phase 85 will add: 'statistics_hs'
```

From src/services/mathEngine/seededRng.ts:
```typescript
export function createRng(seed: number): SeededRng;
export interface SeededRng { intRange(min: number, max: number): number; choice<T>(arr: T[]): T; next(): number; }
```

From src/services/mathEngine/domains/registry.ts:
```typescript
export function getHandler(operation: MathDomain): DomainHandler;
```

From src/services/mathEngine/skills/index.ts:
```typescript
export const SKILLS: readonly SkillDefinition[];
export function getSkillsByOperation(operation: MathDomain): SkillDefinition[];
```

From src/services/mathEngine/bugLibrary/index.ts:
```typescript
export function generateDistractors(answer: Answer, operands: number[], operation: MathDomain, rng: SeededRng): number[];
```

domainHandlerRegistry.test.ts current state (post-Phase 84):
- ALL_OPERATIONS has 21 entries (last entry: 'sequences_series')
- it('has a handler registered for all 21 operations', ...) — must change to 22
- expect(total).toBe(170) — must change to 175
- it('all skills are covered across the 21 domains', ...) — must change to 22
- expectedTypes includes sequences_series: ['numeric']

prerequisiteGating.test.ts current state:
- Asserts: expect(SKILLS.length).toBe(170) — must change to 175

wordProblems.test.ts current state:
- ALL_OPERATIONS has 21 entries (last entry: 'sequences_series')
- gradeMap includes sequences_series: 9
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create statisticsHs.test.ts with failing stubs</name>
  <files>src/__tests__/mathEngine/statisticsHs.test.ts</files>
  <behavior>
    - Test: handler is registered and defined for 'statistics_hs'
    - Test: getSkillsByOperation('statistics_hs').length === 5
    - Test: each of the 5 skill IDs exists: 'stats_stddev_concept', 'stats_normal_rule', 'stats_zscore', 'stats_percentile', 'stats_word_problem'
    - Test: each skill has at least one template
    - Test: every template has distractorStrategy === 'domain_specific'
    - Test: all generator outputs are integer-valued across seeds 1-20 — Number.isInteger(answerNumericValue(result.correctAnswer)) for every skill
    - Test: z-score answers are bounded: answerNumericValue(result.correctAnswer) in range [-2, 2] for stats_zscore across seeds 1-20
    - Test: STATISTICS_HS_BUGS has exactly 3 entries with ids: 'stats_zscore_sign_flip', 'stats_zscore_forgot_mean', 'stats_normal_wrong_band'
    - Test: each bug's description is a non-empty string (feeds bugDescription in AI tutor prompts)
    - Test: generateDistractors for a statistics_hs problem returns exactly 3 distractors, none equal to correct answer
  </behavior>
  <action>
    Create src/__tests__/mathEngine/statisticsHs.test.ts following the same structure as sequencesSeries.test.ts (same folder: src/__tests__/mathEngine/).

    Import order:
    1. No jest.mock() needed — pure unit tests against engine services
    2. Imports: createRng from seededRng, getHandler from domains/registry, getSkillsByOperation from skills/index, getTemplatesBySkill from templates/index, generateDistractors from bugLibrary/index, STATISTICS_HS_BUGS from bugLibrary/index, answerNumericValue from types

    ALL tests FAIL when created because src/services/mathEngine/domains/statisticsHs does not exist yet. This is the correct Wave 0 RED state.

    For the integer-solutions test: iterate seeds 1-20 for all 5 skills. Use the first template returned by getTemplatesBySkill for each skill. Call handler.generate(template, createRng(seed)). Assert Number.isInteger(answerNumericValue(result.correctAnswer)).

    For the z-score bounds test: use the stats_zscore template specifically (domainConfig.type: 'zscore'), iterate seeds 1-20, assert answerNumericValue(result.correctAnswer) >= -2 && answerNumericValue(result.correctAnswer) <= 2.

    For the bug count test: import STATISTICS_HS_BUGS and assert STATISTICS_HS_BUGS.length === 3. Assert each has a non-empty string description.

    For the distractor test: use stats_zscore template with seed 1, call generateDistractors(result.correctAnswer, result.operands, 'statistics_hs', createRng(1)). Assert length === 3 and every distractor !== answerNumericValue(result.correctAnswer).

    File path: src/__tests__/mathEngine/statisticsHs.test.ts
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=statisticsHs 2>&1 | tail -20</automated>
  </verify>
  <done>File exists, all tests FAIL with import or assertion errors (not syntax errors). Running the command shows failures, not parse errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update domainHandlerRegistry.test.ts, prerequisiteGating.test.ts, and wordProblems.test.ts</name>
  <files>
    src/__tests__/mathEngine/domainHandlerRegistry.test.ts,
    src/__tests__/adaptive/prerequisiteGating.test.ts,
    src/__tests__/mathEngine/wordProblems.test.ts
  </files>
  <action>
    Update domainHandlerRegistry.test.ts:
    1. Add 'statistics_hs' to ALL_OPERATIONS array (after 'sequences_series')
    2. Update the two it() descriptions from '21 operations' to '22 operations'
    3. Update expect(total).toBe(170) to expect(total).toBe(175) (5 new skills)
    4. Add statistics_hs: ['numeric'] to the expectedTypes object in the handler answer types test (all statistics_hs answers are NumericAnswer — no FractionAnswer)

    Update prerequisiteGating.test.ts:
    1. Find the assertion expect(SKILLS.length).toBe(170)
    2. Change 170 to 175 (statistics_hs adds 5 skills)
    3. No other changes needed in this file

    Update wordProblems.test.ts:
    1. Add 'statistics_hs' to the ALL_OPERATIONS array (after 'sequences_series')
    2. Add statistics_hs: 9 to the gradeMap object (statistics_hs starts at grade 9)

    All three files must compile without syntax errors. TypeScript type errors for 'statistics_hs' not yet in MathDomain union are expected RED failures — Wave 1 resolves them.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="domainHandlerRegistry|prerequisiteGating|wordProblems" 2>&1 | tail -20</automated>
  </verify>
  <done>All three files updated. Tests fail because 'statistics_hs' is not in MathDomain union yet. No syntax errors in any modified file.</done>
</task>

</tasks>

<verification>
After Wave 0 is complete:
- `npm test -- --testPathPattern=statisticsHs` fails with import/module-not-found errors
- `npm test -- --testPathPattern=domainHandlerRegistry` fails with type or assertion errors referencing 'statistics_hs'
- `npm test -- --testPathPattern=prerequisiteGating` fails (count assertion: 170 no longer matches — expects 175)
- No syntax errors in any modified file
</verification>

<success_criteria>
Four test files exist with the correct test contracts. All new/modified tests FAIL (RED) because the implementation does not exist yet. Wave 1 will turn them GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/085-statistics-extensions-domain/085-01-SUMMARY.md`
</output>
