---
phase: 085-statistics-extensions-domain
plan: 02
type: execute
wave: 1
depends_on:
  - 085-01
files_modified:
  - src/services/mathEngine/types.ts
  - src/services/mathEngine/bugLibrary/statisticsHsBugs.ts
  - src/services/mathEngine/bugLibrary/index.ts
  - src/services/mathEngine/bugLibrary/distractorGenerator.ts
  - src/services/mathEngine/skills/statisticsHs.ts
  - src/services/mathEngine/skills/index.ts
  - src/services/mathEngine/templates/statisticsHs.ts
  - src/services/mathEngine/templates/index.ts
  - src/services/mathEngine/domains/statisticsHs/generators.ts
  - src/services/mathEngine/domains/statisticsHs/statisticsHsHandler.ts
  - src/services/mathEngine/domains/statisticsHs/index.ts
  - src/services/mathEngine/domains/index.ts
  - src/services/mathEngine/domains/registry.ts
  - src/services/video/videoMap.ts
autonomous: true
requirements:
  - STATS-01
  - STATS-02

must_haves:
  truths:
    - "A grade 9-10 student can receive a statistics_hs problem (std dev concept, normal rule, z-score, percentile) and receive feedback"
    - "statistics_hs appears in MathDomain union and TypeScript compiles without errors"
    - "All 5 skills registered and the registry test passes (22 operations, 175 skills)"
    - "All generator outputs are integer-valued across seeds 1-20 (Number.isInteger)"
    - "Z-score answers are bounded in range [-2, 2] across seeds 1-20"
    - "distractorStrategy: 'domain_specific' set on all 5 templates"
    - "3 statistics_hs bug patterns with misconception description strings for AI tutor"
    - "videoMap.ts statistics_hs entry uncommented and active"
  artifacts:
    - path: "src/services/mathEngine/domains/statisticsHs/statisticsHsHandler.ts"
      provides: "DomainHandler dispatching on 5 config types"
      exports: ["statisticsHsHandler"]
    - path: "src/services/mathEngine/domains/statisticsHs/generators.ts"
      provides: "4 generator functions using construction-from-answer pattern"
      exports: ["generateStdDevConcept", "generateNormalDistribution", "generateZScore", "generatePercentile"]
    - path: "src/services/mathEngine/bugLibrary/statisticsHsBugs.ts"
      provides: "3 BugPattern entries with misconception description strings"
      exports: ["STATISTICS_HS_BUGS"]
    - path: "src/services/mathEngine/skills/statisticsHs.ts"
      provides: "5 SkillDefinition entries for grades 9-10"
      exports: ["STATISTICS_HS_SKILLS"]
    - path: "src/services/mathEngine/templates/statisticsHs.ts"
      provides: "5 ProblemTemplate entries, all with distractorStrategy: 'domain_specific'"
      exports: ["STATISTICS_HS_TEMPLATES"]
  key_links:
    - from: "src/services/mathEngine/domains/registry.ts"
      to: "src/services/mathEngine/domains/statisticsHs/statisticsHsHandler.ts"
      via: "HANDLERS['statistics_hs']"
      pattern: "statistics_hs: statisticsHsHandler"
    - from: "src/services/mathEngine/bugLibrary/distractorGenerator.ts"
      to: "src/services/mathEngine/bugLibrary/statisticsHsBugs.ts"
      via: "BUGS_BY_OPERATION['statistics_hs']"
      pattern: "statistics_hs: STATISTICS_HS_BUGS"
---

<objective>
Implement the complete statistics_hs domain: type system extension, 5 skills, 5 templates, 3 bug patterns, 4 generator functions, the domain handler, and all wiring into existing index/registry files. Also uncomment the statistics_hs entry in videoMap.ts.

Purpose: Satisfies STATS-01 (domain handler + 5 skills, integer answers) and STATS-02 (domain_specific distractors, integer-safe z-scores, templates extend data_analysis infrastructure). All statisticsHs.test.ts and domainHandlerRegistry.test.ts tests turn GREEN.

Output: 8 new files + 6 updated index/registry files + 1 updated videoMap.ts. All statisticsHs.test.ts tests pass.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/085-statistics-extensions-domain/085-RESEARCH.md
@.planning/phases/085-statistics-extensions-domain/085-01-SUMMARY.md

<interfaces>
<!-- Key contracts from codebase. Executor should use these directly — no codebase exploration needed. -->

From src/services/mathEngine/types.ts (current MathDomain — append 'statistics_hs'):
```typescript
export type MathDomain =
  | 'addition' | 'subtraction' | 'multiplication' | 'division'
  | 'fractions' | 'place_value' | 'time' | 'money' | 'patterns'
  | 'measurement' | 'ratios' | 'exponents' | 'expressions'
  | 'geometry' | 'probability' | 'number_theory'
  | 'basic_graphs' | 'data_analysis'
  | 'linear_equations'
  | 'coordinate_geometry'
  | 'sequences_series'
  | 'statistics_hs'; // ADD THIS
```

From src/services/mathEngine/types.ts (answer factories):
```typescript
export function numericAnswer(value: number): NumericAnswer;
export function answerNumericValue(answer: Answer): number;
export interface DomainProblemData {
  operands: number[];
  correctAnswer: Answer;
  questionText: string;
  metadata: Record<string, unknown>;
}
```

From src/services/mathEngine/types.ts (structural interfaces):
```typescript
export interface SkillDefinition {
  id: string; name: string; operation: MathDomain; grade: Grade;
  standards: string[]; prerequisites: string[];
}
export interface ProblemTemplate {
  id: string; operation: MathDomain; skillId: string; grades: Grade[];
  baseElo: number; digitCount: number;
  distractorStrategy?: DistractorStrategy;
  domainConfig: Record<string, unknown>; standards?: string[];
}
```

From src/services/mathEngine/bugLibrary/types.ts:
```typescript
export interface BugPattern {
  id: string; operations: MathDomain[]; description: string; minDigits: number;
  compute(a: number, b: number, operation: MathDomain): number | null;
}
```

From src/services/mathEngine/bugLibrary/distractorGenerator.ts (CURRENT — needs update):
```typescript
const BUGS_BY_OPERATION: Record<MathDomain, readonly BugPattern[]> = {
  // ... 21 entries including sequences_series ... add:
  statistics_hs: STATISTICS_HS_BUGS,
};
```

From src/services/mathEngine/domains/registry.ts (CURRENT — needs update):
```typescript
const HANDLERS: Record<MathDomain, DomainHandler> = {
  // ... 21 entries ... add:
  statistics_hs: statisticsHsHandler,
};
```

From src/services/video/videoMap.ts (current — needs update):
```typescript
// Current comment to uncomment and activate:
//   statistics_hs:         'h8EYEJ32oQ8'  (Phase 85)
// Change to:
  statistics_hs: 'h8EYEJ32oQ8',
```

5 skills (stats_ prefix per STATE.md decision [Phase 083] — prefixed IDs to avoid collision):
- id: 'stats_stddev_concept', operation: 'statistics_hs', grade: 9, prerequisites: ['data-analysis.scatter-trend']
- id: 'stats_normal_rule', operation: 'statistics_hs', grade: 9, prerequisites: ['stats_stddev_concept']
- id: 'stats_zscore', operation: 'statistics_hs', grade: 10, prerequisites: ['stats_normal_rule']
- id: 'stats_percentile', operation: 'statistics_hs', grade: 10, prerequisites: ['stats_zscore']
- id: 'stats_word_problem', operation: 'statistics_hs', grade: 10, prerequisites: ['stats_zscore']

Generator integer-safety rules (MANDATORY — avoids Pitfall 1 from RESEARCH.md):
- generateZScore: z = rng.intRange(-2, 2), sigma = rng.choice([5, 10, 15, 20]), mu = rng.intRange(5, 15) * 10, x = mu + z * sigma
- generateNormalDistribution: only 68% (1σ) and 95% (2σ) bands — skip 3σ (avoids 99.7 decimal)
- generateStdDevConcept: answer is 1 or 2 (dataset index) — always integer
- generatePercentile: fixed lookup table — z=0→50, z=1→84, z=-1→16, z=2→97, z=-2→2
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create domain infrastructure files (skills, templates, bug patterns)</name>
  <files>
    src/services/mathEngine/skills/statisticsHs.ts,
    src/services/mathEngine/templates/statisticsHs.ts,
    src/services/mathEngine/bugLibrary/statisticsHsBugs.ts
  </files>
  <behavior>
    Skills (5 entries, stats_ prefix per STATE.md decision [Phase 083]):
    - id: 'stats_stddev_concept', grade: 9, prerequisites: ['data-analysis.scatter-trend']
    - id: 'stats_normal_rule', grade: 9, prerequisites: ['stats_stddev_concept']
    - id: 'stats_zscore', grade: 10, prerequisites: ['stats_normal_rule']
    - id: 'stats_percentile', grade: 10, prerequisites: ['stats_zscore']
    - id: 'stats_word_problem', grade: 10, prerequisites: ['stats_zscore']
    All: operation: 'statistics_hs'

    Templates (5 entries, one per skill):
    - All must have distractorStrategy: 'domain_specific'
    - skillIds match skill IDs exactly (stats_stddev_concept etc.)
    - domainConfig.type strings: 'stddev_concept', 'normal_rule', 'zscore', 'percentile', 'word_problem'
    - Elo: stddev_concept=1000, normal_rule=1050, zscore=1100, percentile=1150, word_problem=1100
    - grades: stddev_concept=[9], normal_rule=[9], zscore=[10], percentile=[10], word_problem=[10]
    - digitCount: 1 for all (z-scores are single-digit integers in range [-2,2])
    - standards: stddev_concept=['HSS-ID.A.2'], all others=['HSS-ID.A.4']

    Bug patterns (3 entries — all apply to operations: ['statistics_hs' as MathDomain]):
    - id: 'stats_zscore_sign_flip': minDigits: 1, compute(a, _b) returns a (operands[0] = wrongSignFlip stored by generators)
    - id: 'stats_zscore_forgot_mean': minDigits: 1, compute(_a, b) returns b (operands[1] = wrongForgetMu stored by generators)
    - id: 'stats_normal_wrong_band': minDigits: 1, compute(a, _b) returns a (operands[0] = wrongBand stored by normal rule generators)
    - Descriptions must be student misconception statements (see action for exact text)
  </behavior>
  <action>
    Create src/services/mathEngine/skills/statisticsHs.ts:
    Export STATISTICS_HS_SKILLS as readonly SkillDefinition[]. Use stats_ prefix on all IDs per STATE.md decision [Phase 083] which established domain-prefixed IDs for HS skills to prevent Map key collision in computeNodePositions. The prerequisite 'data-analysis.scatter-trend' uses dot-notation matching the K-8 skill ID format — this is the exact string from dataAnalysis.ts.

    Create src/services/mathEngine/templates/statisticsHs.ts:
    Export STATISTICS_HS_TEMPLATES. All templates: distractorStrategy: 'domain_specific'. domainConfig.type strings must EXACTLY match the switch case strings in the handler (Task 2). The 'stats_word_problem' template's domainConfig.type must be 'word_problem' (not 'stats_word_problem') — the handler dispatches on the config type string.

    Create src/services/mathEngine/bugLibrary/statisticsHsBugs.ts:
    Export STATISTICS_HS_BUGS. Bug descriptions (write exactly as shown — they feed AI tutor prompt context):
    - stats_zscore_sign_flip: "Student negated the z-score — subtracted μ from x in the wrong order or got the opposite sign, giving -(correct z) instead of the correct value."
    - stats_zscore_forgot_mean: "Student forgot to subtract the mean — computed only z × σ without the (x − μ) step, confusing z·σ with x − μ."
    - stats_normal_wrong_band: "Student confused the 68% and 95% normal distribution bands — applied the 1σ rule when the 2σ rule was needed, or vice versa."

    Bug compute() returns the stored operand unconditionally (generator ensures operands differ from answer). Match the sequencesSeriesBugs.ts pattern.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <done>Three files created, no TypeScript errors in these files when read in isolation. Full typecheck may still fail until registry wiring in Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create statisticsHs domain handler with 4 generators</name>
  <files>
    src/services/mathEngine/domains/statisticsHs/generators.ts,
    src/services/mathEngine/domains/statisticsHs/statisticsHsHandler.ts,
    src/services/mathEngine/domains/statisticsHs/index.ts
  </files>
  <behavior>
    All generators use construction-from-answer — pick the answer first, build the problem around it.

    generateStdDevConcept: present two datasets A and B, ask which has the larger spread.
    - answer is 1 or 2 (integer index) — pick randomly with rng.intRange(1, 2)
    - If answer=1: Dataset A has larger spread (wider range of values)
    - If answer=2: Dataset B has larger spread
    - operands[0] = 3 - answer (the other dataset index — wrong choice)
    - operands[1] = 0 (unused — bug pattern returns operands[0])
    - Generate two datasets with different spreads using rng; the "winning" dataset has larger spread
    - questionText: "Dataset A: {valuesA.join(', ')}. Dataset B: {valuesB.join(', ')}. Which dataset has the larger standard deviation? (Enter 1 for A or 2 for B)"

    generateNormalDistribution: ask what percent of values fall within Nσ of the mean.
    - Only generate 68% (1σ) and 95% (2σ) questions — skip 3σ (avoids 99.7 decimal, per RESEARCH.md)
    - answer is 68 or 95 — pick via rng.choice([68, 95])
    - sigma_count = answer === 68 ? 1 : 2
    - operands[0] = answer === 68 ? 95 : 68 (wrong band — swapped rule)
    - operands[1] = 100 - answer (wrong tail interpretation)
    - questionText: "In a normal distribution with mean μ and standard deviation σ, approximately what percent of values fall within {sigma_count}σ of the mean?"

    generateZScore: calculate z = (x − μ) / σ with integer inputs.
    - Construction-from-answer: z = rng.intRange(-2, 2), sigma = rng.choice([5, 10, 15, 20]), mu = rng.intRange(5, 15) * 10, x = mu + z * sigma
    - answer = z (always integer in [-2, 2])
    - operands[0] = -z (wrongSignFlip: negated z)
    - operands[1] = x - mu (wrongForgetMu: forgot to divide by sigma; note: this equals z * sigma)
    - operands[1] must differ from answer; if z * sigma === z (when sigma=1, impossible since sigma ∈ {5,10,15,20}), it cannot collide
    - questionText: "A dataset has mean {mu} and standard deviation {sigma}. What is the z-score of the value {x}?"

    generatePercentile: given a z-score context, ask what percentile.
    - Use fixed lookup table: z=-2→2, z=-1→16, z=0→50, z=1→84, z=2→97
    - Pick z = rng.choice([-2, -1, 0, 1, 2]), answer = PERCENTILE_TABLE[z]
    - operands[0] = PERCENTILE_TABLE[-z] (wrong sign — used opposite z)
    - operands[1] = 100 - answer (wrong tail interpretation)
    - questionText: "In a normal distribution, a value with a z-score of {z} is at approximately what percentile?"

    Handler dispatches on config.type:
    'stddev_concept'  → generateStdDevConcept
    'normal_rule'     → generateNormalDistribution
    'zscore'          → generateZScore
    'percentile'      → generatePercentile
    'word_problem'    → generateZScore (reuse z-score generator for word problem context)
    default           → throw new Error(`statisticsHsHandler: unknown domainConfig.type "${type}".`)
  </behavior>
  <action>
    CRITICAL RULES:
    1. Construction-from-answer only — pick answer first, build problem around it.
    2. Never call Math.random() — use rng.intRange(min, max) and rng.choice([...]) exclusively.
    3. Z-score range is MANDATORY: z = rng.intRange(-2, 2). Max z-score is 2, min is -2. This keeps bounds tight.
    4. Normal distribution: only 68 and 95 — do NOT generate 99.7 questions (decimal pitfall).
    5. Percentile lookup table is FIXED — do not compute percentiles algorithmically.
    6. All answers are integers — numericAnswer(answer) for every generator. No FractionAnswer in this domain.
    7. Add a comment block at top of generators.ts documenting operand layout per generator.

    generators.ts: Export all 4 generate* functions. File must stay under 500 lines.

    PERCENTILE_TABLE in generators.ts (use a Map or plain object):
    ```typescript
    const PERCENTILE_TABLE: Record<number, number> = {
      [-2]: 2,
      [-1]: 16,
      [0]:  50,
      [1]:  84,
      [2]:  97,
    };
    ```

    For generateStdDevConcept dataset construction:
    - If answer=1 (A wins): datasetA values spread across range [mu-3*sigma, mu+3*sigma], datasetB values clustered in [mu-sigma, mu+sigma]. Use concrete small integers for readability.
    - Keep datasets short (5 values each) for readable question text.
    - Simple approach: pick spread_sigma = rng.intRange(3, 8) for winner, spread_sigma / 3 for loser. Use rng to generate 5 values around a center = rng.intRange(10, 20) * 5.

    statisticsHsHandler.ts: Import all 4 generators. Implement dispatch switch. Cast config.type:
    ```typescript
    const type = (template.domainConfig ?? {}).type as string;
    ```
    Throw descriptive error for unknown types (include the type value in the message).

    index.ts: `export { statisticsHsHandler } from './statisticsHsHandler';`
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=statisticsHs 2>&1 | tail -30</automated>
  </verify>
  <done>Integer answer tests pass across seeds 1-20 for all 5 skills. Z-score bounds test passes (all values in [-2, 2]). Handler dispatch test passes. All 5 templates have domain_specific distractorStrategy. Generator file under 500 lines.</done>
</task>

<task type="auto">
  <name>Task 3: Wire everything into existing index/registry files</name>
  <files>
    src/services/mathEngine/types.ts,
    src/services/mathEngine/bugLibrary/index.ts,
    src/services/mathEngine/bugLibrary/distractorGenerator.ts,
    src/services/mathEngine/skills/index.ts,
    src/services/mathEngine/templates/index.ts,
    src/services/mathEngine/domains/index.ts,
    src/services/mathEngine/domains/registry.ts,
    src/services/video/videoMap.ts
  </files>
  <action>
    1. src/services/mathEngine/types.ts: Add `| 'statistics_hs'` to MathDomain union (append after 'sequences_series'). This unblocks all downstream TypeScript type errors.

    2. src/services/mathEngine/bugLibrary/index.ts:
       Add `export { STATISTICS_HS_BUGS } from './statisticsHsBugs';`

    3. src/services/mathEngine/bugLibrary/distractorGenerator.ts:
       - Add import: `import { STATISTICS_HS_BUGS } from './statisticsHsBugs';`
       - Add to BUGS_BY_OPERATION: `statistics_hs: STATISTICS_HS_BUGS,`
       TypeScript requires this because MathDomain now includes 'statistics_hs' and BUGS_BY_OPERATION is Record<MathDomain, ...>.

    4. src/services/mathEngine/skills/index.ts:
       - Add import: `import { STATISTICS_HS_SKILLS } from './statisticsHs';`
       - Add `...STATISTICS_HS_SKILLS,` to the SKILLS array spread
       - Add re-export: `export { STATISTICS_HS_SKILLS } from './statisticsHs';`

    5. src/services/mathEngine/templates/index.ts:
       - Add import: `import { STATISTICS_HS_TEMPLATES } from './statisticsHs';`
       - Add `...STATISTICS_HS_TEMPLATES,` to ALL_TEMPLATES spread
       - Add re-export: `export { STATISTICS_HS_TEMPLATES } from './statisticsHs';`

    6. src/services/mathEngine/domains/index.ts:
       Add `export { statisticsHsHandler } from './statisticsHs';`

    7. src/services/mathEngine/domains/registry.ts:
       - Add import: `import { statisticsHsHandler } from './statisticsHs';`
       - Add `statistics_hs: statisticsHsHandler,` to HANDLERS object

    8. src/services/video/videoMap.ts:
       Uncomment the statistics_hs entry. The current comment reads:
       `//   statistics_hs:         'h8EYEJ32oQ8'  (Phase 85)`
       Change to an active entry: `statistics_hs: 'h8EYEJ32oQ8',`
       The videoMap type is `Partial<Record<MathDomain, string>>` — once 'statistics_hs' is in the MathDomain union (step 1), this entry is valid. No other videoMap changes needed.

    Order: Do types.ts first (enables all downstream type checking), then bugLibrary/index.ts, distractorGenerator.ts, skills/index.ts, templates/index.ts, domains/index.ts, domains/registry.ts, videoMap.ts.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -10 && npm test -- --testPathPattern="statisticsHs|domainHandlerRegistry|prerequisiteGating" 2>&1 | tail -20</automated>
  </verify>
  <done>npm run typecheck passes with 0 errors. statisticsHs.test.ts passes. domainHandlerRegistry.test.ts passes (22 operations, 175 skills). prerequisiteGating.test.ts passes (175 skills).</done>
</task>

</tasks>

<verification>
Full wave verification after all three tasks:
- `npm run typecheck` — 0 errors (including videoMap.ts statistics_hs entry now valid)
- `npm test -- --testPathPattern="statisticsHs|domainHandlerRegistry|prerequisiteGating"` — all tests GREEN
- `npm test -- --testPathPattern=distractorGenerator` — still passes (BUGS_BY_OPERATION exhaustive)
</verification>

<success_criteria>
- statistics_hs domain handler registered, 5 skills in registry (stats_stddev_concept, stats_normal_rule, stats_zscore, stats_percentile, stats_word_problem), 5 templates all with distractorStrategy: 'domain_specific'
- All generators produce integer answers via construction-from-answer pattern
- Z-score generators cap z at [-2, 2]; sigma ∈ {5, 10, 15, 20}; all answers integers
- Normal distribution: only 68% and 95% bands generated — no 99.7 decimal
- Percentile: fixed lookup table (z=-2→2, z=-1→16, z=0→50, z=1→84, z=2→97)
- 3 bug patterns with misconception description strings for AI tutor
- videoMap.ts statistics_hs entry active: 'h8EYEJ32oQ8'
- All targeted test files GREEN, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/085-statistics-extensions-domain/085-02-SUMMARY.md`
</output>
