---
phase: 089-exponential-functions-domain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/mathEngine/exponentialFunctions.test.ts
  - src/__tests__/mathEngine/domainHandlerRegistry.test.ts
  - src/__tests__/adaptive/prerequisiteGating.test.ts
  - src/__tests__/mathEngine/wordProblems.test.ts
autonomous: true
requirements: [EXP-01, EXP-02]

must_haves:
  truths:
    - "exponentialFunctions.test.ts has RED test stubs that fail at import/module level"
    - "domainHandlerRegistry.test.ts expects 26 operations and 197 skills"
    - "prerequisiteGating.test.ts expects 197 skills"
    - "wordProblems.test.ts includes exponential_functions in ALL_OPERATIONS"
  artifacts:
    - path: "src/__tests__/mathEngine/exponentialFunctions.test.ts"
      provides: "RED test stubs for exponential_functions domain (5 skills, 3 bugs, numeric answers)"
      min_lines: 80
  key_links:
    - from: "src/__tests__/mathEngine/exponentialFunctions.test.ts"
      to: "src/services/mathEngine/domains/exponentialFunctions"
      via: "import of handler, skills, templates, bugs"
      pattern: "import.*exponentialFunctions"
---

<objective>
Create RED test stubs for the exponential_functions domain. Update count assertions in existing test files.

Purpose: Establish failing test contracts that Plan 02 must satisfy -- Wave 0 RED stubs for TDD.
Output: 1 new test file + 3 updated test files, all failing until Plan 02 lands.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/089-exponential-functions-domain/089-RESEARCH.md

Reference prior Wave 0 pattern:
@.planning/phases/088-polynomial-operations-domain/088-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RED test stubs for exponential_functions domain</name>
  <files>src/__tests__/mathEngine/exponentialFunctions.test.ts</files>
  <action>
Create a new test file following the Wave 0 RED stub pattern from prior HS domains (model on polynomials.test.ts).

- Import from `@/services/mathEngine/domains/exponentialFunctions` (handler)
- Import from `@/services/mathEngine/skills/exponentialFunctions` (EXPONENTIAL_FUNCTIONS_SKILLS)
- Import from `@/services/mathEngine/templates/exponentialFunctions` (EXPONENTIAL_FUNCTIONS_TEMPLATES)
- Import from `@/services/mathEngine/bugLibrary/exponentialFunctionsBugs` (EXPONENTIAL_FUNCTIONS_BUGS)
- Import `createRng` from `@/services/mathEngine/seededRng`
- Tests to stub:
  1. "should be registered in domain registry" (handler exists)
  2. "should have 5 skills" (EXPONENTIAL_FUNCTIONS_SKILLS.length === 5)
  3. "should have 5 templates" (EXPONENTIAL_FUNCTIONS_TEMPLATES.length === 5)
  4. "should have 3 bug patterns" (EXPONENTIAL_FUNCTIONS_BUGS.length === 3)
  5. "all skills use operation exponential_functions" (every skill.operation === 'exponential_functions')
  6. "all templates use operation exponential_functions" (every template.operation === 'exponential_functions')
  7. "all templates use domain_specific distractor strategy" (every template.distractorStrategy === 'domain_specific')
  8-12. One test per generator type: exp_evaluate, growth_factor, decay_factor, doubling_time, word_problem -- each creates a template with the right domainConfig.type, calls handler.generate(template, rng), and checks:
    - result.correctAnswer.type === 'numeric'
    - result.questionText is non-empty string
    - result.operands.length >= 3
  13. "exp_evaluate computes base^exponent correctly" -- generate with known seed, verify Math.pow(base, exp) produces the correct answer value
  14. "growth_factor produces correct exponential growth result" -- verify answer = initial * factor^periods
  15. "decay_factor produces correct halving result" -- verify answer = initial / 2^periods and initial is a power of 2
  16-18. One test per bug pattern (exp_linear_thinking, exp_off_by_one_period, exp_growth_decay_swap): compute() returns a non-null number
  19. "all generators produce integer answers" -- run 20 seeds, all correctAnswer.value are integers (Number.isInteger)
  20. "all answers are bounded under 2000" -- run 20 seeds, verify answerNumericValue(result.correctAnswer) < 2000

These imports target files that don't exist yet, so all tests will fail at import/module resolution level (RED).
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test -- --testPathPattern="exponentialFunctions" 2>&1 | head -30</automated>
  </verify>
  <done>Test file exists and fails at import level (RED). exponentialFunctions.test.ts has ~20 test cases covering registry, skills, templates, bugs, generator output types, integer answers, answer bounds.</done>
</task>

<task type="auto">
  <name>Task 2: Update count assertions in 3 existing test files</name>
  <files>src/__tests__/mathEngine/domainHandlerRegistry.test.ts, src/__tests__/adaptive/prerequisiteGating.test.ts, src/__tests__/mathEngine/wordProblems.test.ts</files>
  <action>
Update count assertions to reflect the addition of exponential_functions (5 skills, 1 new operation).

**domainHandlerRegistry.test.ts:**
- Change operation count assertion from 25 to 26 (in the test name and assertion)
- Change total skill count assertion from 192 to 197
- Add `'exponential_functions'` to the ALL_OPERATIONS array
- Add `exponential_functions: ['numeric']` to the expectedTypes map (all exponential_functions skills use numericAnswer)

**prerequisiteGating.test.ts:**
- Change SKILLS.length assertion from 192 to 197

**wordProblems.test.ts:**
- Add `'exponential_functions'` to the ALL_OPERATIONS array
- Add `exponential_functions: 9` to the gradeMap (Common Core HSF-LE standards start in grade 9)

These tests will now fail because the production code still has 25 operations and 192 skills. They will pass after Plan 02 lands.
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test -- --testPathPattern="domainHandlerRegistry|prerequisiteGating|wordProblems" 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 files updated with new counts. Tests fail (RED) because exponential_functions handler doesn't exist yet. domainHandlerRegistry expects 26 ops/197 skills, prerequisiteGating expects 197, wordProblems includes exponential_functions.</done>
</task>

</tasks>

<verification>
All 4 test files should exist and produce RED failures:
- exponentialFunctions.test.ts: fails at import (module not found)
- domainHandlerRegistry.test.ts: fails on count assertions (25 != 26, 192 != 197)
- prerequisiteGating.test.ts: fails on count assertion (192 != 197)
- wordProblems.test.ts: fails on missing exponential_functions in registry
</verification>

<success_criteria>
- 1 new test file created with RED stubs
- 3 existing test files updated with new counts
- All tests fail (RED) as expected
- Test contracts match the 5 skills, 5 templates, 3 bugs, numeric answer type documented in research
</success_criteria>

<output>
After completion, create `.planning/phases/089-exponential-functions-domain/089-01-SUMMARY.md`
</output>
