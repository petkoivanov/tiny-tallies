---
phase: 089-exponential-functions-domain
plan: 03
type: execute
wave: 3
depends_on: [089-02]
files_modified:
  - src/services/mathEngine/wordProblems/templates.ts
autonomous: true
requirements: [EXP-03, EXP-04]

must_haves:
  truths:
    - "Word problem templates for exponential_functions generate without error"
    - "All word problem templates use prefix mode exclusively"
    - "AI tutor hints for exponential functions guide toward base/growth-factor reasoning without stating the factor directly"
  artifacts:
    - path: "src/services/mathEngine/wordProblems/templates.ts"
      provides: "3 prefix-mode word problem templates for exponential_functions domain"
      contains: "exponential_functions"
  key_links:
    - from: "src/services/mathEngine/wordProblems/templates.ts"
      to: "src/services/mathEngine/domains/exponentialFunctions/generators.ts"
      via: "prefix mode word problem template references exponential_functions operations"
      pattern: "operation.*exponential_functions"
---

<objective>
Add 3 prefix-mode word problem templates for the exponential_functions domain and verify AI tutor QA.

Purpose: Complete EXP-03 (word problems) and EXP-04 (AI tutor guidance). Phase 89 done.
Output: 3 new word problem templates in templates.ts. Manual QA auto-approved per user pre-authorization.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/089-exponential-functions-domain/089-RESEARCH.md
@.planning/phases/089-exponential-functions-domain/089-02-SUMMARY.md

Reference prior word problem pattern:
@.planning/phases/088-polynomial-operations-domain/088-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 prefix-mode word problem templates for exponential_functions</name>
  <files>src/services/mathEngine/wordProblems/templates.ts</files>
  <action>
Add 3 word problem templates for exponential_functions to the WORD_PROBLEM_TEMPLATES array in templates.ts. All use prefix mode exclusively (same Pitfall 5 pattern as all prior HS domains).

Template contexts (all at minGrade 9-10, targeting exponential growth/decay scenarios):

1. **Population growth** (minGrade 9, skillFilter: 'growth_factor'):
   - mode: 'prefix'
   - prefix: "A scientist is studying a bacteria colony in a lab. The colony grows at a constant multiplicative rate each hour."
   - operation: 'exponential_functions'
   - Reuses the growth_factor generator which produces exponential growth problems.

2. **Radioactive decay / half-life** (minGrade 10, skillFilter: 'decay_factor'):
   - mode: 'prefix'
   - prefix: "A chemist is tracking a radioactive sample that loses half its mass at regular intervals."
   - operation: 'exponential_functions'
   - Reuses the decay_factor generator.

3. **Investment doubling** (minGrade 10, skillFilter: 'doubling_time'):
   - mode: 'prefix'
   - prefix: "A financial planner is calculating how an investment grows when it doubles at fixed intervals."
   - operation: 'exponential_functions'
   - Reuses the doubling_time generator.

Follow the exact template structure from prior HS domains (sequences_series, statistics_hs, systems_equations, quadratic_equations, polynomials). Each template object has: id, operation, mode, prefix, minGrade, skillFilter.
  </action>
  <verify>
    <automated>cd C:/Projects/tiny-tallies && npm test -- --testPathPattern="wordProblems" 2>&1 | tail -20</automated>
  </verify>
  <done>3 prefix-mode word problem templates added. wordProblems tests pass including exponential_functions in ALL_OPERATIONS. AI tutor QA auto-approved per user pre-authorization (manual QA sign-off). Phase 89 EXP-03 and EXP-04 complete.</done>
</task>

</tasks>

<verification>
1. `npm test -- --testPathPattern=wordProblems` -- exponential_functions word problems generate without error
2. `npm test -- --testPathPattern=exponentialFunctions` -- all domain tests still pass
3. `npm test` -- full suite green
4. `npm run typecheck` -- clean
</verification>

<success_criteria>
- 3 prefix-mode word problem templates exist for exponential_functions domain
- All word problem tests pass including exponential_functions
- AI tutor QA auto-approved (manual QA sign-off per user pre-authorization)
- Full test suite passes
- Phase 89 complete: EXP-01 through EXP-04 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/089-exponential-functions-domain/089-03-SUMMARY.md`
</output>
