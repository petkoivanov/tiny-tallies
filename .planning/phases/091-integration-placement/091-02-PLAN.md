---
phase: 091-integration-placement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/mathEngine/skills/polynomials.ts
  - src/services/mathEngine/skills/exponentialFunctions.ts
  - src/__tests__/adaptive/prerequisiteGating.test.ts
  - src/components/skillMap/skillMapLayout.ts
  - src/components/skillMap/SkillMapGraph.tsx
  - src/__tests__/components/skillMapLayout.test.ts
autonomous: true
requirements: [INT-02, INT-03]

must_haves:
  truths:
    - "polynomials.foil_expansion requires multi_step as a prerequisite (not a root skill)"
    - "exponential_functions.exp_evaluate requires exponents.integer-exponent as a prerequisite (not a root skill)"
    - "The prerequisite chain linear_equations -> systems_equations -> quadratic_equations -> polynomials is fully connected"
    - "The prerequisite chain exponents -> exponential_functions -> logarithms is fully connected"
    - "The skill map renders all 27 domains without node overlap on a standard phone screen"
  artifacts:
    - path: "src/services/mathEngine/skills/polynomials.ts"
      provides: "foil_expansion with multi_step prerequisite"
      contains: "multi_step"
    - path: "src/services/mathEngine/skills/exponentialFunctions.ts"
      provides: "exp_evaluate with exponents.integer-exponent prerequisite"
      contains: "integer-exponent"
    - path: "src/components/skillMap/skillMapLayout.ts"
      provides: "Layout supporting 27 columns with minimum spacing"
      contains: "MIN_COLUMN_SPACING"
    - path: "src/components/skillMap/SkillMapGraph.tsx"
      provides: "Horizontal ScrollView wrapping the SVG"
      contains: "ScrollView.*horizontal"
  key_links:
    - from: "src/services/mathEngine/skills/polynomials.ts"
      to: "src/services/mathEngine/skills/linearEquations.ts"
      via: "prerequisite edge: foil_expansion -> multi_step"
      pattern: "prerequisites.*multi_step"
    - from: "src/services/mathEngine/skills/exponentialFunctions.ts"
      to: "src/services/mathEngine/skills/exponents.ts"
      via: "prerequisite edge: exp_evaluate -> integer-exponent"
      pattern: "prerequisites.*integer-exponent"
    - from: "src/components/skillMap/SkillMapGraph.tsx"
      to: "src/components/skillMap/skillMapLayout.ts"
      via: "computeNodePositions with dynamic content width"
      pattern: "computeNodePositions"
---

<objective>
Wire the missing cross-domain prerequisite edges for HS domains and update the skill map layout to handle 27 domains via horizontal scrolling.

Purpose: Without cross-domain prerequisites, polynomials and exponential_functions are root skills that could unlock before students learn foundational algebra/exponents. Without layout changes, the skill map has overlapping nodes at 27 columns.
Output: Complete prerequisite DAG with HS chains, horizontally scrollable skill map fitting 27 domains.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/091-integration-placement/091-RESEARCH.md

@src/services/mathEngine/skills/polynomials.ts
@src/services/mathEngine/skills/exponentialFunctions.ts
@src/__tests__/adaptive/prerequisiteGating.test.ts
@src/components/skillMap/skillMapLayout.ts
@src/components/skillMap/SkillMapGraph.tsx
@src/__tests__/components/skillMapLayout.test.ts

<interfaces>
From src/components/skillMap/skillMapLayout.ts:
```typescript
export const NODE_RADIUS = 24;

export function computeNodePositions(
  width: number,
  height: number,
  headerHeight: number,
  // ...
): NodePosition[];
```

From src/__tests__/adaptive/prerequisiteGating.test.ts:
```typescript
expect(SKILLS.length).toBe(201);  // Must remain 201 — no skills added/removed
```

From src/services/mathEngine/skills/polynomials.ts (foil_expansion skill):
```typescript
prerequisites: [],  // Currently empty — needs multi_step
```

From src/services/mathEngine/skills/exponentialFunctions.ts (exp_evaluate skill):
```typescript
prerequisites: [],  // Currently empty — needs exponents.integer-exponent
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire missing cross-domain prerequisite edges for HS domains</name>
  <files>src/services/mathEngine/skills/polynomials.ts, src/services/mathEngine/skills/exponentialFunctions.ts, src/__tests__/adaptive/prerequisiteGating.test.ts</files>
  <action>
In polynomials.ts:
1. Find the `foil_expansion` skill definition. Change `prerequisites: []` to `prerequisites: ['multi_step']`. This wires polynomials into the linear_equations chain per INT-02 spec: linear_equations -> systems_equations -> quadratic_equations -> polynomials.

In exponentialFunctions.ts:
2. Find the `exp_evaluate` skill definition. Change `prerequisites: []` to `prerequisites: ['exponents.integer-exponent']`. This wires exponential_functions into the exponents chain per INT-02: exponents -> exponential_functions -> logarithms.

IMPORTANT: Do NOT change any other skill definitions. Do NOT add or remove skills. SKILLS.length must remain 201.

In prerequisiteGating.test.ts:
3. Keep the `SKILLS.length === 201` assertion unchanged.
4. Add new test cases to verify the HS cross-domain chains:
   - Test: `foil_expansion` has `multi_step` in its prerequisites (INT-02 chain: polynomials depends on linear_equations)
   - Test: `exp_evaluate` has `exponents.integer-exponent` in its prerequisites (INT-02 chain: exponentials depend on exponents)
   - Test: `substitution_simple` (systems_equations root) has `one_step_addition` in its prerequisites (verify existing edge)
   - Test: `factoring_monic` (quadratic_equations root) has `multi_step` in its prerequisites (verify existing edge)
   - Test: `log10_eval` (logarithms root) has `exp_evaluate` in its prerequisites (verify existing edge)
   - Test: `stats_stddev_concept` (statistics_hs root) has prerequisite from data_analysis (verify existing edge)
   - Test: the DAG remains acyclic (existing cycle detection test should still pass)

The test names should be descriptive, e.g.: 'HS cross-domain: polynomials depends on linear_equations via multi_step'.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=prerequisiteGating -x</automated>
  </verify>
  <done>foil_expansion prereqs include multi_step, exp_evaluate prereqs include exponents.integer-exponent, SKILLS.length remains 201, DAG is acyclic, all 6 HS cross-domain chains verified by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Update skill map layout for 27 domains with horizontal scrolling</name>
  <files>src/components/skillMap/skillMapLayout.ts, src/components/skillMap/SkillMapGraph.tsx, src/__tests__/components/skillMapLayout.test.ts</files>
  <action>
In skillMapLayout.ts:
1. Add a constant: `const MIN_COLUMN_SPACING = 64;` (2 * NODE_RADIUS + 16px gap between nodes).
2. In `computeNodePositions`, compute a dynamic content width:
   ```typescript
   const numColumns = operations.length; // will be 27
   const contentWidth = Math.max(width, (numColumns + 1) * MIN_COLUMN_SPACING);
   ```
3. Use `contentWidth` instead of `width` for column X-position calculations (the `colSpacing` or equivalent variable that determines horizontal spacing between columns).
4. Return or expose the `contentWidth` so SkillMapGraph can set the SVG width. If computeNodePositions currently returns just an array of NodePositions, add the content width to the return value (e.g., return `{ nodes, contentWidth }` or add it as metadata). Check the current return signature and adapt accordingly.

In SkillMapGraph.tsx:
5. Import `ScrollView` from `react-native`.
6. Wrap the existing SVG element in `<ScrollView horizontal showsHorizontalScrollIndicator>`.
7. Set the SVG `width` attribute to the computed `contentWidth` from `computeNodePositions` (instead of the screen width).
8. The vertical height remains the screen height — only horizontal scrolling is added.

In skillMapLayout.test.ts:
9. Update any assertions that assumed the old column count (18 domains). With 27 domains, column positioning tests may need adjustment.
10. Add a test: given a narrow width (e.g., 400px) and 27 columns, content width should be >= 27 * MIN_COLUMN_SPACING. Verify no two nodes in the same row have overlapping X positions (distance >= 2 * NODE_RADIUS).

IMPORTANT: Do NOT change NODE_RADIUS. Do NOT filter domains out of the skill map. Do NOT add grade-based filtering — the skill map shows ALL skills.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=skillMapLayout -x</automated>
  </verify>
  <done>Skill map layout computes dynamic content width for 27 columns with minimum spacing. SkillMapGraph wraps SVG in horizontal ScrollView. No node overlap at phone-width screens. Layout tests pass with 27-column assertions.</done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern=prerequisiteGating -x` passes (DAG edges + acyclicity)
- `npm test -- --testPathPattern=skillMapLayout -x` passes (27-column layout)
- `npm run typecheck` passes (no type errors from return type changes)
- SKILLS.length remains exactly 201
</verification>

<success_criteria>
- polynomials.foil_expansion prerequisites include multi_step
- exponential_functions.exp_evaluate prerequisites include exponents.integer-exponent
- All 6 HS cross-domain prerequisite chains verified by tests
- DAG remains acyclic
- Skill map content width dynamically expands for 27 columns (MIN_COLUMN_SPACING = 64)
- SkillMapGraph uses horizontal ScrollView
- No node overlap in layout test assertions
</success_criteria>

<output>
After completion, create `.planning/phases/091-integration-placement/091-02-SUMMARY.md`
</output>
