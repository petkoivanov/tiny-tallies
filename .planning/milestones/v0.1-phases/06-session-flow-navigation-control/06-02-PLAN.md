---
phase: 06-session-flow-navigation-control
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/navigation/types.ts
  - src/navigation/AppNavigator.tsx
  - src/screens/SessionScreen.tsx
  - src/screens/ResultsScreen.tsx
  - src/__tests__/screens/SessionScreen.test.tsx
  - src/__tests__/screens/ResultsScreen.test.tsx
autonomous: true
requirements: [SESS-03, SESS-04, NAV-02, NAV-03]

must_haves:
  truths:
    - "SessionScreen renders current problem as multiple choice with 4 answer options"
    - "Tapping an answer shows correct/incorrect feedback for ~1.5 seconds then auto-advances"
    - "Back button and swipe gesture are disabled during active session"
    - "Quit button shows confirmation dialog; confirming discards session and returns to Home"
    - "After last problem, session navigates to Results screen"
    - "Results screen shows correct/total score, XP earned, and session duration"
    - "Done button on Results resets navigation stack to Home (no back-nav to completed session)"
  artifacts:
    - path: "src/screens/SessionScreen.tsx"
      provides: "Full session UI with problem display, MC answers, feedback, progress, quit button"
      min_lines: 80
    - path: "src/screens/ResultsScreen.tsx"
      provides: "Session summary with score, XP, duration, done button"
      min_lines: 60
    - path: "src/navigation/types.ts"
      provides: "Updated RootStackParamList with sessionId params"
      contains: "sessionId"
    - path: "src/navigation/AppNavigator.tsx"
      provides: "gestureEnabled: false on Session screen"
      contains: "gestureEnabled"
  key_links:
    - from: "src/screens/SessionScreen.tsx"
      to: "src/hooks/useSession.ts"
      via: "useSession hook for problem data, answer handling, feedback state"
      pattern: "useSession"
    - from: "src/screens/SessionScreen.tsx"
      to: "@react-navigation/native"
      via: "usePreventRemove for back navigation guard, Alert.alert for quit confirmation"
      pattern: "usePreventRemove"
    - from: "src/screens/SessionScreen.tsx"
      to: "src/screens/ResultsScreen.tsx"
      via: "navigation.navigate('Results', { sessionId }) on session complete"
      pattern: "navigate.*Results"
    - from: "src/screens/ResultsScreen.tsx"
      to: "@react-navigation/native"
      via: "CommonActions.reset to Home on Done press"
      pattern: "CommonActions.reset"
---

<objective>
Wire the useSession hook into SessionScreen and ResultsScreen with navigation guards, feedback display, and session summary. This transforms the placeholder screens into a functional session flow.

Purpose: This plan delivers the user-facing session experience. The child taps "Start Practice" on Home, sees problems one at a time with feedback, cannot accidentally exit, and sees a results summary at the end.

Output: Updated SessionScreen with MC answer UI, ResultsScreen with summary, navigation guards, and screen tests.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-session-flow-navigation-control/06-CONTEXT.md
@.planning/phases/06-session-flow-navigation-control/06-RESEARCH.md
@.planning/phases/06-session-flow-navigation-control/06-01-SUMMARY.md

@src/hooks/useSession.ts
@src/services/session/index.ts
@src/services/session/sessionTypes.ts
@src/screens/SessionScreen.tsx
@src/screens/ResultsScreen.tsx
@src/screens/HomeScreen.tsx
@src/navigation/types.ts
@src/navigation/AppNavigator.tsx
@src/store/appStore.ts
@src/theme/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update navigation types and AppNavigator with session params and gesture disable</name>
  <files>
    src/navigation/types.ts
    src/navigation/AppNavigator.tsx
  </files>
  <action>
Update `src/navigation/types.ts`:
- Change `Session: undefined` to `Session: { sessionId: string }` (sessionId is timestamp-based, identifies the session for screen lifecycle)
- Change `Results: undefined` to `Results: { sessionId: string }` (passed through so Results knows which session to display)
- Remove the Phase 6 comment placeholder since we're now implementing it

Update `src/navigation/AppNavigator.tsx`:
- Add `options={{ gestureEnabled: false }}` to the Session Stack.Screen (prevents iOS swipe-back gesture during session, per research Pitfall 6)
- Keep Results screen with default options (gesture is fine on results since session is over)
- No other changes needed -- headerShown: false is already global
  </action>
  <verify>
    <automated>cd C:/projects/tiny-tallies && npm run typecheck 2>&1 | tail -10</automated>
    <manual>Verify Session screen has gestureEnabled: false and types include sessionId params</manual>
  </verify>
  <done>
    - RootStackParamList has sessionId param for Session and Results screens
    - Session screen has gestureEnabled: false to prevent swipe-back
    - TypeScript compiles cleanly with updated param types
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement SessionScreen with problem display, MC answers, feedback, and navigation guards</name>
  <files>
    src/screens/SessionScreen.tsx
    src/__tests__/screens/SessionScreen.test.tsx
  </files>
  <action>
Rewrite `src/screens/SessionScreen.tsx` (currently a placeholder) to render the full session experience:

**Layout (top to bottom):**
1. **Header bar:** Session phase label ("Warmup", "Practice", "Cooldown") on left, progress indicator ("3 / 15") in center, Quit button (X icon from lucide-react-native) on right. All minimum 48dp touch targets.
2. **Problem display area:** Show the problem text (e.g., "23 + 45 = ?") centered, using large font for readability.
3. **Answer options:** 4 multiple-choice Pressable buttons in a 2x2 grid layout. Each shows the answer value. Minimum 48dp height. Use StyleSheet.create for all styles.
4. **Feedback overlay/indicator:** When feedbackState is active, show a simple text/icon indicator: checkmark icon (Check from lucide) with colors.correct (#84cc16) for correct, X icon (X from lucide) with colors.incorrect (#f87171) for incorrect. Not animated (Phase 8). Disable answer buttons during feedback display.

**Behavior:**
- On mount: useSession hook initializes the session (generates queue, starts session in store)
- Each answer tap calls handleAnswer(selectedValue)
- During feedback (1.5s), answer buttons are disabled (pointer-events or disabled prop)
- When isComplete becomes true, navigate to Results with sessionId
- Quit button (X icon) triggers the navigation guard flow:

**Navigation guard using usePreventRemove:**
- `usePreventRemove(isSessionActive, callback)` where isSessionActive comes from the hook (true while session is running, false after complete or quit)
- In callback: show `Alert.alert('Quit Practice?', "Are you sure? Your progress won't be saved.", [{text: 'Keep Going', style: 'cancel'}, {text: 'Quit', style: 'destructive', onPress: () => { handleQuit(); navigation.dispatch(data.action); }}])`
- The explicit Quit button (X icon) triggers `navigation.goBack()` which gets intercepted by usePreventRemove, showing the same dialog

**Navigation on complete:**
- When isComplete transitions to true, navigate to Results: `navigation.navigate('Results', { sessionId })`
- Use useEffect watching isComplete to trigger navigation

**Problem text formatting:**
- Access problem.operands and problem.operation from currentProblem.problem
- Format as: "{operand1} {operator} {operand2} = ?" where operator is '+' for addition, '-' for subtraction
- Answer options from currentProblem.presentation.options array, each has { value, isCorrect, bugId? }

**Styles:** Use StyleSheet.create. Use theme tokens (colors, spacing, typography, layout) from @/theme. Maintain the dark theme background. Ensure 48dp minimum touch targets on all interactive elements.

Create `src/__tests__/screens/SessionScreen.test.tsx`:
- Screen renders problem text and 4 answer options
- Tapping an answer shows feedback indicator
- Answer buttons disabled during feedback
- Quit button triggers confirmation dialog (mock Alert.alert)
- Progress indicator shows correct count format
- Session phase label updates (warmup -> practice -> cooldown)

Mock useSession hook to control test state. Mock Alert.alert from react-native. Mock usePreventRemove. Use @testing-library/react-native for render + fireEvent.
  </action>
  <verify>
    <automated>cd C:/projects/tiny-tallies && npm test -- --testPathPattern=screens/SessionScreen --no-coverage 2>&1 | tail -20</automated>
    <manual>Verify SessionScreen renders problems, handles answers, shows feedback, and guards navigation</manual>
  </verify>
  <done>
    - SessionScreen renders current problem with 4 MC answer buttons
    - Feedback indicator shows correct (green check) / incorrect (red X) for 1.5s
    - Answer buttons disabled during feedback display
    - Quit X button triggers usePreventRemove confirmation dialog
    - Progress indicator shows problem count
    - Session phase label reflects current phase
    - All screen tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement ResultsScreen with session summary and Done navigation</name>
  <files>
    src/screens/ResultsScreen.tsx
    src/__tests__/screens/ResultsScreen.test.tsx
  </files>
  <action>
Rewrite `src/screens/ResultsScreen.tsx` (currently a placeholder) to show the session summary:

**Layout (centered, card-style):**
1. **Title:** "Session Complete!" in large bold text
2. **Score display:** "{correct} / {total}" with correct count highlighted in colors.correct
3. **XP earned:** "+{xp} XP" displayed prominently
4. **Duration:** "Time: {minutes}m {seconds}s" -- format durationMs from session result
5. **Done button:** Large Pressable with "Done" text, 48dp minimum height, uses CommonActions.reset to navigate to Home (existing pattern from Phase 1)

**Data source:**
- Read sessionId from route params: `useRoute<RouteProp<RootStackParamList, 'Results'>>()`
- Read session results from store: sessionScore, sessionAnswers.length (total), sessionXpEarned, sessionStartTime
- Calculate duration: Date.now() - sessionStartTime (or accept it as computed by useSession and passed via a different mechanism)
- Actually, since useSession hook commits results to the store before navigating to Results, read from store:
  - `score` = sessionScore from store
  - `total` = sessionAnswers.length from store
  - `xpEarned` = sessionXpEarned from store
  - For duration: the hook should store the duration somewhere accessible. Options: (a) pass as route param, (b) compute from sessionStartTime in store. Use option (b): `durationMs = Date.now() - sessionStartTime` but sessionStartTime gets cleared by endSession().

  Better approach: Have useSession compute the duration before calling endSession() and pass it as a route param alongside sessionId. Update Results params to: `Results: { sessionId: string; score: number; total: number; xpEarned: number; durationMs: number }`. This avoids timing issues with store state clearing.

  Update `src/navigation/types.ts` accordingly: `Results: { sessionId: string; score: number; total: number; xpEarned: number; durationMs: number }`

**Done button behavior:**
- Calls CommonActions.reset with index 0, routes [{ name: 'Home' }] (same pattern as existing placeholder)
- This prevents back-navigation to the completed session

**Styles:** StyleSheet.create, dark theme, theme tokens, 48dp touch targets on Done button.

Create `src/__tests__/screens/ResultsScreen.test.tsx`:
- Screen renders score, XP, and duration from route params
- Done button triggers CommonActions.reset to Home
- Correct formatting of duration (e.g., "2m 30s")
- Handles zero XP and zero score edge cases

Mock @react-navigation/native (useRoute returns params, useNavigation returns dispatch mock). Use @testing-library/react-native.
  </action>
  <verify>
    <automated>cd C:/projects/tiny-tallies && npm test -- --testPathPattern=screens/ResultsScreen --no-coverage 2>&1 | tail -20</automated>
    <manual>Verify ResultsScreen shows score, XP, duration and Done navigates to Home</manual>
  </verify>
  <done>
    - ResultsScreen shows correct/total score, XP earned, session duration
    - Done button resets navigation stack to Home
    - Duration formatted as minutes and seconds
    - Navigation types updated with Results params (score, total, xpEarned, durationMs)
    - All screen tests pass
    - Full test suite passes: npm test --no-coverage
    - TypeScript clean: npm run typecheck
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm test -- --testPathPattern=screens --no-coverage` -- all screen tests pass
2. `npm run typecheck` -- zero TypeScript errors
3. `npm test --no-coverage` -- full suite passes (no regressions from updated nav types)
4. Verify SessionScreen uses usePreventRemove + gestureEnabled: false for navigation guard
5. Verify ResultsScreen uses CommonActions.reset for stack cleanup
</verification>

<success_criteria>
- SessionScreen renders problems one at a time with 4 MC answers
- Correct/incorrect feedback displays for 1.5s then auto-advances
- Navigation guards prevent accidental exit (usePreventRemove + gestureEnabled: false)
- Quit button shows confirmation dialog; confirming discards session
- ResultsScreen shows score, XP, duration; Done resets to Home
- All tests pass; TypeScript strict mode clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-session-flow-navigation-control/06-02-SUMMARY.md`
</output>
