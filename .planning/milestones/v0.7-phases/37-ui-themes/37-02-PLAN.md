---
phase: 37-ui-themes
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - App.tsx
  - src/navigation/AppNavigator.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/SessionScreen.tsx
  - src/screens/ResultsScreen.tsx
  - src/screens/SandboxScreen.tsx
  - src/screens/ConsentScreen.tsx
  - src/screens/BadgeCollectionScreen.tsx
  - src/screens/SkillMapScreen.tsx
  - src/screens/AvatarPickerScreen.tsx
  - src/components/Header.tsx
  - src/components/session/SessionHeader.tsx
  - src/components/session/CompactAnswerRow.tsx
  - src/components/session/CpaSessionContent.tsx
  - src/components/session/CpaModeIcon.tsx
  - src/components/session/ManipulativePanel.tsx
  - src/components/chat/ChatPanel.tsx
  - src/components/chat/ChatBubble.tsx
  - src/components/chat/ChatBanner.tsx
  - src/components/chat/HelpButton.tsx
  - src/components/chat/ResponseButtons.tsx
autonomous: true
requirements: [THME-01]

must_haves:
  truths:
    - "All screens render with theme-aware colors from useTheme()"
    - "AppNavigator contentStyle uses dynamic background color from useTheme()"
    - "ThemeProvider wraps NavigationContainer in App.tsx"
    - "Session and chat components use dynamic colors"
  artifacts:
    - path: "App.tsx"
      provides: "ThemeProvider wrapping NavigationContainer"
      contains: "ThemeProvider"
    - path: "src/navigation/AppNavigator.tsx"
      provides: "Dynamic contentStyle from useTheme()"
      contains: "useTheme"
  key_links:
    - from: "App.tsx"
      to: "src/theme/index.ts"
      via: "ThemeProvider import"
      pattern: "import.*ThemeProvider.*from.*@/theme"
    - from: "src/navigation/AppNavigator.tsx"
      to: "src/theme/index.ts"
      via: "useTheme() for contentStyle"
      pattern: "useTheme"
---

<objective>
Migrate screens, navigation, and core session/chat components from static `colors` import to dynamic `useTheme()` hook. Wrap App.tsx with ThemeProvider.

Purpose: First half of the 45-file color migration. Covers all screens (the most visible surfaces) plus the navigation layer and session/chat components.
Output: 21 files migrated to useTheme(), ThemeProvider active in App.tsx, AppNavigator using dynamic colors.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-ui-themes/37-CONTEXT.md
@.planning/phases/37-ui-themes/37-RESEARCH.md
@.planning/phases/37-ui-themes/37-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From src/theme/index.ts (after Plan 01):
```typescript
export { ThemeId, ThemeColors, THEMES } from './colors';
export function ThemeProvider({ children }: { children: ReactNode }): JSX.Element;
export function useTheme(): { colors: ThemeColors };
export { spacing, typography, layout };  // static, unchanged
```

Migration pattern for each file:
```typescript
// BEFORE:
import { colors, spacing, typography, layout } from '@/theme';
const styles = StyleSheet.create({
  container: { backgroundColor: colors.background },
});
export default function MyComponent() {
  return <View style={styles.container} />;
}

// AFTER:
import { useTheme, spacing, typography, layout } from '@/theme';
export default function MyComponent() {
  const { colors } = useTheme();
  const styles = useMemo(() => StyleSheet.create({
    container: { backgroundColor: colors.background },
  }), [colors]);
  return <View style={styles.container} />;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ThemeProvider in App.tsx, AppNavigator dynamic colors, and screen migrations (9 files)</name>
  <files>App.tsx, src/navigation/AppNavigator.tsx, src/screens/HomeScreen.tsx, src/screens/SessionScreen.tsx, src/screens/ResultsScreen.tsx, src/screens/SandboxScreen.tsx, src/screens/ConsentScreen.tsx, src/screens/BadgeCollectionScreen.tsx, src/screens/SkillMapScreen.tsx, src/screens/AvatarPickerScreen.tsx</files>
  <action>
    1. Update `App.tsx`:
       - Import `ThemeProvider` from `@/theme`
       - Wrap `NavigationContainer` with `ThemeProvider` (ThemeProvider must be INSIDE SafeAreaProvider but OUTSIDE NavigationContainer so AppNavigator can use useTheme())
       - Keep StatusBar style="light" (all themes are dark backgrounds)

    2. Update `src/navigation/AppNavigator.tsx`:
       - Import `useTheme` from `@/theme`
       - Inside component: `const { colors } = useTheme()`
       - Replace hardcoded `contentStyle: { backgroundColor: '#1a1a2e' }` with `contentStyle: { backgroundColor: colors.background }`

    3. Migrate each screen file (8 screens) using this mechanical pattern:
       - Change import: replace `colors` with `useTheme` in the `@/theme` import (keep spacing, typography, layout as-is)
       - Add `import { useMemo } from 'react'` if not already imported
       - Inside the component function, add `const { colors } = useTheme()` at the top
       - Move `StyleSheet.create({...})` from module scope INTO the component, wrapped in `useMemo(() => StyleSheet.create({...}), [colors])`
       - Delete the old module-scope `const styles = StyleSheet.create(...)` declaration
       - If the component uses `colors.X` directly in JSX (not just styles), those references automatically work since `colors` is now a local variable

    Important: Do NOT change any logic, layout, or non-color styling. This is a mechanical color migration only.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>App.tsx wraps with ThemeProvider, AppNavigator uses dynamic contentStyle, all 8 screens use useTheme() with useMemo styles</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Header, session components, and chat components (11 files)</name>
  <files>src/components/Header.tsx, src/components/session/SessionHeader.tsx, src/components/session/CompactAnswerRow.tsx, src/components/session/CpaSessionContent.tsx, src/components/session/CpaModeIcon.tsx, src/components/session/ManipulativePanel.tsx, src/components/chat/ChatPanel.tsx, src/components/chat/ChatBubble.tsx, src/components/chat/ChatBanner.tsx, src/components/chat/HelpButton.tsx, src/components/chat/ResponseButtons.tsx</files>
  <action>
    Apply the same mechanical migration pattern to all 11 files:

    For each file:
    1. Change `import { colors, ... } from '@/theme'` to `import { useTheme, ... } from '@/theme'` (drop `colors` from import, add `useTheme`)
    2. Add `import { useMemo } from 'react'` if not already present
    3. Inside the component function, add `const { colors } = useTheme()` as the first line
    4. Move `const styles = StyleSheet.create({...})` from module scope into the component body, wrapped in `useMemo(() => StyleSheet.create({...}), [colors])`
    5. Keep all non-color imports (spacing, typography, layout) as static imports from @/theme

    Files in order:
    - `Header.tsx` -- shared Header component
    - `SessionHeader.tsx` -- session top bar
    - `CompactAnswerRow.tsx` -- answer option rows
    - `CpaSessionContent.tsx` -- CPA mode content area
    - `CpaModeIcon.tsx` -- CPA stage indicator icon
    - `ManipulativePanel.tsx` -- manipulative display panel
    - `ChatPanel.tsx` -- AI chat panel
    - `ChatBubble.tsx` -- individual chat message bubble
    - `ChatBanner.tsx` -- chat info banner
    - `HelpButton.tsx` -- pulsing help button
    - `ResponseButtons.tsx` -- chat response option buttons

    Note for animated components (HelpButton has Reanimated): The useMemo pattern works the same way -- colors used inside `useAnimatedStyle` callbacks will reference the local `colors` variable from the closure.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>All 11 core components migrated to useTheme() with useMemo-cached styles, no static colors references remain in these files</done>
</task>

</tasks>

<verification>
- `npm run typecheck` -- clean for all migrated files (remaining unmigrated files from Plan 03 will still have errors)
- `npm test -- --testPathPattern="HomeScreen|SessionScreen|ResultsScreen"` -- existing screen tests still pass
</verification>

<success_criteria>
- ThemeProvider wraps NavigationContainer in App.tsx
- AppNavigator uses useTheme() for dynamic contentStyle
- All 8 screens migrated from static colors to useTheme()
- Header + 5 session components + 5 chat components migrated
- No behavioral changes -- only color sourcing changed
- TypeScript compiles for all migrated files
</success_criteria>

<output>
After completion, create `.planning/phases/37-ui-themes/37-02-SUMMARY.md`
</output>
