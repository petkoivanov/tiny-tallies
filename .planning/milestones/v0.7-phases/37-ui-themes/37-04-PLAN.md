---
phase: 37-ui-themes
plan: 04
type: execute
wave: 3
depends_on: ["37-02", "37-03"]
files_modified:
  - src/components/session/SessionWrapper.tsx
  - src/screens/ThemePickerScreen.tsx
  - src/screens/SessionScreen.tsx
  - src/screens/HomeScreen.tsx
  - src/navigation/AppNavigator.tsx
  - src/navigation/types.ts
  - src/components/animations/BadgeUnlockPopup.tsx
  - src/__tests__/components/SessionWrapper.test.tsx
  - src/__tests__/screens/ThemePickerScreen.test.tsx
autonomous: true
requirements: [THME-03, THME-04, THME-05]

must_haves:
  truths:
    - "Session screen displays themed ambient decoration around math problems"
    - "User can navigate to theme picker screen and see all 5 themes"
    - "User can preview theme colors before equipping"
    - "Locked themes show which badge is needed to unlock"
    - "Equipping a theme immediately changes app colors"
  artifacts:
    - path: "src/components/session/SessionWrapper.tsx"
      provides: "Per-theme decorative ambient overlay with slow Reanimated animations"
      min_lines: 80
    - path: "src/screens/ThemePickerScreen.tsx"
      provides: "Theme selection screen with preview, equip, locked state display"
      min_lines: 100
  key_links:
    - from: "src/screens/SessionScreen.tsx"
      to: "src/components/session/SessionWrapper.tsx"
      via: "SessionWrapper wrapping session content"
      pattern: "SessionWrapper"
    - from: "src/screens/ThemePickerScreen.tsx"
      to: "src/store/appStore.ts"
      via: "setChildProfile({ themeId }) to equip theme"
      pattern: "setChildProfile.*themeId"
    - from: "src/screens/HomeScreen.tsx"
      to: "ThemePickerScreen"
      via: "navigation to ThemePicker"
      pattern: "navigate.*ThemePicker"
---

<objective>
Create the SessionWrapper component with per-theme ambient animations and the ThemePickerScreen with live preview, locked/unlocked state, and badge requirements display.

Purpose: Delivers the visible user-facing features: themed session visuals (THME-03) and theme picker UI (THME-04/THME-05).
Output: SessionWrapper with 5 theme-specific decorations, ThemePickerScreen with Phase 36 picker pattern, navigation wiring, HomeScreen entry point.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-ui-themes/37-CONTEXT.md
@.planning/phases/37-ui-themes/37-RESEARCH.md
@.planning/phases/37-ui-themes/37-01-SUMMARY.md

<interfaces>
From src/theme/colors.ts (Plan 01):
```typescript
export type ThemeId = 'dark' | 'ocean' | 'forest' | 'sunset' | 'space';
export interface ThemeColors { background, backgroundLight, surface, surfaceLight, primary, primaryLight, primaryDark, correct, incorrect, textPrimary, textSecondary, textMuted: string }
export const THEMES: Record<ThemeId, ThemeColors>;
```

From src/theme/index.ts (Plan 01):
```typescript
export function useTheme(): { colors: ThemeColors };
export { spacing, typography, layout };
```

From src/store/constants/themes.ts (Plan 01):
```typescript
export const THEME_COSMETICS = [
  { id: 'ocean', label: 'Ocean', emoji: '\u{1F30A}', badgeId: 'mastery.grade.1' },
  { id: 'forest', label: 'Forest', emoji: '\u{1F332}', badgeId: 'behavior.streak.gold' },
  { id: 'sunset', label: 'Sunset', emoji: '\u{1F305}', badgeId: 'behavior.sessions.gold' },
  { id: 'space', label: 'Space', emoji: '\u{1F680}', badgeId: 'behavior.challenge.perfect' },
] as const;
```

From src/store/constants/avatars.ts:
```typescript
export function isCosmeticUnlocked(badgeId: string, earnedBadges: Record<string, unknown>): boolean;
```

From src/store/slices/childProfileSlice.ts (Plan 01):
```typescript
// themeId: ThemeId field, set via setChildProfile({ themeId })
```

From src/screens/AvatarPickerScreen.tsx (Phase 36 pattern to follow):
- Sectioned layout: regular items grid + locked unlockable items with badge requirements
- Live preview area at top
- CosmeticDetailOverlay for locked item badge info
- Navigation: registered in AppNavigator with Stack.Screen

From src/navigation/types.ts:
```typescript
export type RootStackParamList = { Home, Session, Results, Sandbox, BadgeCollection, SkillMap, AvatarPicker, Consent };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionWrapper component with per-theme ambient animations</name>
  <files>src/components/session/SessionWrapper.tsx, src/screens/SessionScreen.tsx, src/__tests__/components/SessionWrapper.test.tsx</files>
  <behavior>
    - SessionWrapper renders children unchanged (pass-through)
    - SessionWrapper renders decorative elements based on themeId from store
    - Decorative elements use pointerEvents="none" so they don't intercept touch
    - Each theme has a distinct visual: dark=subtle particles, ocean=floating bubbles, forest=swaying leaves, sunset=warm gradient bars, space=twinkling stars
    - Animations are slow (4-8 second cycles) and low opacity (0.1-0.3)
  </behavior>
  <action>
    1. Create `src/components/session/SessionWrapper.tsx`:
       - Import useAppStore for themeId, Reanimated for animations
       - Props: `{ children: ReactNode }`
       - Read `themeId` from store (fall back to 'dark')
       - Render a container View with `{ flex: 1 }` containing:
         a. A decorative overlay layer (absolutely positioned, pointerEvents="none") with theme-specific elements
         b. The children (the actual session content)
       - Theme decorations (all absolutely positioned, low opacity, slow animations):
         - **dark**: 3-4 small circles with very slow vertical drift (6s cycle), opacity 0.1
         - **ocean**: 4-5 circle "bubbles" with slow upward drift + slight horizontal wobble (7s cycle), opacity 0.15, using primary/primaryLight colors
         - **forest**: 3-4 small leaf-shaped views (rotated squares) with gentle sway (5s cycle), opacity 0.15, green-tinted
         - **sunset**: 2-3 horizontal gradient-like bars at top/bottom edges with slow opacity pulse (8s cycle), opacity 0.2, warm colors
         - **space**: 5-6 tiny dot "stars" at random positions with twinkle effect (opacity pulse 0.05-0.25 at staggered 4-6s cycles)
       - Use `withRepeat(withTiming(...))` pattern from existing AvatarCircle sparkle animation
       - Keep animated nodes under 6 total per theme for performance
       - Position all decorative elements at screen EDGES only (top 10%, bottom 10%, sides) -- nothing near center where math problems display

    2. Integrate in `src/screens/SessionScreen.tsx`:
       - Import `SessionWrapper`
       - Wrap the main session content (the SafeAreaView or outermost container) with `<SessionWrapper>...</SessionWrapper>`
       - This is a simple wrapping -- no logic changes to SessionScreen

    3. Write tests in `src/__tests__/components/SessionWrapper.test.tsx`:
       - Mock Reanimated, useAppStore
       - Test renders children
       - Test renders with default theme (dark)
       - Test decorative container has pointerEvents="none"
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=SessionWrapper</automated>
  </verify>
  <done>SessionWrapper renders per-theme ambient decorations around session content, integrated in SessionScreen, animations are slow and non-distracting</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ThemePickerScreen with preview, navigation wiring, HomeScreen entry point</name>
  <files>src/screens/ThemePickerScreen.tsx, src/navigation/types.ts, src/navigation/AppNavigator.tsx, src/screens/HomeScreen.tsx, src/components/animations/BadgeUnlockPopup.tsx, src/__tests__/screens/ThemePickerScreen.test.tsx</files>
  <behavior>
    - ThemePickerScreen shows all 5 themes in a scrollable list
    - Default dark theme is always available (no badge required)
    - 4 non-default themes show locked state when badge not earned
    - Equipped theme has visual indicator (checkmark or highlight)
    - Tapping unlocked theme equips it via setChildProfile({ themeId })
    - Tapping locked theme shows CosmeticDetailOverlay with badge requirement
    - Live preview shows a mini card with theme colors (background, surface, primary, text)
    - HomeScreen has navigation entry to ThemePicker (Palette icon)
    - BadgeUnlockPopup shows theme cosmetic text for theme badges
  </behavior>
  <action>
    1. Add `ThemePicker: undefined` to `RootStackParamList` in `src/navigation/types.ts`

    2. Register in `src/navigation/AppNavigator.tsx`:
       - Import ThemePickerScreen
       - Add `<Stack.Screen name="ThemePicker" component={ThemePickerScreen} />`

    3. Create `src/screens/ThemePickerScreen.tsx` following AvatarPickerScreen pattern:
       - Import `THEMES`, `ThemeId`, `useTheme` from `@/theme`
       - Import `THEME_COSMETICS` from `@/store/constants/themes`
       - Import `isCosmeticUnlocked` from `@/store/constants/avatars`
       - Import `CosmeticDetailOverlay` from `@/components/avatars/CosmeticDetailOverlay`
       - Read `themeId`, `earnedBadges`, `setChildProfile` from store
       - Layout structure:
         a. **Preview area** at top: A mini card showing how the selected preview theme looks -- render a small View with the theme's background, a surface-colored inner card, primary-colored button/accent, and text samples in textPrimary/textSecondary. This gives a "snapshot" of the color world.
         b. **Default theme** section: Single card for "Default Dark" (always equippable, no badge needed)
         c. **Unlockable themes** section: 4 cards from THEME_COSMETICS
       - Each theme card shows: emoji, label, mini color swatch (3-4 color dots: background, surface, primary, textPrimary)
       - Unlocked themes: tap to equip (calls `setChildProfile({ themeId: id })`), show checkmark if currently equipped
       - Locked themes: show Lock icon overlay, tap opens CosmeticDetailOverlay with badge requirement
       - Use `useTheme()` for the screen's own styling (it renders with the CURRENT theme)

    4. Add entry point in `src/screens/HomeScreen.tsx`:
       - Add a theme/palette button near the avatar area (use `Palette` icon from lucide-react-native)
       - On press: `navigation.navigate('ThemePicker')`

    5. Update `src/components/animations/BadgeUnlockPopup.tsx`:
       - Import `getCosmeticUnlockText` (already imported for avatars/frames)
       - The function was extended in Plan 01 to return theme text -- verify it is already being called. If BadgeUnlockPopup already calls getCosmeticUnlockText, no changes needed here. If not, add the cosmetic unlock text display for theme badges.

    6. Write tests in `src/__tests__/screens/ThemePickerScreen.test.tsx`:
       - Mock navigation, store, lucide icons, CosmeticDetailOverlay
       - Test renders all 5 theme names
       - Test default dark theme shows as equippable (no lock)
       - Test locked theme shows lock indicator when badge not earned
       - Test unlocked theme shows equip action
       - Test tapping unlocked theme calls setChildProfile with themeId
       - Test tapping locked theme opens overlay
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=ThemePickerScreen</automated>
  </verify>
  <done>ThemePickerScreen shows 5 themes with live preview, lock state, equip action, navigation wired from HomeScreen, BadgeUnlockPopup shows theme unlock text</done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern="SessionWrapper|ThemePickerScreen"` -- new tests pass
- `npm run typecheck` -- full TypeScript compilation clean
- `npm test` -- full suite passes (no regressions)
</verification>

<success_criteria>
- SessionWrapper renders per-theme ambient decorations in SessionScreen
- Animations are slow (4-8s cycles), low opacity (0.1-0.3), edges only
- ThemePickerScreen shows all 5 themes with preview, equip, locked state
- Default dark always available, 4 themes badge-gated
- HomeScreen has navigation entry to ThemePicker
- Equipping a theme immediately changes app colors via ThemeProvider
- All tests pass, TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/37-ui-themes/37-04-SUMMARY.md`
</output>
