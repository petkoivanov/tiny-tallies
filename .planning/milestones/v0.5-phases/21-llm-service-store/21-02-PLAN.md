---
phase: 21-llm-service-store
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/tutor/promptTemplates.ts
  - src/services/tutor/rateLimiter.ts
  - src/services/tutor/geminiClient.ts
  - src/services/tutor/__tests__/promptTemplates.test.ts
  - src/services/tutor/__tests__/rateLimiter.test.ts
  - src/services/tutor/__tests__/geminiClient.test.ts
  - src/services/tutor/index.ts
autonomous: true
requirements: [LLM-01, LLM-02, LLM-04, LLM-05]

must_haves:
  truths:
    - "Gemini client initializes lazily from API key in expo-secure-store and throws descriptive error if key missing"
    - "callGemini sends systemInstruction + userMessage to gemini-2.5-flash and returns Zod-validated text"
    - "callGemini aborts on external AbortSignal and on 8-second timeout"
    - "Prompt templates produce age-appropriate system instructions with correct word limits per age bracket"
    - "Prompt templates NEVER include correctAnswer in any prompt"
    - "Rate limiter blocks at 3/problem, 20/session, 50/day and returns child-friendly messages"
  artifacts:
    - path: "src/services/tutor/geminiClient.ts"
      provides: "Lazy singleton Gemini client with callGemini function"
      exports: ["getGeminiClient", "resetGeminiClient", "callGemini"]
    - path: "src/services/tutor/promptTemplates.ts"
      provides: "Pure functions for system instruction and hint/teach/boost prompts"
      exports: ["buildSystemInstruction", "buildHintPrompt"]
    - path: "src/services/tutor/rateLimiter.ts"
      provides: "Pure rate limit checking and message functions"
      exports: ["RATE_LIMITS", "checkRateLimit", "getRateLimitMessage"]
  key_links:
    - from: "src/services/tutor/geminiClient.ts"
      to: "expo-secure-store"
      via: "getItemAsync for API key"
      pattern: "SecureStore\\.getItemAsync"
    - from: "src/services/tutor/geminiClient.ts"
      to: "@google/genai"
      via: "GoogleGenAI constructor and models.generateContent"
      pattern: "models\\.generateContent"
    - from: "src/services/tutor/geminiClient.ts"
      to: "src/services/tutor/types.ts"
      via: "geminiResponseSchema for Zod validation"
      pattern: "geminiResponseSchema\\.parse"
---

<objective>
Build the three core tutor service modules: Gemini client (with abort/timeout), prompt templates, and rate limiter.

Purpose: These pure service functions are the building blocks the useTutor hook will compose. Each is independently testable with no UI or store dependency.
Output: Three tested service modules in src/services/tutor/ with barrel exports.
</objective>

<execution_context>
@C:/Users/petko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/petko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-llm-service-store/21-RESEARCH.md

<interfaces>
<!-- From src/services/tutor/types.ts (created by Plan 01, but types are defined in RESEARCH.md so executor can create if needed) -->
```typescript
export type TutorMode = 'hint' | 'teach' | 'boost';
export type AgeBracket = '6-7' | '7-8' | '8-9';

export interface PromptParams {
  ageBracket: AgeBracket;
  cpaStage: 'concrete' | 'pictorial' | 'abstract';
  problemText: string;
  operation: string;
  wrongAnswer?: number;
  bugDescription?: string;
  tutorMode: TutorMode;
  hintLevel: number;
}

export const geminiResponseSchema = z.object({
  text: z.string().min(1).max(1000),
});
export type GeminiResponse = z.infer<typeof geminiResponseSchema>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prompt templates and rate limiter with tests</name>
  <files>src/services/tutor/promptTemplates.ts, src/services/tutor/rateLimiter.ts, src/services/tutor/__tests__/promptTemplates.test.ts, src/services/tutor/__tests__/rateLimiter.test.ts</files>
  <behavior>
    promptTemplates:
    - Test: buildSystemInstruction with ageBracket '6-7' includes "under 8 words"
    - Test: buildSystemInstruction with ageBracket '7-8' includes "under 10 words"
    - Test: buildSystemInstruction with ageBracket '8-9' includes "under 12 words"
    - Test: buildSystemInstruction includes "NEVER reveal the answer"
    - Test: buildSystemInstruction includes CPA stage
    - Test: buildHintPrompt includes problemText and operation
    - Test: buildHintPrompt includes wrongAnswer when provided
    - Test: buildHintPrompt includes bugDescription when provided
    - Test: buildHintPrompt omits wrongAnswer/bugDescription when not provided
    - Test: NO prompt template function accepts a correctAnswer parameter (type-level enforcement)

    rateLimiter:
    - Test: checkRateLimit returns null when all counts below limits
    - Test: checkRateLimit returns 'problem' when problemCallCount >= 3
    - Test: checkRateLimit returns 'session' when sessionCallCount >= 20
    - Test: checkRateLimit returns 'daily' when dailyCallCount >= 50
    - Test: checkRateLimit prioritizes problem > session > daily (checks in order)
    - Test: getRateLimitMessage returns non-empty encouraging string for each kind
    - Test: getRateLimitMessage returns empty string for null
  </behavior>
  <action>
    1. Write tests FIRST for both modules.

    2. Create `src/services/tutor/promptTemplates.ts`:
       - `buildSystemInstruction(params: PromptParams): string` — constructs system instruction with: Math Helper persona, age-bracket word limit (6-7=8, 7-8=10, 8-9=12), growth mindset language, effort praise, NEVER reveal answer, NEVER compute math, CPA stage awareness
       - `buildHintPrompt(params: PromptParams): string` — constructs user message with problem text, operation, optional wrong answer, optional bug description, Socratic hint instruction
       - CRITICAL: No function accepts correctAnswer. This is enforced by the PromptParams type which deliberately omits it.

    3. Create `src/services/tutor/rateLimiter.ts`:
       - `RATE_LIMITS` const: { perProblem: 3, perSession: 20, perDay: 50 }
       - `RateState` interface: { problemCallCount, sessionCallCount, dailyCallCount }
       - `RateLimitKind` type: 'problem' | 'session' | 'daily' | null
       - `checkRateLimit(state: RateState): RateLimitKind` — checks in order: problem, session, daily
       - `getRateLimitMessage(kind: RateLimitKind): string` — child-friendly encouraging messages per kind

    4. Run tests, verify GREEN.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="promptTemplates|rateLimiter"</automated>
  </verify>
  <done>All prompt template and rate limiter tests pass, pure functions exported, no correctAnswer in any prompt parameter</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gemini client singleton with abort/timeout and tests</name>
  <files>src/services/tutor/geminiClient.ts, src/services/tutor/__tests__/geminiClient.test.ts, src/services/tutor/index.ts</files>
  <behavior>
    - Test: getGeminiClient returns GoogleGenAI instance when API key exists in secure store
    - Test: getGeminiClient throws descriptive error when API key not found
    - Test: getGeminiClient returns cached instance on second call (lazy singleton)
    - Test: resetGeminiClient clears cached instance so next call re-initializes
    - Test: callGemini calls models.generateContent with correct model, systemInstruction, temperature 0.7, maxOutputTokens 200
    - Test: callGemini returns validated text from response
    - Test: callGemini throws on empty response text (Zod validation)
    - Test: callGemini aborts when external abortSignal fires
    - Test: callGemini aborts after 8-second timeout (use fake timers)
  </behavior>
  <action>
    1. Write tests FIRST in `src/services/tutor/__tests__/geminiClient.test.ts`:
       - Mock `@google/genai` with jest.mock: GoogleGenAI constructor returns object with models.generateContent mock
       - Mock `expo-secure-store` with jest.mock: getItemAsync returns test API key (or null for error test)
       - Use jest.useFakeTimers() for timeout test
       - Call resetGeminiClient() in beforeEach to clear singleton between tests

    2. Create `src/services/tutor/geminiClient.ts`:
       - Module-level `clientInstance: GoogleGenAI | null = null`
       - `getGeminiClient(): Promise<GoogleGenAI>` — checks cache, reads API key from SecureStore.getItemAsync('gemini-api-key'), throws descriptive Error if null, creates and caches GoogleGenAI instance
       - `resetGeminiClient(): void` — sets clientInstance to null (for testing/key rotation)
       - `CallGeminiOptions` interface: { systemInstruction: string, userMessage: string, abortSignal?: AbortSignal }
       - `callGemini(options: CallGeminiOptions): Promise<string>`:
         a. Await getGeminiClient()
         b. Create timeoutController = new AbortController(), setTimeout 8000ms to abort
         c. Combine signals: if options.abortSignal exists, try AbortSignal.any([options.abortSignal, timeoutController.signal]); if AbortSignal.any is unavailable, fall back to manual listener pattern (listen to external signal, forward abort to timeoutController)
         d. Call client.models.generateContent({ model: 'gemini-2.5-flash', contents: options.userMessage, config: { systemInstruction, temperature: 0.7, maxOutputTokens: 200 }, httpOptions: { signal: combinedSignal } })
         e. Extract response.text, validate with geminiResponseSchema.parse({ text })
         f. Return parsed.text
         g. finally: clearTimeout

    3. Update `src/services/tutor/index.ts` barrel to export from ./geminiClient, ./promptTemplates, ./rateLimiter in addition to ./types.

    4. Run tests, verify GREEN. Then run `npm run typecheck`.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=geminiClient && npm run typecheck</automated>
  </verify>
  <done>Gemini client tests pass, lazy init works, abort/timeout works, Zod validates response, barrel exports all service modules, typecheck clean</done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern="promptTemplates|rateLimiter|geminiClient"` — all service tests pass
- `npm run typecheck` — no type errors
- `npm test` — existing tests unbroken
- No correctAnswer parameter in any prompt template function signature
- gemini-2.5-flash is the model string
- Temperature 0.7, maxOutputTokens 200
</verification>

<success_criteria>
- Three service modules (geminiClient, promptTemplates, rateLimiter) exported via barrel
- All pure functions have unit test coverage
- Gemini client handles missing API key gracefully (throws, doesn't crash)
- AbortController + 8s timeout pattern implemented with fallback for AbortSignal.any()
- Rate limiter uses configurable RATE_LIMITS constants
</success_criteria>

<output>
After completion, create `.planning/phases/21-llm-service-store/21-02-SUMMARY.md`
</output>
